# Project defaults; explicit session or spawn choices can override them.
model = "gpt-6-astra"

[agents]
enabled = true
default_subagent_model = "gpt-5.6-luna"
default_subagent_reasoning_effort = "medium"
max_concurrent_threads_per_session = 3
