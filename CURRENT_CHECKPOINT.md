# Current checkpoint

This file is the frequently refreshed active-work, branch, and verification stamp.

- Implemented inventory and revision history: [`CHANGELOG.md`](CHANGELOG.md)
- Remaining work and gates: [`plan.md`](plan.md)
- Supporting-document roles: [`docs/README.md`](docs/README.md)
- Dated entries older than the last three build days: `docs/archive/CHECKPOINT_ARCHIVE_*.md` (2026-09-18/19 in `_2026-09-18_2026-09-19`,
  2026-09-03 to 2026-09-17 in `_2026-09-03_2026-09-17`, 2026-09-01/02 in
  `_2026-09-01_2026-09-02`, 2026-08-31 and earlier in `_2026-08`)

---

## Active state at a glance

**Read this block first.** It answers "where are we?"; the dated entries below are the
record behind it and the newest wins if they disagree. Refresh it on every handoff: one
sentence per cell, one line per gate, the clause behind it in the entry named beside it.

| | |
|---|---|
| Fix waiting for `main` | **SP1 / SP2 setup-score repair is reviewed and integrated on an isolated branch.** No score change from this work is live. |
| Agent settings | **2026-09-15, trader-directed: the project Astra model pin is removed.** Session choice selects the lead. |
| Latest work | **2026-09-22 trader-directed setup-score repair:** correct missing chart inputs, score snapshots and five-session grading (SP1), and small-sample PQS bonuses (SP2). The full Sep22 assessment was approved. Existing TJ-9Q journal repair passed on a copy; live gate #162 stays owed. |
| Working branch | **`codex/setup-score-repair-2026-09-22`**, based on `main` `fb95854e`; both reviewed fixes are merged here. The desk checkout stays on `main`. |
| Unmerged / open | **SP1 / SP2 reviewed and integrated; targeted checks, ruff, smoke and selftest passed. Full suite had a fixed fixture omission plus the known timing flake, so no clean full-suite exit is claimed.** Phase 0.33 code, including TJ-9E and the per-trade Save, is already on `main`; the per-trade Save still owes its own review. TJ-8 and existing live gates remain open; deferred TJ-14C / TJ-12F / TJ-6M / TJ-7 / TJ-9E follow-ups stay in plan.md. ONE pytest suite at a time. Existing CLAUDE size debt is unchanged. |
| Next action | The trader may choose when to move the reviewed branch to `main` and restart the desk; a clean full-suite rerun is still useful at that gate. Then observe fresh scores and run the prospective 20-entry-session trial plus five-session outcome wait. Only three historical entry days have a pre-close Points observation and mature outcome; archived features cannot rebuild the full v2 score. Live journal gate #162 stays trader-only. |
| Trader actions owed | **No input needed for this build.** Live journal migration remains gate #162: trader only, desk down, market closed, outside 22:00–06:00 Pacific; exact commands and backup/undo are in DESK_INTERNALS TJ-9Q. Confirming trade labels remains the trader's click. Existing Mentor exit-note/prediction clicks, model probe and Phase 0.33 live checks remain owed in the gates below. |
| Last verified baseline | **Isolated SP integration, 2026-09-22:** full suite 10,293 passed, 14 skipped, 72 subtests, 2 failed: new fixture metadata was then repaired and the known `test_ws_10a_scan_freshness` timing check passed alone. The 78 affected/new tests passed after repair; ruff clean, smoke 7/7, source selftest 97/97. No clean full-suite exit after the fixture edit. Inherited Sep21 baseline remains 10,263 passed / 14 skipped / 72 subtests with the same timing flake. |
| Frozen exe | **Rebuilt and verified 2026-09-16: source and frozen both 87/87**, `(frozen)` stamp. The desk runs from SOURCE. |
| Desk | **Last recorded restart: 2026-09-21 12:06 Pacific, source `main` `b4c1c4f7`, IB connected.** `fb95854e` reconciles that restart in docs. This Sep22 work has neither restarted the desk nor changed its checkout or live stores; each restart needs the trader's word. |

### 2026-09-22 - setup-score repair approved (SP1 / SP2, isolated build)

The trader said: **"Go ahead and make the fixes as per the full assessments then reconsider scoring changes."** `plan.md` SP names the exact scoring/data/evidence seams; this is their ask-first authorization. SP1 pins the legacy Points calculation, removes missing-data clean-path credit, carries same-scan chart facts, records score revisions and checks them against five exchange sessions without splitting tied values. SP2 pins the old PQS, counts finite representative closed episodes, uses coherent win counts and measured-session coverage, and withholds the no-loss/full-payoff bonus. Both fixes are integrated on this isolated branch: combined targeted checks passed 156 tests plus 5 subtests. SP1 review reproduced 20 red with base production code and 21 green on the fix; SP2 independently reproduced all ten new tests red against base and green against the fix. No new fitted weights or personal-fit rank has been applied.

**Trade-data readiness, verified on a private copy:** existing TJ-9Q `--apply` moved 231 fills, refused zero, retained 621 executions / 216 trades / 189 annotations, stranded zero annotations, left no pending repair, and created a backup. No live import switch or journal changed. The existing Journal and Mentor already own Confirm / Correct; labels and gate #162 remain the trader's act. **Evidence limitation:** 709 of 3,881 old Points rows were logged at or after their entry-session close. Only three entry sessions have pre-close logs with mature five-session outcomes in the copy; their archived features lack complete same-scan S/R provenance. A pure S/R-only proxy changed the rankings without improving their top-third favorable rate, so it is not evidence for a new weight. This reconstruction is not proof of future returns. The full run result and its limits are recorded below.

**Verification after integration:** combined focused checks 156 passed plus five subtests. Full suite reached 10,293 passed, 14 skipped and 72 subtests, with a new fixture-contract omission and the pre-existing WS-10A timing flake as its two failures. Both failed checks were re-run after the fixture correction with the SP tests in one 78-pass selection; the timing check passed alone. Ruff passed; smoke 7/7 and source selftest 97/97 passed against scratch data roots. This does not turn the earlier full-suite exit green. The score code and docs remain on the isolated branch; `main`, the running desk and live journal were not changed.

### 2026-09-21 - TJ-9E MERGED (`7230b30d`) and the per-trade Save merged onto it (`182f3e08`)

Active item: TJ-9E - **the Mentor tells an EXIT from an ENTRY**. Trader, this morning: *"trade mentor should be able to differentiate between trade entrys and exits. trade exits should ask for 'why did you exit, what emotions did you have, what technicals were you observing' ideally we can just write it out and the AI fills this stuff in overnight. trade entrys are good the way they are"*. Branch `claude/tj9e-exit-notes`, tip `1876bb08`, merged into `lead/p033-integration2` as **`7230b30d` after FOUR review rounds by reproduction: NO-GO, NO-GO, NO-GO, GO in round 4** (the merge message says round 4 "was still running" - the GO arrived minutes later at `1876bb08` and nothing moved after it). Entries are untouched (`scripts/trade_mentor_ai.py`'s diff is EMPTY over the whole branch). An exit is any trade with a closing fill in the reviewed session, whenever it opened: ONE forced free-text box with two answer-state buttons and no dropdown, the row surviving an EMPTY `missing` so a swing closed last week is asked about at all; the words append-only as `EXIT_NOTE_RAW` written BEFORE anything reads them, a blank refused and a failed append LOUD; provenance ruled by the EXIT's session (116 of 180 live closed trades exit on a day they did not open); one ask per (trade, exit session), so a two-session exit - 13 live - is two boxes on two mornings. The nightly stage-2 slot `exit_note_fields` drafts `why` (new closed vocabulary `exit_reasons_v1`), `felt` (TJ-7's list through ITS loader) and `watching` (up to three exact quotes), every value a character span that must reproduce its quote, from an ALLOW-LIST request holding the note's words, the symbol, the side and the two code lists and nothing else; `verify_reply` re-checks every bound, one bad value rejects the WHOLE reply byte-identically, nothing waiting means no model call, one call per note, at most `EXIT_NOTES_PER_NIGHT` (20) a night. A reading is `provisional` until the trader's own Confirm / Correct - the ONE writer of `EXIT_NOTE_FIELDS` - keyed by `check.exit_key` = `<trade>@<exit session>`, with ONE home (the questions-area draft row), offered for `EXIT_DRAFT_OFFER_SESSIONS` (5) sessions by the REGISTRY's budget of three, never against rewritten words (`Rewrite` is the door to a second note), never confirmed by age. `unexplained_exit_count` makes the 09:00 check RIDE. Readers: Day Review's trades section and `day_report_card.exit_note_counts` ("exits explained K of N, C of those confirmed by you", integers only). Fixed on the way: `journal_store` now NAMES its three trade statuses and the Mentor imports them, so the seven live `CLOSED_PARTIAL` trades stop being invisible to the 09:00 check. **Nine blockers closed across the three NO-GO rounds**, the sharpest being the timeline: a trade exits Monday, the note is typed Tuesday 09:00, Tuesday NIGHT drafts it, and Wednesday's card reviews TUESDAY - so the Confirm click could never arrive until a waiting reading was offered on its OWN clock, independently of the session the card reviews. Reviewer gates at `1876bb08`: TJ-9E 105 passed; the Mentor selection 368 passed / 0 errors; all of that plus the ten order pins and `test_tj13a_night_slates.py` in ONE process 697 passed; ruff clean; source selftest 97/97; no ask-first file, no new `.md`, no live store touched.
**Merged onto it the same day (`182f3e08`): the per-trade Mentor Save** (branch `claude/mentor-stores-answers-2026-09-21` `0caf2bba`, built by a second session from the trader's own report, entry below) - **no reviewer round of its own, merged on the trader's instruction to combine today's work, and a review is OWED.** Nine conflict hunks in three files, resolved by the lead: the peer's per-trade shape IS the gate and TJ-9E's exit box is one more FIELD of its trade (`_open_fields` asks `_exit_is_open`), filing a trade writes its exit note FIRST, `_drop_trade_block` forgets the block's exit widgets, and `MainWindow` keeps both lanes. `tests/test_tj9e_per_trade_save.py` pins what neither branch could test alone. Gates **#176-#180** (TJ-9E) and **#181** (the Save) owed; the combined FULL suite is the lead's next action. Long form: DESK_INTERNALS "TJ-9E"; CHANGELOG Recent changes; follow-ups: plan.md's TJ-9E entry.

### 2026-09-21 - the Mentor stores an answered trade (lead fix from the trader's report, MERGED `182f3e08`)

Trader, gate #154's first live morning: *"trade mentor asks me about my trades over and over despite me already answering. if i answer the questions about a trade please then dont ask for it again just store that info."* Measured on a COPY of the live journal at 09:02 PT: ONE `RECALLED` row ever, zero `RECALLED_RAW`, no setup confirmed from the card, and five trades on that morning's card - one Save over twenty dropdowns. Branch `claude/mentor-stores-answers-2026-09-21` off `main` `3cc81cf4`: forced is now PER TRADE (each block has `Save this trade`, the bottom Save files every answered trade, a filed trade leaves the card alone), typed words beside a blank dropdown and the trade's one raw note are answers, and `_mentor_answered` finds a trade's answer by `trade_id` so a `once` question does not return when the position closes. Six new tests, five RED before the fix; one older gate assertion in `test_tj14b_card_merges_a_fresh_task.py` rewritten to the per-trade rule. No detector, score, alert or store schema touched; no ask-first file. **Gate #154 reads differently now**: answer ONE trade, press its Save, and the next card must not list it. **MERGED** onto TJ-9E as `182f3e08` the same day, with no review of its own (a review is OWED): the anticipated overlap - TJ-9E editing the same `_pending_answers` / `save_trade_check` - was resolved by folding its exit box into `_open_fields` as one more field of its trade. Gate **#181**. Verification: full suite on `0caf2bba` **10,156 passed, 14 skipped, 72 subtests, 0 failed** (2026-09-21 daytime, AI lock free); ruff clean. A first run with `-x`, sharing the machine with other pytest runs, stopped on `test_g7_speed_pass` (a timing test; 21/21 alone and green in the full re-run). Smoke and selftest not re-run - no engine, import or asset changed. Long form: DESK_INTERNALS, the TJ-9 entry's 2026-09-21 paragraph.

### 2026-09-20 - TJ-7 MERGED; every building packet of Phase 0.33 is merged

Branch `claude/tj7-mood-fields` (tip `bcfdd918`; reviewer **GO in round 1** at `83318aa2`, no blockers), merged `b63db7af` into `lead/p033-integration2`, with the lead's guard amendment `ae7c06c7` and the follow-up merge `0a0a0be4` on top. **Not on `main`, so nothing of this is live until the trader says to fast-forward.** **This was the LAST building packet of Phase 0.33.** What it is: **a mood is a field the desk REPORTS, and nothing acts on it.** ONE additive `market_journal` row key `mood` (schema `trader_mood_v1`) holds a 1-5 `score` off `MOOD_SCALE`, at most two `state_tags` and a `process` block (`followed_plan`, a `note` of at most 200 characters). It is `{}` on a row nobody clicked, **ABSENT on every old row** (43 of the 84 live journal rows on 2026-09-20) and never a default - all three absences read `None`, because a reader that made one a neutral 3 would invent a feeling nobody had - and `mood_of` / the point-in-time `mood_at` are its ONLY readers. `MoodFieldError` is LOUD at `build_entry` and the same question is asked again at `is_publishable`, the gate every write passes: a score off the scale, a tag outside the versioned closed vocabulary `state_tags_v1.json` (own loader `scripts/trader_state_tags.py`, which OWNS the cap), an unknown plan answer, a 201-character note **REFUSED and never truncated**, and a mood on a MACHINE row refused through `is_machine_entry`'s own rule. ONE strip (`scripts/ui/widgets/mood_strip.py`) serves both surfaces - the Mentor's `day_close` hook (no new registry kind, the budget of three unmoved) and the desk's journal tab - optional, nothing pre-selected, never greying TJ-9's Save, a mood with no plan answer STILL FILED, and `set_questions` became a MERGE for the whole questions box so a half-clicked answer survives the same questions being offered again. The day pack's `mood` section mints citable, time-ordered ids through the ONE seam `day_review_pack.mood_source_id` (shared with the Day Review payload), projects each item through `MOOD_ITEM_FIELDS`, and keeps a clock-free `inputs_hash` that MOVES when a mood moves; `week_review_narration.EVIDENCE_KEYS` and `improvement_ideas.EVIDENCE_KEYS` each gained the ONE key `mood` and **neither gained a `MEASURABLES` entry**; `market_read_grades.context_for(..., mood_entries=)` takes only the mood known AT THE STAMP; Day Review prints ONE mood line off the ONE payload. **THE TAGGER NEVER SEES A MOOD**, and TJ-7 adds **no nightly slot**, so the NINE order pins do not move; source selftest stays **97/97** (89 lazy engine imports + 8 asset checks, verified in `scripts/selftest.py`).
**The invariant was re-proven by TRACING importers, not by reading source.** The eleven modules that can reach a mood include no detector, score, alert, watchlist, Focus, review queue, `review_learning` or cohort grader; the five modules that touch `review_policy.json` import none of the four mood modules; `context_for` has exactly ONE caller (the Day Review worker); and the tagger's evidence body, built over a row scored 1, tagged `tilted` and noting "I was furious about the gap", contains none of it.
**The lead's guard amendment `ae7c06c7`, and why.** TJ-7's green guard flagged ANY file under `scripts/` whose SOURCE held both `review_policy` and a mood word - and three of the modules the packet REQUIRED TJ-7 to touch state that very invariant in prose, so **the guard was unsatisfiable by its own packet**. The builder took the only move open to him (a builder may not weaken a test) and reworded four shipped docstrings; the cost was that the searchable token `review_policy.json` left three modules stating a sec-5 invariant. The amendment narrows the guard to **CONTACT with the policy** - a module that imports, loads, drafts or saves it (five today, asserted non-empty) and also handles a mood - and restores all four sentences.
**The follow-up merge `0a0a0be4` (16 new tests, 10 red before): a misclick is not an answer.** The faces shipped as an exclusive `QButtonGroup`, Qt will not un-check the checked member of one, and the Mentor reads "a score is set" as "the strip was touched" - so a finger landing on 2 instead of 3 filed a mood nobody meant, permanently. One face at a time is now enforced in the widget's own handler and a second click on the chosen face clears it, provably identical to never having touched the strip; the machine-row refusal became behavioural; and `mood_source_id` became the ONE id seam, while the page still BUILDS its own section rather than reading the written pack, because an evening mood arrives after the post-close pack.
**Lead decisions ratified, each the trader's to overrule.** (a) **ONE row key `mood`** - one block the readers can be counted on one hand beats three loose keys every reader must learn. (b) **A mood with no plan answer is still filed** - a click the trader made is never lost because another question on the same row went untouched. (c) **An over-long note is REFUSED, not truncated** - the ledger is append-only and has no second chance; the UI caps its own input instead. (d) **The mood rides `day_close`** - a second registry kind would need a second named consumer and would spend one of the budgeted three. (e) **Both closed evidence lists gained `mood`** - the week story and the ideas slot cannot cite what they were not handed. (f) **A second click clears a face** - a misclick is not an answer. (g) **The payload MINTS through the pack's seam rather than reading the pack** - an evening mood arrives after the post-close pack was written. (h) **The mood guard checks CONTACT with the review policy, not words in a docstring** - as written it was unsatisfiable by its own packet.
**Two follow-ups recorded, neither built** (plan.md 12.5): a verifier rule against the night pairing a mood with a RESULT - gate **#175** stands in its place, and the one live fence today is TJ-6's closed `MEASURABLES` - and a re-offered Mentor subject whose prompt or options CHANGED keeping its OLD label (latent; no shipped kind varies either for a fixed `subject_id`). On today's desk the trader reads **"No mood recorded yet for this session (n 0)."** - a count, never a percentage - because the live journal held 84 rows and ZERO moods. Gates **#151** (three parts; its citation half unreadable until a night writes a pack and a story for a session carrying a mood) and the new **#175** owed. Long form: CHANGELOG Recent changes; DESK_INTERNALS "TJ-7".

### 2026-09-20 - TJ-6 MERGED

Branch `claude/tj6-ideas` (tip `d78945f0`; two review rounds by reproduction - **NO-GO** in round 1, **GO** in round 2), merged `a7809d7c` into `lead/p033-integration2`, with the lead's ninth slot-order pin `be435cd7` and one fix `41d3f759` on top. **Not on `main`, so nothing of this is live until the trader says to fast-forward.** What it is: the trader's *"the local AI can have a voice somewhere where it offers ideas of what we can improve based on what it reads"* built as the stage-3 slot `improvement_ideas` (`scripts/ai_jobs/improvement_ideas.py`), appended LAST behind `setup_research`, `uses_model=True`, `RESERVE_MINUTES = 10.0`, `max_attempts=2`, night-only and on every night's slate. It reads the last five day packs and their stories, the week's walk-away totals, TJ-15's contrast groups, the two measurables the desk really computes and a checked-in 30-line program card - **`EVIDENCE_KEYS` is CLOSED** and holds no bar, tape, tick, lake or warehouse section - asks the MEDIUM local model ONCE and re-checks every bound from the INPUT afterwards. A fabricated citation or a fourth USABLE idea rejects the answer WHOLE with the ideas store AND the trader's state file byte-identical; an idea with no evidence, no measurable, an unknown kind or text over 280 characters is DROPPED alone and counted by reason into the ledger row, the one predicate `drop_reason` serving both the verifier and the writer because the item schema is validated SHAPE-ONLY (the shared validator's `enum` and `maxLength` would turn two named DROPS into whole rejections). The store is APPEND-ONLY and folded by `idea_id`, a repeat inside 60 EXCHANGE sessions appending a row with a higher `seen_count`, a dismissed idea never returning (checked on the normalised TEXT, because the id carries its first session). **KEEP and DISMISS are the TRADER's clicks** through `scripts/ui/widgets/ideas_card.py` on BOTH pages - validated, fail-closed, temp-and-rename with every other entry byte-identical, one write in flight, its floor a size hint, `ideas` riding Day Review's and the week's ONE payload - and a Keep FREEZES its baseline from the night's LAST WRITTEN pack (`miss_contrast.read_latest`) or the day packs' own report-card lines and names which; `unmeasured` never a zero, a second Keep never re-freezing. A later reading says higher or lower **only when the two Wilson intervals (`evidence_contrast.rate`, imported) do not overlap**, else "no clear change", and never a rate under `MIN_REPORTABLE_N`. "For WISHLIST - copy" is selectable TEXT; nothing writes `WISHLIST.md`.
**Round 1's blocker, in plain words: 16 model calls a night on a desk with zero packs.** A night that asked and stored nothing returned `ledger.STATUS_SKIPPED` with no artifact, and `skipped` is in neither `ledger.CANONICAL_COMPLETION_STATUSES` nor `ledger.ATTEMPT_STATUSES`, so neither the runner's already-done check nor `max_attempts=2` bit while the scheduled task repeats every 30 minutes for 8 hours. The reviewer measured it (three droppable-idea runs, one model call each, identical; 16 `skipped` rows leaving `completed_jobs` empty) on a home folder holding **zero `pack.json`** - the exact shape of the first nights after this merge. Fixed in `d78945f0` and re-measured in round 2 through the REAL `runner.run_slots`, 16 consecutive passes per case: zero packs **0** model calls, all-dropped **1** (`ok`), a whole rejection **2** then the cap, a normal night **1**; a stale, corrupt or empty marker fails OPEN, and a marker write that raises keeps the night `ok` and loses no idea. Round 1's two advisories became rules in the same commit: a row arriving mid-write comes in DISABLED, and `0.5000 (n 30)` against `0.5001 (n 50,000)` now reads "no clear change". **The hard invariant held under attack**: the only importers are the runner slot, the two page services, the card, the two panels and `project_paths`; no `scripts/` sweep is rooted at `PERSISTENT_DATA_DIR`, so the two files are swept by nothing; and six replies that tried to keep, dismiss, mark or path-write changed nothing but the ideas store, `ai_ideas_state.json` byte-identical in all six with no `.tmp` anywhere.
**Lead decisions ratified, each the trader's to overrule.** (a) **Ship with the TWO measurables that exist** - a Mentor question's answer mix has no pooled reader, and the registry being CLOSED is what makes adding it later one safe entry (follow-up **TJ-6M**, no wave). (b) **A Keep reads the last written pack, never a 20-session build on a click** - a contrast build behind a button is a page freeze, and the frozen reading records WHICH pack it came from so the before is a fact with an address. (c) **Nothing to cite means NO model call** - a night with no citable id would make every citation read as a lie, and "tonight carries nothing to cite" is the honest answer rather than a whole rejection. (d) **Asked once is done (`ok`, not `skipped`)** - `skipped` is in neither `CANONICAL_COMPLETION_STATUSES` nor `ATTEMPT_STATUSES` and the task fires 16 times a night, so only an `ok` row (plus the `ai_ideas_asked.json` marker beside the store, never the trader's state file) stops the later passes. (e) **The cap of three counts USABLE ideas, before the dismissed filter** - one predicate `drop_reason` for the verifier and the writer, conservative on purpose, because two answers about what a usable idea is would eventually disagree. (f) **Higher or lower only when the intervals do not overlap** - the desk's one Wilson, imported from `evidence_contrast.rate`, because two unequal samples that differ in the fourth decimal are not a change the trader should act on.
**Lead amendment `be435cd7` and fix `41d3f759`:** the full suite on the trial merge found a NINTH slot-order pin - `tests/test_setup_research_pipeline.py` asserted `setup_research` is the LAST slot, in a file TJ-6 never touched; it now says `setup_research` ends stage 3 and only `improvement_ideas` may follow it. That is the **second time in one day** a targeted run could not see another packet's pin (TJ-5's was R4's minimum-height count). The fix unlinks the asked marker's temp file when the rename fails. Source selftest stays **97/97** (no new lazily-imported module; the card reaches it statically). Trial-merge suite 21:44 PDT: **10,048 passed** plus the ninth pin; the full suite on the real merge is **OWED** after the nightly AI lock frees (~06:00 PDT 2026-09-21). On today's desk - no `ai_ideas.jsonl`, no `ai_ideas_state.json`, zero packs - Day Review reads *"Nothing yet - the desk's AI writes up to three ideas a night, each one citing your own sessions, and you keep or dismiss each one here."* and Week Review the card with `Kept 0 of 0`. Live gates **#150** (as written), **#172**, **#173** and **#174** owed. Long form: CHANGELOG Recent changes; DESK_INTERNALS "TJ-6".

### 2026-09-20 - TJ-5 MERGED

Branch `claude/tj5-week-review` (tip `e93ffe49`; reviewer **GO in round 1** at `f7fac9cb`, no blockers, with one follow-up commit `ea8f8bdc` for five cheap advisories), merged `1b9d77e0` into `lead/p033-integration2`. **Not on `main`, so nothing of this is live until the trader says to fast-forward.** What it is: the trader's *"5 of these days collated into one tab … to see if I was right, to see if I chased in bad news environments, and to compare what I actually said to what I did"* built as Weekend Prep's FIRST step - five DAY cards (headline, were-you-right tally, chased flag, said-vs-did line, a small SPY sparkline), the overnight week story, the week's walk-away totals and a deterministic strip of TJ-12's report-card lines re-cut by exchange week for four weeks and for the month to date. **The page computes nothing**: ONE payload (`weekend_prep_service.read_week_review`, the week strip and the review-learning callouts inside it) on ONE worker, single-flight (three clicks -> one read, the button re-enabled on EVERY ending including a raise), five sparklines built once and only repainted, and the tests prove the boundary by monkeypatching `build_pack`, `day_report_card.build`, `week` and `run_week_review_narration` to RAISE while the page still renders. A day nobody packed says `not packed` with `no reads graded` - never blank, never `0 right of 0` - and a day whose line the desk could not BUILD is carried as `unreadable_sessions` and said, while the counts do not move. The Saturday-only slot `week_review_narration` narrates only the week's own packs: a CLOSED `EVIDENCE_KEYS` (no bars, no lake, no journal stream - 15 keys and 19,121 bytes in the captured request), bounds taken from the INPUT and re-checked by the verifier after the reply, session-qualified citations, whole rejection with the prior file byte-identical, an unchanged-hash skip, a deterministic scaffold and ZERO model calls under three narrated days, and exactly ONE call otherwise - on the MEDIUM local model until TJ-13B's probe row exists, the ledger saying why.
**Reviewer numbers.** 21 attacks on the narration, 21 whole rejections, the prior week file byte-identical after every one, no `.tmp` left behind, status always `ledger.STATUS_FAILED`; gate #170's sums re-derived by hand (9/8/5 and 13/11/6); the payload measured 0.029 s and 0.5 MB of peak on a 20-session tree; **37 `read_pack` calls per page open RECORDED, not repaired** (29 ms today, scales with real pack size). On the live store - three session folders, ZERO `pack.json` - the page will read `0 of 5 sessions have facts` and *"No week story yet. The Saturday night slot writes it from the week's own packs; nothing is fetched or narrated by this page."*
**Lead decisions ratified, each the trader's to overrule.** (a) The pooling lives in ONE place, `day_report_card.week_from_cards`, because `week(sessions)` needs TJ-11's `WalkawayDay` objects a pack does not carry - and a week pooled from cards names NO best family and says why, because a best of five day-winners is a ranking of days, not of families. (b) The slot sits BEFORE `ticker_briefs`: the briefs reserve 120 minutes and the week story is what the trader opens Weekend Prep to read. (c) Saturday-only goes through the EXISTING `runner.WEEKEND_ONLY_SLOTS` - a second constant would be a second place to forget. (d) `provider._local_setting` (reading a setting through BOTH import identities of `project_paths`) is ACCEPTED: production has one file and cannot diverge, and the cleaner fix - patching the seam `ai_jobs` actually reads in the TEST helper - would have moved TJ-13B's landed tests to fix a test-only ambiguity. (e) The unmeasured reserve is the call TIMEOUT plus a named margin (40 min), because 30 minutes beside a 1,800 s timeout left nothing for the model load and the write; a measured probe still wins. (f) An unreadable day is NAMED and never counted as quiet, and its counts stay out of `n` - adding it would invent a denominator for a measurement nobody made.
**Lead amendment `e93ffe49`:** the week sparkline states its floor as a size HINT - the full suite on the trial merge caught a second minimum-height setter breaking R4's one-owner pin `test_the_ten_row_floor_is_one_constant` in a file TJ-5 never touched, which no targeted run could see. `selftest.LAZY_ENGINE_MODULES` gains `ai_jobs.week_review_narration`: source selftest **96/96 -> 97/97** (verified on this tip). Full suite on the merge: **PENDING the lead's run**. The slot-order pins are now EIGHT files (plan.md 12.3): `tests/test_tj5_week_slot_and_slate.py` pins the adjacency `observation_tags` -> `week_review_narration` -> `ticker_briefs`. Live gates **#149** (re-worded: ONE model call, MEDIUM-local until the probe row exists, and UNREADABLE until three of a week's sessions are packed), **#170** (pinned as a number; the live sum on the desk's own week still owed) and **#171** (new: one `week_review_narration` row on Friday's session the first Saturday night, none on a weeknight) owed. TJ-6 is UNBLOCKED. Long form: CHANGELOG Recent changes; DESK_INTERNALS "TJ-5".

### 2026-09-20 - TJ-12 MERGED

Branch `claude/tj12-report-card` (tip `1a88d4a4`, THREE review rounds by reproduction against read-only copies of the live stores - NO-GO, NO-GO, GO), merged `843a3f02` into `lead/p033-integration2`. **Not on `main`, so nothing of this is live until the trader says to fast-forward.** What it is: the trader's *"I want what I missed to be very apparent. I want what I did well with to also be very apparent."* built as SIX deterministic lines heading Day Review above the story - Did well, Missed, Your reads, Congruence, Process, How fresh - each `{key, text, n, measured, target}` and each clickable to the table or section it came from. `scripts/day_report_card.py` is PURE and **computes no new statistic**: every number is read from the owner that measured it (`walkaway_day`'s `REAL_MISS_V1` verdicts, its sentences and its skill cells' own `low`; `prediction_ledger.your_reads`' integers; `market_read_grades.congruence_lines`; `trade_origin.planned_state` with the row's own `label_provenance`), the best family comes from the ONE Wilson LOWER bound and never from the rate, nothing under `MIN_REPORTABLE_N` is named, and `n - measured` never enters a rate. A DAY line carries no `rate_lb` at all - a Wilson over one session is a statistic the card invented - and only `week()` computes one, from counts POOLED once per session. **Every line is guarded on its own**, so one owner raising costs that sentence (`could not be read: ...`, `measured_ok: False`) and the other five still say what they measured; the card is built on the Day Review WORKER inside the ONE payload (`PAYLOAD_KEYS` gains `report_card`, `read_day` fills it LAST in its own guard) and the page only FORMATS it. `day_review_pack.build_pack` accepts a `report_card=` and mints one `source_id` per line, but `build_pack_for` hands it the session's card since the follow-up `c37d5673` (merged `ccecf320`, lead-reviewed): five day lines through `day_report_card.pack_card`, `How fresh` kept OUT of the hashed body so the ledger never re-narrates a day.
**Three review rounds, each finding the same defect class: unmeasured presented as fine.** Round 1 blocker 1: `How fresh` tested `status != "ok"` and named **22 slots broken for 2026-09-18 when 2 were** - 20 had finished `ok` hours earlier and were `skipped` by the next half-hourly pass (`daily_digest` ok 22:02:10, skipped 03:30:41), and only `journal_import` (failed x3) and `ai_summary` (degraded) were really not ok; the card now IMPORTS `STATUS_OK` and `ATTEMPT_STATUSES` from `ai_jobs/ledger.py`, spells no status of its own, names `failed` and `degraded` apart, and lets the LAST deciding row win. Round 1 blocker 2: the Process line called **30 of the trader's 33 trades since 2026-08-20 `unplanned`** (17 sessions, mean 1.8 a session) because `planned_state` answers `unplanned` whenever nothing precedes the first fill and the desk reads only two of the four lanes; it now never prints a bare `unplanned` while a lane is unread, NAMES the unread lanes from the one constant `DESK_ORIGIN_LANES_READ` that both lane builders build from, carries `lanes_read` / `lanes_unread`, and the `trade_origin` prompt leads with the same caveat and offers `a_focus_pick`. Round 2 blocker 3: a session older than the 256 KB / 500-row ledger tail read as a clean night - on the live ledger (1,256,082 B, 483 rows) the window holds 173 rows back to 2026-09-11, so **9 of the 15 sessions the picker offers said "none reported trouble" over zero slots** and the half-visible 2026-09-11 said 5 finished ok when 16 did; the tail now says whether it truncated and how far back it saw, a session at or past that edge is `unknown` with no counts, a covered empty session is `no_rows`, and the file is still opened exactly ONCE either way.
**Lead decisions ratified, each the trader's to overrule.** (a) SIX lines, not the plan's five - the trader's 2026-09-19 second look added `How fresh`, and a page that shows yesterday's numbers without saying the night failed lies quietly; plan.md 12.4 and gate #157 are re-worded here. (b) The module is `scripts/day_report_card.py` and the two `mentor_questions` consumer strings MOVED to match it, because a consumer the registry's AST walk cannot resolve is exactly what that walk exists to catch. (c) TJ-12 WAKES `trade_origin` and `open_position_check` inside this packet, because TJ-14B registered them dormant naming THIS packet as the reader and a kind whose reader has shipped is a question worth asking. (d) The wake STAYS although two of the four origin lanes are unread, because today the trader's own click is the only way the desk can learn a trade's origin, the prompt SAYS the lanes are unread, `a_focus_pick` is offered and `Stop asking this` retires the subject. (e) The LAST deciding row wins - the builder's rule, accepted over the lead's "any `ok` wins" - because an `ok` followed by a later `failed` re-run is real news and the tester's own fixture pins that a slot whose last row is not `ok` is NAMED. (f) Past the ledger tail the line says `unknown` rather than reading further, because no sentence on a page open is worth a whole-file read of a 1.2 MB ledger, and `unknown` is the honest answer to what the read could not see.
Follow-up owed and NOT scheduled: **TJ-12F** - one bounded reader per store for the Focus-add and armed-alert lanes plus both names in `DESK_ORIGIN_LANES_READ`; until it lands the Process line and the prompt must keep naming them unread. **Advisory recorded, not repaired (review 3), for TJ-5:** `how_fresh` called with an EMPTY `session` pools every night the tail holds, because `_slot_verdicts` skips its filter when `session` is `""`; unreachable from the desk (`DayReviewService._report_card` always passes the session), so it is a note for whoever calls `how_fresh` next - the week strip - and not a defect on this branch. `selftest.LAZY_ENGINE_MODULES` gains `day_report_card`: source selftest **95/95 -> 96/96** (verified on this tip). Full suite on the merge: **PENDING the lead's run**. Live gates **#157** (re-worded to six lines), **#168** (`How fresh` after a real night), **#169** (the wake, which now also carries TJ-14B's origin clause) and **#170** (TJ-5's week re-cut) owed. Long form: CHANGELOG Recent changes; DESK_INTERNALS "TJ-12".

### 2026-09-20 - the NaN-row fix for Day Review session bars (lead, from the trader's console report)

`claude/tj2-nan-bars` `c26c6a8f`, merged into `lead/p033-integration2` `ed62bd0a`; NOT live (the desk is up on `main` `16ec284e`). One all-NaN union-index row used to cost a name its whole session tape (about eighty names at the 16:05 start); now the hole is skipped and the tape kept. 4 fail-before-fix tests + 87 neighbours green. **Owed after it is live:** re-fetch the 19 `day_review\bars\*.parquet` files written at 16:06 by the old code (they lack the dropped names). Long form: CHANGELOG Recent changes.

### 2026-09-20 - TJ-4 MERGED

Branch `claude/tj4-day-story` (tip `6c1e2506`, FOUR review rounds by reproduction, NO-GO ×4 - the product was confirmed whole in round 4 and its one blocker was a racing TEST), merged `d929e34f` into `lead/p033-integration2`. **Not on `main`, so nothing of this is live until the trader says to fast-forward.** What it is: the trader's 2026-09-19 amendment built - **the model narrates verdicts, it never makes them.** `scripts/day_review_pack.py` builds ONE hash-stable pack per session at `DAY_REVIEW_DIR/sessions/<date>/pack.json`: twelve `SECTIONS` (`report_card` and `mood` present and empty as hooks), the builder PURE - no store, no clock of its own - so a rebuild hours later is byte-identical and the `inputs_hash` skip is honest (the clock lives in `built_at`, outside the hash). Every item names a `source_id` that ONE `_Minter` guarantees unique inside that pack, because ids are DERIVED from what they point at and a colliding id would let a reader silently take the LAST row - a narration quoting the second row's verdict while naming the first. An observation and a prediction from one Mentor card are SEPARATE items, an empty observation emits nothing, and a machine row or the pasted forecast never reaches `trader_said`. The pack sits inside `day_review_index._prune`'s 40-session delete path, safe only because it is rebuildable - the hash-stability test is what proves it. `scripts/ai_jobs/day_review_narration.py` is the Stage 2 slot `day_review_narration` (`uses_model=True`, `reserve_minutes=10.0`, `max_attempts=3`) after `ai_summary` and before `observation_tags` and `ticker_briefs`; it writes `narration/<date>.json` and the ONE rolling `d1_view.json` from the trader's D1 PREDICTION clicks and D1 notes over `LATELY_SESSIONS` (`market_thesis.current_theses` is NOT read; never an M5 item). Six rules reject an output WHOLE with the prior file byte-identical and a `degraded_no_narrative` row: a verdict that does not EQUAL the measured read row its `evidence_id` names, a claim on a read the pack does not carry or a second claim on one read, an `observation` graded as a call, a `chased_against_news` other than `unknown` with no pasted forecast, an empty headline, and any schema or bound breach. Two artifacts, two verdicts - a rejected story never costs the rolling view its file. **The bounds come FROM THE PACK** and the schema handed to the model carries those numbers; `MAX_GRADED_CLAIMS` (64), `MAX_SOURCES` (512), `MAX_OPEN_THESES` (32) are absolute RENDER ceilings only, and a short story is said as `graded K of N reads`. The page reads both verified files on its ONE worker (`day_story`, `d1_view` in the payload); **Redo story** BUILDS that session's pack first on an off-Qt `_RedoPackWorker`, then writes a `redo_requested` marker by day (no process) or starts one below-normal child inside the night window. `run_ai_jobs.py --session` is accepted ONLY with `--slot day_review_narration`, reaches that one slot through an additive `run_slots(session_override=)`, and every ledger row that run writes - the window refusal included - is keyed to the session the slot WORKED on.
**Four review rounds, each on the previous round's fix.** Round 1: the button lied. By day the Redo click said "queued for tonight", and for every session except the one the night was about to narrate anyway nothing would ever read the marker - and the page's DEFAULT pick during a session day is the PREVIOUS session, so the default click was the broken one. Round 1 also found an unbounded reply: `ai_summary.validate_structured_output` enforces keys, types, enums and a top-level `maxLength` but **not `maxItems` nor an item's length**, so a 5,000-source, 500-claim answer was accepted, written whole (372 KB) and drawn line by line on the Qt thread. Round 2: the fix starved itself and the caps were too small for a good day. Three markers for PACKLESS sessions ate the whole sweep budget and, being oldest, sat at the head of the queue every night after - live, not theoretical, the home folder holding three session folders and zero packs - and the round-1 caps (6 claims / 24 sources) sat BELOW a real full session (8 reads / 25 ids, two of the six scheduled cards storing two entries each), so the trader lost their story on exactly the days they answered every prompt. The same round found a worst-case night declaring five nine-minute calls against a ten-minute reserve, the night-only rule broken from inside. Round 3: the trader's own hand - three impatient clicks built three `_RedoPackWorker`s, three full `read_day` builds racing on one `pack.json` and three child processes, the button enabled throughout. Round 4: the product was whole and the TEST was the blocker - `test_a_session_switch_mid_build_neither_frees_nor_strands_the_button` counted `service.calls` while the worker was still on its way to the append (2 failures in 8 runs); the lead fixed it at `6c1e2506` and measured the fail-before-fix the previous commit promised and never recorded (9 failed / 5 passed reverted, 14 passed on the fix, 25 of 25 green).
**Lead decisions ratified, each the trader's to overrule.** (a) The slot sits before `ticker_briefs` - the PACKET over plan change 2 - because gate #158 wants the day story finished before 23:30 Pacific and the briefs reserve 120 minutes in front of it, and it cannot move further forward either, two pins putting `ai_summary` directly after `measured_report`. (b) `--session` reaches ONE slot only, refused with any other `--slot` (exit 2, nothing run), because a flag that silently did nothing elsewhere would read like a general override of the night's own session date. (c) `pack.json` stays PRUNABLE inside the 40-session delete path, because it is rebuildable and the hash-stability test proves it. (d) The night SWEEPS queued redos, three NARRATED a night, because otherwise "queued for tonight" is true for one day in fifteen. (e) The caps come FROM THE PACK, because a real full day measured 8 reads / 25 ids - above any fixed cap we first chose - and a fixed cap loses the story on the best-answered days. (f) `reserve_minutes` stays 10.0 (the tester pinned it) and the slot RE-ASKS the window itself before every later call (`SWEEP_CALL_MINUTES` 9.0), because a reserve buys one call and a sweep makes several. (g) A Redo BUILDS the pack first and only ONE runs at a time, because the night can only narrate a session it has a pack for and three clicks must not be three heavy builds. (h) `request_redo` and the CLI fail CLOSED on an unknown calendar - a write the trader asked for is not evidence, and `".."` once wrote a marker at the `day_review` ROOT while `"2026-09-18-extra"` was truncated into a real day's folder.
**Two live facts, measured 2026-09-20:** `C:\TradingBotData\day_review\sessions\` holds three folders and ZERO `pack.json`, so the first Redo after this merge is also that session's first pack build; and NO live journal row carries a TJ-14A prediction yet, so the first morning's were-you-right list will be EMPTY unless the trader clicks a prediction on the Mentor card first - the honest empty state, not a defect. `selftest.LAZY_ENGINE_MODULES` gains `day_review_pack` and `ai_jobs.day_review_narration`: source selftest **93/93 -> 95/95** (verified on this tip). Full suite on the merge: **PENDING the lead's run**. Live gate **#148** (three clauses) owed. Long form: DESK_INTERNALS "TJ-4"; slot position: decision 0018 addendum 2026-09-20.

### 2026-09-20 - TJ-16 MERGED

Branch `claude/tj16-prediction-contrast` (tip `d0d61f95`, reviewer GO after two rounds), merged `c4a760e5` into `lead/p033-integration2`, with the lead's seventh slot-order pin `4f131b4c` on top. **Not on `main`, so nothing of this is live until the lead's next fast-forward.** What it is: what leads to a good call, and what the trader was actually looking at when they said it. `scripts/prediction_ledger.py` is the read ledger's reader - the CURRENT grade row per read (`market_read_grades.current_grades`, so a matured re-grade counts once at its current verdict), `rest_of_day` and `next_5_sessions` never pooled with each other and a click never pooled with an extracted stance (`PoolingError`), and **every accuracy cell printed beside what `always_up`, `same_as_the_last_hour` and `with_the_d1_environment` scored on the SAME stamps**. On the tester's ten-session fixture the trader went 22 of 40 and the three naive rules went 24, 32 and 32 - a sentence only sayable because `baseline_reads` re-answers each row from that row's own point-in-time snapshot and no bars at all; a baseline has no stored verdict so it is measured NOW through `market_read_grades._verdict_for` (the module attribute, never a copied band), and one that cannot answer - `compressed` is not a direction - LEAVES the fraction instead of counting as a loss. Calibration by `How sure` says "High did not beat Low" when it did not, and `None` (not "no") while either bucket is under `MIN_REPORTABLE_N`. The deterministic Stage 1 slot `scripts/ai_jobs/prediction_contrast.py` sits DIRECTLY after `miss_contrast` and above the pair that closes the stage: right against wrong per point-in-time context field through TJ-15's one `evidence_contrast.contrast`, `lately` and `all` printed apart, per horizon, with `by_hour` and `by_environment` tables carrying `n`, the Wilson bounds and `reportable` in every cell, a `flat` counted on the horizon and in NEITHER group, and an `unmeasured` field contributing NOTHING rather than a zero (on the fixture eight of forty reads had no `spy_vs_prior_range`, so it ranks on 32 rows, not 40). The Stage 2 slot `scripts/ai_jobs/observation_tags.py` (local MEDIUM model, after `ai_summary`, before `ticker_briefs`) codes `mentor.observation` and `mentor.prediction.because` from a closed versioned vocabulary of 11 codes with its OWN loader, each code carrying the exact span that must reproduce its quote; **the payload is BUILT from the two texts and the picklist, so there is no verdict key to remember to delete** - the same session's grade ledger sits on disk beside the job and none of it reaches the prompt. `your_reads`, `tendencies` and `read_latest` ship as READERS with no page: Day Review's line is TJ-12's, the Week Review tables TJ-5's.
**Two reviews by reproduction, NO-GO then GO at `d0d61f95`.** Round 1's first blocker: `MAX_TAGS` and `additionalProperties: false` lived only inside `TAGS_JSON_SCHEMA`, which is **a grammar hint to the provider and never a guard** - the reviewer handed the slot a 10,000-row reply and watched it publish `ok`, and a row carrying `"why": "smuggled"` was believed. `verify_reply` now re-checks the SHAPE as well as the grounding and each rule throws the WHOLE reply away: over 60 rows, a key outside `REPLY_KEYS` at the top, a key outside `TAG_KEYS` on a row, or a byte-identical duplicate row (identity `(note_id, code, start, end)`, so key order cannot dodge it); overlapping spans with DIFFERENT codes stay accepted, because one sentence can cite a level and be hedged. Round 1's second blocker: the nightly journal read was unbounded - every row ever written, filtered in Python. It is now WINDOWED at the stream (`EvidenceLedger.read(start=session, end=session)`), safe because a CORRECTION carries the ORIGINAL `session_date`; measured on a COPY of the live stream, 84 rows unwindowed against 7 for one session, identical answer, and a supersede pair still resolves to the corrected text. Round 1's advisory became a rule: hindsight in `because` is LABELLED (`written_after_the_session` on every stored tag, `entries_written_after` in the tags header and in the pack's `tags` block, present and zero, never absent), never dropped, never re-ranked (the fixture built with and without the label is byte-identical across every horizon number) and never told to the tagger. **Live fact, measured 2026-09-20: `C:\TradingBotData\day_review\` holds only `sessions\`, so there are ZERO clicked grades and zero notes carrying an observation** - the first thing the trader sees is the honest empty state, `no clicked calls yet` with `rate: None`, an empty pack and an `ok` row, never `failed` and never 0.0.
**Lead decisions ratified, each the trader's to overrule.** (a) The tag vocabulary has its OWN loader, because the shared `ui/annotations/vocabulary.py` `_parse` is veto-shaped (a unique single-character hotkey, a per-entry `note_required`) and is called by the capture rail on every click - widening it for a nightly reader is risk for no gain. (b) Tags and packs live beside the digest in the AI store rather than under `DAY_REVIEW_DIR`, because both are rebuildable evidence and a superseding sibling with `read_latest` is what every other nightly pack already is. (c) The contrast shows EVERY feature over the floor rather than a top 3, because nothing may be selected by a result and the context block is about a dozen fields, not TJ-15's 264 - the bounded view is `tendencies`, at most three cells ordered by `n` then name. (d) A byte-identical duplicate tag row rejects the WHOLE reply, because a model that repeats itself has not been verified and de-duping silently would store a file that does not say what the model returned; it is strict FIRST and trivially loosened to a de-dupe if the first live nights show it firing. (e) A note written after the session is LABELLED and counted, never dropped and never told to the tagger, because the trader's own words are the artifact under study while a prompt saying "this was written after the close" would tell the tagger something about the outcome.
Full suite on the merge (`eca27e6f`): **9,670 passed, 14 skipped, 72 subtests, 1 failed** - the known timing flake `test_g7_speed_pass::test_showing_the_research_tab_loads_only_the_child_whose_tab_is_open`, green 3 of 3 alone; ruff clean. Live gates **#164-#167** owed (and #161 with them). Long form: DESK_INTERNALS "TJ-16"; slot positions: decision 0018 addendum 2026-09-20.

### 2026-09-20 - TJ-14B MERGED

Branch `claude/tj14b-mentor-questions` (tip `cfe137d1`, reviewer GO after three rounds), merged `161e905c` into `lead/p033-integration2`. **Not on `main`, so nothing of this is live until the lead's next fast-forward.** What it is: TJ-14 items 2-5 - the Mentor asks only what something will READ. `scripts/mentor_questions.py` is PURE (no store, no Qt, no clock; every lane arrives in the `state` mapping the caller built) and is the card's single description of all nine question kinds: trigger, click options, the store it `writes`, cadence / expiry, priority, and the `consumer` plus `answer_key` that make the question worth asking. `consumer_report()` resolves the dotted consumer and walks its SOURCE with `ast`, counting the key only where it is used in CODE - a comment, a docstring or a bare string statement is refused - and the probe is deliberately NOT a call, because a calling probe would pass for `json.dumps`. **A question is ASKED only when its answer has a reader** (decision 0021 answer 28): FOUR kinds ship registered and DORMANT with the packet that builds their reader - `trade_origin` and `open_position_check` -> **TJ-12**, `grader_gap` -> **TJ-10**, `quick_like_followup` -> **TJ-14C** - `pending()` never puts one on a live card, `consumer_report` reports it as `dormant` with the packet named, and **no shim reader was written**; waking one is `dormant_until=""` plus its lane in `MainWindow._mentor_question_state`, which already names all five. TJ-10 has since merged on this branch and its `grader_gap` field is on every grade row, so that kind can be woken as a small follow-up - deliberately not done inside TJ-14B. Beyond TJ-9's forced trade section and TJ-14A's forced prediction rows a card asks at most THREE questions by priority, the remainder counted on the card and carried to the next - never dropped, never a fourth - AWAY asks nothing, and `Stop asking this` retires ONE subject through `TradeMentorService.stop_asking`, persisted in `trade_mentor_slots.json` under `C:\TradingBotData`, so it survives a restart and a cache wipe.
**Three review rounds by reproduction through the real Qt slot, NO-GO / NO-GO / GO.** Round 1's three blockers: the two-day pre-card pull pre-empted and CONSUMED TJ-9's three-day morning catch-up (the service refused the second start and `morning_import_retry` stamped `last_retry` anyway, so a Monday whose Friday-night import failed never reached back to Friday); first-come spent the day's whole budget by the 08:00 card, so no fill after 11:00 ET was ever imported and `same_session` - the label the packet exists to make reachable - was unreachable every afternoon while the card kept asking; and `quick_like_followup` named a consumer that reads its key off another store, which made it the fourth dormant kind. **ONE card now starts AT MOST ONE import**: the catch-up goes FIRST and outside the cap with `last_retry` stamped only when an import actually started, otherwise `pre_card_pull` runs on one of the day's three RESERVED, SPACED cards (`pull_slot_ids`: 09:00, the middle card, the LAST card, read off the session's real slot list, an early close forfeiting the middle attempt rather than rolling it earlier). Measured through the real `_show_trade_mentor_prompt`: the six slots of 2026-09-14 call the importer with days `[3, 2, 2, 2]`; the four slots of the 2026-11-27 early close start two pulls. `PULLS_PER_DAY_CAP` 3, `PULL_FAILURES_PER_DAY_CAP` 2, `PRE_CARD_PULL_DAYS` 2, one persisted tally (a corrupt one read as empty and rewritten clean), never in AWAY, never a token refresh of its own, never raising, the card never waiting - and **there was no per-day failure cap anywhere in the repository before this packet**. Round 2's blocker: `set_trade_check` returned early on a not-ready journal and drew NOTHING, including for fills the desk had already seen today. Round 3's blocker: once the 09:00 not-ready card drew today's fills, the host's `open_answers_session()` early return skipped `set_trade_check` on every later slot, so the REVIEWED session's trades were never asked about that day; the card now MERGES each delivered slot's fresh task row by row (existing widgets and half-set values kept, a new trade added after them, a row no longer owed dropped, heading and freshness line always rewritten, the Save gate recomputed), every block naming its session. `save_answers` writes `label_provenance` from `trade_origin.label_provenance` with the boolean `recalled_after_session` that matches, and a DATE-ONLY first fill is never `same_session`. `NARRATION_JSON_SCHEMA` gains the optional `mentor_question_options` (<=4 strings, `maxLength` 60) and validates it - a five-option night is rejected WHOLE and the prior verified file stands byte-identical - so the overnight question becomes ONE click a day instead of the `One thing to test: ...` line printed forever with no answer. `selftest.LAZY_ENGINE_MODULES` gains `mentor_questions`: **source selftest 92/92 -> 93/93**.
**Lead decisions ratified, each the trader's to overrule.** (a) The day's pre-card pulls are THREE, reserved for the 09:00, 11:00 and last cards, plus ONE uncapped morning catch-up that goes first - a reviewer showed every pull was otherwise spent by 08:00. (b) The card MERGES a fresh task, so a not-ready morning becomes yesterday's questions while half-set widgets survive. (c) A same-day answer is labelled `same_session` and a date-only fill never is - a broker file is blind to time, so there is no moment a label could have been made before. Recorded, not repaired: `confirm_setup` can still stamp `same_session` for a date-only fill, because `trade_origin.py` is TJ-9's pure rule and moving it moves every caller at once (a line for TJ-12). Full suite on the merge: **pending the lead's run**. Live gate **#159**'s TJ-14B clauses (its `trade_origin` clause moves to TJ-12's gate, that kind being dormant) and the new gate **#163** owed. Long form: DESK_INTERNALS "TJ-14B".

### 2026-09-20 - TJ-10 MERGED

Branch `claude/tj10-read-grader` (tip `2967e4a6`), merged `57b44ca9` into `lead/p033-integration2`, integration fix `3e52d94f`. **Not on `main`.** What it is: decision 0021 answer 14 built as `scripts/market_read_grades.py` - a PURE grader with no model anywhere. A READ is a CLICKED prediction (`market_journal.prediction_of`) or a stance EXTRACTED from the trader's prose, never pooled (`PoolingError`); `no_view` is recorded and never graded. `rest_of_day` anchors on the OPEN of the first completed M5 bar that STARTS after the stamp and ends at the last completed bar at or before `session_close` (half days included); `next_5_sessions` anchors on the DECISION session's own daily close, checkpoints at 1, 3 and 5 exchange sessions, takes its verdict from the 5-session one and reads `pending <date>` until then - never zero, never wrong - and refuses an anchor while that session is still trading. The flat band is 0.25 of the benchmark's point-in-time daily ATR(14), edge inclusive, stamped `flat_band_rule: "atr_0.25_v1"` on every row with `flat` IN the accuracy denominator; **the 0.25 is the LEAD's number, chosen because the packet gave none, and it is the TRADER's to change - never from one session's result.**
Two reviews by reproduction on hand-built bars and staged copies of the live stores, NO-GO then GO. **Round 1's three blockers, all fixed:** (1) `regrade_matured` could not measure on this desk and DESTROYED the pending row it failed on - an absent store turned a correct `pending` into `unmeasured:no_anchor_close` forever when the true answer was `right`, +5.0 ATR; ONE loader set now serves the page and the night (`daily_bars_for_symbol`, `session_bars_for`, `atr_for_session`) and `verdict_rank` appends only when the rank RISES, so an `unmeasured` never supersedes a `pending`, a data-unmeasured row is revisited every run, a measured verdict is FINAL, a re-grade carries the ORIGINAL context forward and the file only grows. (2) The page presented an EXTRACTED stance as the trader's own call; every line, label and chip now names its source (`your call: down (clicked 07:02)` vs `we read your note as up`), a click always beats an extracted stance of that timeframe, and contradictory notes compare with NOTHING. (3) The M5 half of congruence was never built; `m5_picks_side_mix` now pairs the rest-of-day read with that session's M5 likes (`not_today` counted apart, D1 picks never entering it). The store is strict by lead decision: a gradable CLICK whose context is only the named absence is REFUSED (`ContextMissingError`), the grade is held back, the payload row says `grader_gap: "context_unbuildable: ..."` and the next pass retries. The follow-up `2967e4a6` floors an under-floor side mix to `too_few`, makes each line say its OWN content first on a both-ways day, and cut the forming-close anchor.
The deterministic slot `read_grades_mature` (`uses_model=False`, `max_attempts=3`, `reserve_minutes=2.0`) sits after `theta_pick_grading` and before `miss_contrast` / `market_story_rollups` / `measured_report`, inside `_deterministic_stage` so it is on the weeknight, Saturday AND Sunday slates; a missing store gives an `ok` row with a reason in under a second and never raises. Decision 0018 carries the addendum. At integration the lead added the slot to TJ-13B's two weeknight-slate pins and moved the 49-note text fixture to `tests/fixtures/day_review/`.
Measured on staged copies of the live stores: **0 clicks exist yet** (the click card went live 2026-09-20) and 7 extracted reads on each of 09-17 and 09-18; the vocabulary bump moves the 49 live notes from 18 to 20 directional with NO reversal. Two live-desk facts: `C:\TradingBotData\data\daily_bars\SPY.parquet` EXISTS (189 rows through 09-18) yet `chart_snapshot.load_d1_bars` answers EMPTY for SPY / QQQ / IWM / VXX on this desk for its own, so-far unexplained reason - a follow-up question - so every benchmark daily bar came from the machine-local cache (1,993 symbols); and the live home has NO `day_review\bars\` folder, so every rest-of-day read is honestly `unmeasured:no_completed_bar_after_the_stamp` until a tape exists. **Gate #155 therefore REQUIRES the TJ-2A session tape before it can pass.** Advisories recorded: nothing counts clicks held back for `context_unbuildable`; `baseline_reads` needs GRADE rows (TJ-12 / TJ-16 must feed it those); the congruence D1 label is the SAME session's while the context block uses the PRIOR session's; `internals_bars_at` now has its first production caller; `MIN_REPORTABLE_N` = 30 is the "name a winner" floor, so the M5 side-mix line will read `too few to call` on most days and a smaller NAMED floor would be the trader's or the lead's number.
Full suite on the merge `57b44ca9`: **9,536 passed, 14 skipped, 72 subtests, 2 failed - the known flake `test_ws_10a_scan_freshness` and `test_fixture_contract` (the TJ-10 text fixture sat where the market-data provenance contract applies; moved in `3e52d94f` and re-run green with the AI lock free, 2026-09-20)**. Live gate **#155** owed. Long form: DESK_INTERNALS "TJ-10".

### 2026-09-20 - TJ-9Q MERGED

Branch `claude/tj9q-questrade-instrument` (tip `76f3cf2a`), merged `86c64f96` into `lead/p033-integration2`. **Not on `main`, and nothing in the live journal has moved.** What it is: TJ-9's item 7, split out after the tester's step 0 refuted three of its premises. Measured on byte-exact COPIES of the live journal, 2026-09-19/20: 226 of 226 Questrade executions carry `security_type = 'UNKNOWN'` (the executions endpoint sends no `securityType` and no `symbolType`), `net_amount` and `gross_amount` are NULL on all 226 - the broker's cash is the payload's own `totalCost`, which already carries the option multiplier - the stored `multiplier` column is 1.0 on all 226, and `normalize_side` did not map `STO` / `BTC` / `Cov` (live: STO 4, BTC 4, COV 25), so `_signed_quantity` read a sold put as +qty. The trader's **three sold-put positions** (four STO fills; AAOI has two) therefore read `direction = 'LONG'` and sit OPEN with `quantity_closed` 0, and the one BOUGHT put (QQQ, BTO/STC) is correctly LONG and CLOSED but 100x too small. Correct P&L: AAOI +241.03, BE -666.99, QBTS -57.96, QQQ -81.98. A forward-only classifier was refuted because `group_key` and `trade_id` key on the type - it would split every one of the 29 open UNKNOWN Questrade positions from its closing fill; `execution_uid` never moves and `fill_signature` is the CSV path's and is untouched.
What was built: `classify_questrade_security_type` (symbol AND side must agree; a stated type wins; a disagreement or an empty field stays UNKNOWN; nothing from a ticker's length); `normalize_side` mapping `STO`->SELL, `BTC`->BUY, `COV`->BUY, with the old map kept by name as `normalize_side_pre_tj9q`; and the switch `QUESTRADE_INSTRUMENT_FROM_SYMBOL = False`, its effective value read at CALL time from `local_settings["questrade_instrument_from_symbol"]`, gating the IMPORT seams (`_append_normalized`, `journal_statement_import._execution_from_row`) and never the classifier - with it OFF both paths are BYTE-IDENTICAL to before, proved base-versus-tip through the real seams. `scripts/journal_reclassify.py` is the one way stored rows move: dry run by DEFAULT reading COPIES (0.2 s, zero bytes), `--apply` (0.3-0.4 s) taking a byte-exact timestamped backup FIRST, moving type + side + multiplier through ONE store method (`JournalStore.reclassify_executions`, Questrade rows only - it refuses any other broker's uid), rebuilding, re-keying every `trade_annotations` row by largest execution overlap and REFUSING a tie rather than guessing, carrying the machine's trade-keyed rows by the same rule (`ai_trade_enrichment` carried or its position refused; `note_lane_verdicts` carried or DROPPED under `refresh_auto_tags`' own rule for that derived table; `opportunity_events` never rewritten, carried by a `trade_aliases` row with unresolvable references counted and named), verifying, and RESTORING the backup byte-exact with exit 1 if anything is wrong.
Two reviews by reproduction on copies of the live journal, NO-GO then GO. **Round 1's blocker:** the switch flipped ON after a refusal, and a new fill then split that contract into two positions. It is now written LAST and only when NOTHING was refused AND no open UNKNOWN Questrade position remains; otherwise the rows that moved stay moved and consistent, the blockers are named, and the run exits **4** (`EXIT_SWITCH_STAYED_OFF`, re-runnable). Exit codes: 0 done, 1 verify failed and restored, 2 would not start, 3 busy (the desk slot or the `ai_jobs_runner` lock), 4 moved with the switch off. `--apply` refuses a `--db` under `C:\TradingBotData` or the DAS without `--i-am-the-trader`, and an interrupted `--apply` is repaired by running it again.
Measured by the reviewer on a copy, 2026-09-20: 226 fills moved, 0 refused; columns moved `security_type` 226 (OPT 10 / STK 216), `side` 33 (STO->SELL 4, BTC->BUY 4, COV->BUY 25), `multiplier` 10; ZERO non-Questrade rows touched; all 616 `execution_uid`s and every `net_amount` / `gross_amount` / `commission` / `fees` / `quantity` / `price` byte-identical; exactly FOUR positions move and no equity trade; total recomputed P&L 4,737.45 -> 4,184.25 (the -553.20 is exactly the four option corrections); 185 annotations in and 185 out on live trades, content identical, `label_provenance` still empty on all 185; `ai_trade_enrichment` 24 of 24; `note_lane_verdicts` 94 carried and 4 dropped (CRDO, FBIN, IOVA, SMH - one execution shared by two rebuilt trades; machine `no_claim` verdicts, nothing the trader wrote); every existing `opportunity_events` row byte-identical, 94 aliases, 4 left and named; the tax report identical for 2026 (76 positions, CAD 5,426.33) and 2025 (24, CAD 1,308.97); a second `--apply` is a no-op; page one of the report is ~40 lines, decision first (cut from 609).
**The ONE ungated change, lead-approved:** `journal_file_authority._BUY_SIDES` gains `COV` (the set held `COVER`), so the 25 stored covers stop counting as cash coming IN - 14 (account, day) pairs move, 72,596.51 total absolute = twice the covers' 36,298.26 (SMPL 2026-09-18 +366.98 -> -366.98) - which makes the file AGREE with the sync on those days. It has NO automatic caller: only a trader-initiated statement import or "Check a statement...". Also ungated and correct: a hand-typed `STO` in the manual-fill dialog now stores SELL.
Full suite on the merged tip: **9,396 passed, 14 skipped, 72 subtests, 1 failed - the known unowned flake `test_ws_10a_scan_freshness` - on `86c64f96` (everything live + TJ-13B + TJ-9Q), nightly AI lock free, 2026-09-20 06:57 PDT; ruff clean, smoke 7/7, source selftest 92/92**. Live gate **#162** owed, and the trader's `--apply` with it. Long form: DESK_INTERNALS "TJ-9Q".

### 2026-09-20 - TJ-13B MERGED

Branch `claude/tj13b-large-local` (tip `800ecb8c`), merged `e54c8203` into `lead/p033-integration2`. **Not on `main`.** What it is: TJ-13 item 7's MEASURING half. In 476 live ledger rows the only models ever named were 12B tags; `ai_local_model_large` (a Gemma-3 27B Q3_K_M GGUF tag; medium is `gemma3:12b-tbv3ctx-64k`) was configured and had **never run**, so the `reserve_minutes` that decides whether the week story may start at all could not be written down by anybody. It is now MEASURED, once, by the trader, and read back from the ledger. `scripts/ai_jobs/model_probe.run_model_probe`, reached as `run_ai_jobs.py --probe-model large`, is a COMMAND and never a slot: it builds no slate, runs no other job, exits 0 measured / 1 refused with a printed reason, and has exactly THREE refusals - it is not night, a job is running, this session was already measured. Because a probe is a model LOAD, every rule about model loads applies: night-only whatever `--force` says (`PROBE_RESERVE_MINUTES` = 45, so it refuses from ~05:15 PDT), a deleted COPY of the newest week of fact packs and never the live store, and **the `ai_jobs_runner` lock as the DEFAULT guard** - sentinel `USE_RUNNER_LOCK`, with `None` resolving to the same thing, so there is deliberately no value of `lock` that means "unguarded" and no call can load the 27B beside a running 12B; the guard is HELD across the whole measurement, so a 22:30 firing under it stands down cleanly, and a box with no exclusion primitive gets a REFUSAL. One `manual_test` row per measurement (`job="model_probe"`) carries load seconds, tokens per second, `peak_memory_mb` (the MACHINE's in-use memory, with `baseline_memory_mb` beside it) and the context length the SERVER accepted, each labelled with its `basis`; `manual_test` never counts as session coverage and the row's `session_date` is the LAST session, so a Saturday-night probe files under Friday.
`reserve_minutes_from_probe` is `(load + 3,500 tokens / measured rate) x 1.25` - 600 s at 10 tok/s gives 19.8 min - and returns `None`, never a default, when the tier was never measured; a MEDIUM probe never answers for LARGE, and the newest row wins. `provider.week_review_plan` writes no row: with no measurement it answers `may_run_large: False` with its reason and the week story still runs on the MEDIUM local model saying why; with one it names the large tag and the derived reserve. `provider.request_with_fallback` sends the IDENTICAL closed schema to both tiers, rejects an invented `source_id` whole, publishes NOTHING when both fail, writes no ledger row itself (`ledger_path` accepted and unused by design), and returns asked / answered / why for the SLOT to record under `model_attribution` - the ledger's `model` column being one string, a silent downgrade is otherwise unreadable. It RAISES `ValueError` for `openai` before anything is sent, so **TJ-5's week slot must catch that** and record a FAILED row; `openai` stays a setting, off. `local_large` lives in the `ai_jobs` seam, never in `ai_summary.normalize_provider` nor `ai_credentials`, whose vault knows only openai and anthropic.
Review round 1 was NO-GO on one blocker: `--force` never reached the probe, although the probe's own refusal told the trader to pass it. Fixed, and the lead then verified the fix round with the real AI lock FREE - **340 AI-jobs tests green**. Integration notes: a guard pinned "byte-for-byte to the branch's base" (`tests/test_tj13b_local_large_provider.py`'s weeknight slate test) went red the moment the lead's `1f260ffa` fixed slot order on `main`; it was re-pinned to main's order on the lead's authority (same 20 names, positions 12-14), and **TJ-10's coming `read_grades_mature` slot must be added to that tuple at integration**. Also recorded: while the `ai_jobs_runner` lock is held, about 42 tests that call the real `runner.run_slots` FAIL rather than skip (29 in `test_ai_jobs_runner`, the rest in tj13a / tj13b / `test_ai_evidence_coverage` / `test_ai_jobs_store_window`) - `docs/AGENT_TEAM.md` is corrected.
**The 27B has still never run**: the measurement is the trader's first Saturday night (gate **#158**'s TJ-13B clause, trader actions above). Still TJ-13B and NOT built: item 6's Sunday extras - suggested setup tags for old untagged trades, and the week-ahead note; the week STORY itself is TJ-5's. Full suite on the merged tip: **9,396 passed, 14 skipped, 72 subtests, 1 failed - the known unowned flake `test_ws_10a_scan_freshness` - on `86c64f96` (everything live + TJ-13B + TJ-9Q), nightly AI lock free, 2026-09-20 06:57 PDT; ruff clean, smoke 7/7, source selftest 92/92**. Long form: DESK_INTERNALS "TJ-13B".

### Operational restart log — Phase 0.33 (plan.md 12.6a window)

One line per restart: time (PDT), commit, why, result. Doc-only commits to this file are made while the desk is up (no Python file changes under a running process); every CODE update to the main checkout waits for the desk to be down.

- 08:03 START `907a673e` (code = TJ-2 merge `d3ae3aff`, which had never run live; the desk had been down since 09-18 21:08) - why: prove the pre-build baseline starts, so `last good` is a commit seen running. Source selftest 92/92 first. Result: up, window shown, Auto Pilot ON from saved state and idle for the weekend, scanner child alive, IB not logged in (TWS is the trader's, left alone). **`last good` = `907a673e`.**
- 16:03 STOP (graceful window-close request; the desk and its scanner child exited in under 10 s; no scan, save or AI job was running; it had been up 8 h with no error but the IB login it never had) - why: land wave 1. `main` fast-forwarded `3d733619` -> `c29156ad` while the desk was DOWN; source selftest 92/92 on the new `main`.
- 16:03 START `c29156ad` (wave 1: TJ-9 + TJ-11 + TJ-13A, code tip `8077a758`) - result: up, window shown, scanner child alive, Auto Pilot ON from saved state and idle for the weekend, no traceback, import or attribute error in the log; IB still not logged in (the trader's). **`last good` is now `c29156ad`; the one before it is `907a673e`.** If a later start fails, `git revert -m 1` the offending merge (`8077a758` TJ-9, `a89ec7d5` TJ-11, `9eaae1dd` TJ-13A), never `reset`.
- 20:57 STOP (graceful window-close; desk and scanner child gone in 3 s; idle for the weekend, no scan, save or AI job running; up 4 h 53 min clean) - why: land TJ-11F before the 22:00 AI task starts (the task runs from this checkout, so `main` is not touched while it runs). `main` fast-forwarded `04a2de0e` -> `7989758c` while the desk was DOWN; source selftest 92/92.
- 20:57 START `7989758c` (wave 1 + TJ-11F, code tip `f00ec302`) - result: up, window shown, scanner child alive, no traceback, import or attribute error in the log. **`last good` is now `7989758c`; before it `c29156ad`.** A failed later start reverts `f00ec302` with `git revert -m 1`.
- 21:29 STOP (graceful window-close, gone in 4 s, idle, no scan / save / AI job) - why: land TJ-3 before the 22:00 AI task. `main` fast-forwarded `996b79b7` -> `760870d7` while the desk was DOWN; source selftest 92/92.
- 21:30 START `760870d7` (wave 1 + TJ-11F + TJ-3, code tip `72647104`) - result: up, window shown, scanner child alive, no traceback, import or attribute error. **`last good` is now `760870d7`; before it `7989758c`.** A failed later start reverts `72647104` with `git revert -m 1`. `main` is NOT touched again until the night's AI task has finished (it runs from this checkout).
- 2026-09-20 05:44 STOP (graceful window-close, gone in 3 s; idle; the 05:30 AI firing had exited in 2 s and no `run_ai_jobs` / scan process was running) - why: land TJ-15 + TJ-14A between AI firings. `main` fast-forwarded `6f8113d7` -> `38c270eb` while the desk was DOWN; source selftest 92/92.
- 2026-09-20 05:45 START `38c270eb` (wave 1 + TJ-11F + TJ-3 + TJ-15 + TJ-14A, code tip `1f260ffa`) - result: up, window shown, no traceback, import or attribute error. **`last good` is now `38c270eb`; before it `760870d7`.** A failed later start reverts `e8c04f88` (TJ-14A) and/or `fb3f55e9` + `1f260ffa` (TJ-15) with `git revert`.
- **Gate #158, TJ-13A half - `weekend-read` 2026-09-20 (read-only, from `ai_jobs-20260919.log` / `-20260920.log`):** the first SATURDAY slate ran on the new code from 22:00 PDT, nothing by day. `journal_import` failed in 53 s on an IBKR Flex network error (not the desk's). `ai_summary` ran 17,486 s and ended `completion=partial` - 51 of 52 slices read, one slice failed (`setups.scan_factors`), and the SYNTHESIS was written: the first non-degraded ending after four `unsynthesized_fallback` nights (09-15..09-18), so the bounded synthesis package worked. `weekly_synthesis` ran for the FIRST TIME EVER with no typed command: 261 s, `15 of 10 graded cohort session(s); 40 cell(s); narrated`. The run ended 02:56; every later firing (03:00-05:30) found nothing due and exited in 2 s. Still owed: Sunday night's slate (deterministic stage + the still-owed `journal_import` retry only), the brief's membership-only count on a weeknight, and the trader's own read of the two summaries.
- 2026-09-20 06:11:59 the desk CLOSED through its normal window-close path 26 minutes after the 05:45 start (clean shutdown lines in `trading_bot.log`, no traceback, no Windows event, no scheduled task, nothing in the repo closes a window; the lead did not close it - the trader had typed at 05:44, so most likely a person closed the window). It ran clean on `38c270eb` for those 26 minutes. The lead restarts it with the next fast-forward.
- 2026-09-20 07:13 (desk already DOWN since 06:11) `main` fast-forwarded `9fa10e4b` -> `acaa13c0` (adds TJ-13B + TJ-9Q, code tip `86c64f96`); source selftest 92/92. 07:14 START `acaa13c0` - result: up, window shown, no traceback, import or attribute error. **`last good` is now `acaa13c0`; before it `38c270eb`.** A failed later start reverts `86c64f96` (TJ-9Q) and/or `e54c8203` (TJ-13B) with `git revert -m 1`. TJ-9Q ships with its import switch OFF: nothing about the live journal changes until the trader runs `journal_reclassify --apply`.
- 2026-09-20 11:47:34 graceful STOP (window close; Sunday, Auto Pilot idle, no scan, no save, no `run_ai_jobs` process; all desk processes gone in 2 s); last good `739ce5a4`; `main` fast-forwarded `739ce5a4` -> `cdad9533` (adds TJ-10 + TJ-14B, code tip `161e905c`); source selftest 93/93. 11:47:59 START `cdad9533` - result: up, window shown, no traceback, import or attribute error; the one ERROR line is IB 502 (TWS not logged in, the trader's).
- 2026-09-20 15:45:45 the desk CLOSED through its normal shutdown path (`Away report writer lease released on shutdown`, no traceback) - NOT the lead, who was paused by the trader from 12:58 to 16:00; the second such close today. The grant ended 15:35, so the lead did not restart it. `main` was NOT moved (it stays `16ec284e`); NO start - the next start, and the fast-forward that takes TJ-16, are the trader's call.
- 2026-09-21 06:21:45 graceful STOP on the trader's word ("Go ahead and stop the desk update main then bring it back up. The overnight job is done"): the nightly AI task ended 06:00:03 exit 0, no scan, no save, all desk processes gone in 8 s; last good `16ec284e`; `main` fast-forwarded `16ec284e` -> `3cc81cf4` (adds TJ-16, TJ-4, the NaN-row fix, TJ-12, TJ-5, TJ-6, TJ-7); source selftest 97/97. 06:22:14 START `3cc81cf4` - result: up, no traceback, import or attribute error, zero NaN bar errors; IB 502 only (TWS not logged in). Done 8 minutes before the open instead of after the owed full suite, which was started at once in a scratch worktree.
- 2026-09-21 12:05:34 graceful STOP on the trader's word ("combine all of todays work to main then commit and push and restart the desk"), market hours: the 10:00 swing scan had finished 10:14 and none was running, no tracker write, the M5 scanner child retired with the desk (one `qt-bouncebot-retire` thread over its budget, WARNING only), all processes gone in 12 s; last good `3cc81cf4`; `main` fast-forwarded `3cc81cf4` -> `b4c1c4f7` (adds TJ-9E, GO in review round 4, and the per-trade Mentor Save, merged WITHOUT a review of its own - a review is owed); combined full suite first: 10,263 passed + the one known flake; source selftest 97/97. 12:06:06 START `b4c1c4f7` - result: up, window shown, IB connected 12:08:21, no traceback, import, attribute or NaN error; `main` pushed to origin.
- **INCIDENT 2026-09-20 ~06:45 (contained, desk was down):** a reviewer's scratch script outside pytest set `TRADINGBOTV3_DATA_DIR` but not `LOCALAPPDATA`, and `project_paths.CACHE_DIR` lives under `%LOCALAPPDATA%\TradingBotV3\machine_cache`, which that variable does not move - it wrote a 6-row fake `SPY.csv` over the desk's live daily-bar cache. The reviewer restored it from `C:\TradingBotData\data\daily_bars\SPY.parquet` and reported it; the lead verified 189 of 189 rows identical to the parquet, the same shape as `QQQ.csv` / `IWM.csv`, and no other cache file modified that day. RULE CHANGE owed in `CLAUDE.md` / `docs/AGENT_TEAM.md`: a scratch script sets BOTH variables and aborts if any project path resolves under `C:\TradingBotData` OR the real `%LOCALAPPDATA%\TradingBotV3`; every agent brief since carries it.

### Open gates, newest first

| # | Gate | Owed by |
|---|---|---|
| 181 | **An answered trade is stored at once and never asked again (the per-trade Mentor Save, MERGED 2026-09-21 `182f3e08`, NO reviewer round - a review is OWED)** - on the 09:00 card answer ONE trade's fields (and its exit box, if it has one), press that trade's `Save this trade`: the block leaves the card at once, the other trades' half-typed words are untouched, and the NEXT card does not list that trade again. | trader, first 09:00 card after the restart |
| 180 | **The note and the count are read where the trader reads the day (TJ-9E, MERGED 2026-09-21, OWED)** - Day Review's trades section shows the trader's own exit sentence on the row, and the three fields beside it once confirmed; the report card's Process line reads "exits explained K of N, C of those confirmed by you" in INTEGERS, and says `unmeasured` rather than zero when nobody opened the notes. | trader, first Day Review after an exit note |
| 179 | **A trade that exited twice is asked twice (TJ-9E, MERGED 2026-09-21, OWED)** - the shape 13 live trades have: closing legs on two dates give two boxes on two mornings, the second night drafts only the second note, and confirming one reading leaves the other exactly where it was. | trader, first trade closed across two sessions |
| 178 | **The night never sees how it turned out (TJ-9E, MERGED 2026-09-21, OWED)** - the request the `exit_note_fields` slot sent holds no price, no P&L, no R and no fill, read from the slot's OWN published file and its ledger row, never from a model log. A whole-reply rejection naming a bad span, an unknown code or a count over its bound is the bounds check working, not a failure. | lead, first night after an exit note |
| 177 | **The reading arrives on a morning the trade is not on (TJ-9E, MERGED 2026-09-21, OWED - the one that closes the loop)** - write an exit note one morning, let the night run, and the NEXT morning's card carries ONE `exit_note_fields` ledger row and the reading as its own row in the questions area with Confirm / Correct, **even though that trade is not in the session the card reviews**. Confirm writes ONE `EXIT_NOTE_FIELDS` row, the block disappears, and it never comes back. | trader + lead, second morning after an exit note |
| 176 | **A closed trade is asked why, and the words are on disk at Save (TJ-9E, MERGED 2026-09-21, OWED)** - a trade with a closing fill in the reviewed session shows ONE exit box on the 09:00 card (including a half-exited `CLOSED_PARTIAL` trade and a swing whose entry fields were answered last week); that trade's Save stays grey until the box holds words or one of the two answer-state buttons; and the words are on disk at Save as one `EXIT_NOTE_RAW` row with the note, the exit session and its provenance. | trader, first 09:00 card after the restart |
| 175 | **Never a mood paired with a result (TJ-7, MERGED 2026-09-20, NEW at the merge, OWED)** - the first Saturday after a week that carries a mood, the week package holds the `mood` section with its **session-qualified** ids, and the week story CITES one of them or says nothing about mood at all - **never a mood paired with a result** ("you lose when tired"). Nothing forbids that pairing today: the verifier only re-checks that a citation is in `allowed_source_ids`, and the one live fence is TJ-6's closed `MEASURABLES`, which mood is not in, so a `process` idea naming a mood is DROPPED. A narration/verifier rule is a later packet. | trader + lead, first Saturday after a week with a mood |
| 151 | **A mood shows, and an untouched strip changes nothing (TJ-7, MERGED 2026-09-20, OWED in THREE parts)** - (1) the trader clicks a face on the journal tab or on the session's last Mentor card, saves, and Day Review's "What you said" column shows the mood line with the score, the chips and the plan answer, `n` beside the count and **no percentage**; (2) a note saved with the strip UNTOUCHED writes exactly the row it always wrote (`mood` present and empty) and the line reads *"No mood recorded yet for this session (n 0)."*; (3) **the citation half - "the next story cites it" - is UNREADABLE until a night has written a pack AND a story for a session carrying a mood**: the live day-review folder held ZERO packs on 2026-09-20, so the first honest reading is `narrated 0 of N`, which is the feature working and not the gate passing. | trader, first session with a mood clicked (parts 1-2 the same evening) |
| 174 | **One ask a night (TJ-6, MERGED 2026-09-20, OWED - review 1's blocker, read on the live desk)** - on the first morning after a full night the ledger holds **at most ONE `improvement_ideas` row per session that loaded a model**; on today's desk, with zero packs, that is a repeating `skipped` row saying "tonight carries nothing to cite" and NO model call at all. A second model-loading row for one session means the asked marker or the `ok` ending is not doing its job. | lead, first morning after a full night |
| 173 | **A night never writes the trader's half (TJ-6, MERGED 2026-09-20, OWED)** - the night AFTER a Keep, the ledger shows the `improvement_ideas` slot ran and `ai_ideas_state.json` is **byte-identical** to what the trader's click left. | lead, first night after a Keep |
| 172 | **A kept idea is checked, and an overlap is not a change (TJ-6, MERGED 2026-09-20, OWED)** - the first Saturday after a kept `process` idea has run its window, Week Review prints that idea with a before and an after, each carrying its own `n`; under `MIN_REPORTABLE_N` it says "too few to call" and prints no rate, and when the two Wilson intervals overlap it says "no clear change" rather than naming a winner. | trader, first Saturday after a kept process idea's window |
| 171 | **One week story, on Friday's session (TJ-5, MERGED 2026-09-20, OWED)** - the first Saturday night the slot runs, the ledger shows ONE `week_review_narration` row on FRIDAY's session date, and NO such row on any weeknight. | lead + trader, first Saturday night after TJ-5 |
| 170 | **The week strip pools counts, not rates (TJ-5, MERGED 2026-09-20; the desk's own week OWED)** - the week strip's pooled `n` equals the SUM of the day cards' `n` over the same window, and its rate comes from pooled COUNTS with the ONE Wilson, never from the mean of the days' rates. Pinned as a NUMBER in `tests/test_tj5_week_strip.py::test_the_weeks_pooled_counts_are_the_sum_of_the_day_cards`; what remains owed is the same sum read on the desk's OWN week. | trader, first week with packed sessions |
| 169 | **The origin wake is asked once, with its caveat (TJ-12, MERGED 2026-09-20, OWED)** - on the first Mentor cards after a real session, a trade the desk cannot connect to anything the trader said before its first fill is asked where it came from **at most once**, the prompt carrying its own caveat about the unread lanes and offering `a_focus_pick`; a trade with a like or claim stamped before its first fill is NOT asked; a date-only broker fill is never asked; at most three budgeted questions sit on a card; AWAY prompts nothing; and an OPEN position past five exchange sessions is asked once a week whether the thesis is intact. This is also where TJ-14B's *a trade from nowhere is asked its origin once* is read. **Until TJ-12F lands**, the Process line must still name the Focus-add and armed-alert lanes as unread and never print a bare `unplanned`. | trader, first session after the restart |
| 168 | **`How fresh` names ONLY a slot whose LAST row was not `ok` (TJ-12, MERGED 2026-09-20, OWED)** - on the first desk open after a real night the line names the story's stamp, the last session with VERIFIED fill coverage and any slot whose LAST deciding ledger row for that session is not `ok` - **only** those, `failed` and `degraded` listed apart, a slot that ran and was later `skipped` NOT named. A session older than the ledger tail says *night status unknown for this session*, never "none reported trouble"; a covered session with no rows says it has no rows; and on a desk with no AI store the line says `night status unknown` and **no `ai_store/logs/` folder appears**. | trader, first desk open after a real night |
| 167 | **The tags say how much was written after the fact (TJ-16, MERGED 2026-09-20, OWED)** - on the first night the tagger publishes, `entries_written_after` is present in the tags-file header and in the next pack's `tags` block, and it is 0 on a session the trader answered live; nothing in the pack is filtered by it. | lead, first night the tagger publishes |
| 166 | **The tags reach the contrast (TJ-16, MERGED 2026-09-20, OWED)** - the night AFTER the tagger's first success, the next `prediction_contrast` pack carries `tag:<code>` features with both side counts and `tags.reads_matched` is non-zero; a zero there means the tags and the reads are not joining on `entry_id` and the contrast is blind to the words. | lead, night after the tagger's first success |
| 165 | **The tagger quotes what it read and never sees an outcome (TJ-16, MERGED 2026-09-20, OWED)** - on the first weeknight the tagger runs, the stored `observation_tags-<session>.json` carries a span for every code that reproduces its quote against the journal row, and the request body recorded for that night contains no verdict, grade id, price or bar. NOT a failure: `degraded_no_narrative` rows until a local model has answered this contract, and a rejection naming the CAP, a forbidden KEY or a REPEATED row - that is the bounds check working. | lead, first weeknight the tagger runs |
| 164 | **The right-vs-wrong contrast, off real clicks (TJ-16, MERGED 2026-09-20, OWED)** - after a session in which the trader answers Mentor cards with direction clicks, `prediction_contrast`'s pack carries a right-vs-wrong table by hour and by environment with `n` in every cell, and its `Rest of day` / `Next 5 sessions` counts match the read ledger by hand. | trader + lead, first session with prediction clicks |
| 163 | **The Mentor's pulls survive the day (TJ-14B, MERGED 2026-09-20, OWED)** - the day's pull tally SURVIVES a restart, so the second card of the day after a restart does not start the count again; the three pre-card pulls land on the 09:00 card, the middle card and the last card of the session and nowhere else, with TJ-9's three-day morning catch-up going FIRST on a not-ready morning and not spending one of them; and the overnight question appears as a CLICK once a day, with the old `One thing to test: ...` line not printed beside it. | trader, first full session after the restart |
| 162 | **A sold put is a sale (TJ-9Q, MERGED 2026-09-20, OWED)** - after the trader's own `--apply` run the Journal shows the three sold puts as SHORT and CLOSED with AAOI **+241.03**, BE -666.99, QBTS -57.96 and QQQ -81.98; the next Questrade import adds a fill without creating a second position for a contract that already exists; the overnight AI narration still shows for a re-keyed Questrade trade; and "Check a statement..." on a day holding a cover no longer reports a disagreement the size of twice the cover. | trader, next day with the market closed |
| 161 | **What leads to a good call (TJ-16, PLANNED)** - after two weeks of clicks Week Review shows accuracy beside three naive baselines, accuracy by confidence and a right-vs-wrong table by hour and environment, every cell with `n`; each narrated tendency points at a cell. | trader, two weeks after TJ-16 |
| 160 | **What the misses had in common (TJ-15, PLANNED)** - Saturday's Week Review shows, for the veto reason with the most real misses, what those misses measured differently, each row with both `n`. | trader, after TJ-15 |
| 159 | **The Mentor asks only for what is missing (TJ-14, PLANNED)** - an ordinary hourly card is one click; a new fill is asked its setup and stop on the next card; a trade from nowhere is asked its origin once; `Stop asking this` holds. | trader, after TJ-14 |
| 158 | **The night serves the trader first (TJ-13A MERGED 2026-09-19, first half READ 2026-09-20; TJ-13B MERGED 2026-09-20, its clause OWED)** - the morning brief lists no membership-only name, `ai_summary` is synthesized or fails fast and runs Saturday night only, no ledger row starts between 06:00 and 22:00 Pacific on any day, Sunday night's ledger shows only the deterministic stage plus whatever the weekend still owed; then (TJ-4) the day story before 23:30 Pacific and (TJ-13B) a week story whose ledger row names the large local model. **TJ-13B clause:** on a Saturday night inside the window and before ~05:15 PDT, with no AI job running, `--probe-model large` exits 0 and writes exactly ONE `manual_test` row whose `load_seconds`, `tokens_per_second`, `peak_memory_mb` and `context_tokens_accepted` are all > 0 with `basis` named, `peak_memory_mb` stays inside this box's 32 GB beside the running desk, `--status` still reports the slate unchanged, and `week_review_plan()` then answers `may_run_large: True`; a refusal exits 1, says why and touches no prior row, and `--force` re-measures but still loses to the window and to a held lock. The TJ-13A half was read 2026-09-20 (restart log). | lead + trader, first Saturday night after TJ-13B |
| 157 | **The report card (TJ-12, MERGED 2026-09-20; RE-WORDED at the merge, the amendment having made the card six lines)** - yesterday's Day Review opens on **SIX** lines - Did well, Missed, Your reads, Congruence, Process, How fresh - whose numbers match the tables under them; with no trades the Process line says so rather than printing zero; a line whose input is missing says so and prints no number; and clicking each line scrolls to the table or section it names. | trader, first Day Review after the restart |
| 156 | **Walk-away v2 (TJ-11 MERGED 2026-09-19; TJ-11F MERGED the same evening)** - **a Friday-evening call reads as FRIDAY's, measured from Friday's close**: the 18 D1 calls filed 2026-09-18 21:04-21:07 PT are on Friday's Day Review and not on Monday's; a name vetoed on Monday that ran 2 ATR by Wednesday is on Wednesday's Earlier-calls table; every table carries its sentence and the skill line shows `n`, `measured`, `pending` and the Wilson interval. | trader, first sessions after the restart |
| 155 | **Read grader and congruence (TJ-10, PLANNED)** - each Mentor answer with a view shows right / wrong / flat or pending with its date; the congruence counts match the day's likes. | trader, after TJ-10 |
| 154 | **Forced 09:00 trade labels (TJ-9, MERGED 2026-09-19, OWED)** - the 09:00 card lists yesterday's trades with a suggested setup, each trade's Save waits for THAT trade's answers and a saved trade is never listed again (per-trade since the 2026-09-21 fix, once merged), a skipped section returns at 10:00, the Journal shows them confirmed; plus a first-morning check that a `journal not ready` card becomes the questions once the fills land. | trader, first session after the restart (Monday 2026-09-21) |
| 149 | **Week Review on a Saturday (TJ-5, MERGED 2026-09-20; RE-WORDED at the merge)** - on Saturday Weekend Prep opens on Week Review with five cards and a week story whose examples cite the trader's own notes, and the ledger shows **ONE model call - a MEDIUM-local one until TJ-13B's probe row exists**, with the ledger saying why (`model_attribution.fallback_reason`); it is a LARGE-local call only after that measurement. **UNREADABLE until the post-close tick has written packs for three of a week's sessions**: on today's live store the honest first state is `0 of 5 sessions have facts` and `narrated 0 of 5` - a deterministic scaffold and no model call at all, which is the feature working, not the gate passing. | trader, first Saturday with three packed sessions |
| 150 | **The AI's ideas, and the trader's click (TJ-6, MERGED 2026-09-20, OWED)** - the morning after a night that ran, Day Review shows up to three ideas, each citing the trader's own evidence (a note or a walk-away row); **Dismiss hides one for good across a desk restart**; **Keep on a `program` idea shows it under "For WISHLIST - copy"** and nothing writes `WISHLIST.md`; the ledger shows ONE `improvement_ideas` row with its counts. **UNREADABLE until a night has written packs and a story for the sessions it reads** - on today's store the slot stores nothing and records `skipped` with no model call, which is the feature working, not the gate passing. | trader, first morning after a night with packs |
| 148 | **The morning-after story (TJ-4, MERGED 2026-09-20, THREE clauses)** - (1) the morning after an overnight run, yesterday's Day Review opens on a story with a headline, a were-you-right list whose every line names one of the trader's own notes and a chased verdict that cites the pasted forecast or says `unknown`; the ledger shows `day_review_narration` OK and the D1 view lists the trader's open theses (nothing to grade is a PASS, not a failure). (2) Every printed verdict equals the ledger's: that session's `market_read_grades` rows compared by `read_id` all agree, and the page's `graded K of N reads` matches the story file's own count. (3) A DAY **Redo story** leaves a marker, starts no process and the night clears it: both `sessions/<date>/pack.json` and `sessions/<date>/redo_requested.json` appear, no `run_ai_jobs` process starts, and the next night's `day_review_narration` row names that session, rewrites its `narration/<date>.json` and the marker is gone. | trader + lead, first morning after an overnight run |
| 145 | **One Day Review page (TJ-1)** - after restart the left nav shows **Day Review** where Market Journal and Daily Recap were, and neither old page is anywhere; opening Day Review on a completed session paints in under a second once its index exists and the first entry click is under 300 ms (bench on a staged home: the retired page's session read 13,500-16,500 ms across runs settle p50, Day Review's WARM read **820 ms p50 / 834 ms p95**, entry click 121 ms; **warm is the state after the close and outside trading hours (500-820 ms), which is when the page is read** - DURING a session an appended M5 row for one of the ~1,198 names that index's own observations reference (effectively the whole scanned universe) rebuilds it, 11,380 ms on the read worker with the Qt thread at 0.17 ms and the page still showing what it had, while an append for any other name is decided in 28 ms and keeps the open at 502 ms with the 22 MB body untouched; a COLD open of a session with no index yet paints after **12.5 s** while it builds one, and the post-close tick that normally builds it returns in **0.2 ms** and lands the index 11 s later on a worker - the desk must never freeze while it happens); no `[desk]` row is visible on the page, in the story or in the overnight narration, and flipping the Auto mode adds a line to the Auto Pilot log and no journal row; "Paste daily forecast…" stores a brief for the session the trader chose and shows it under External forecast, and a second paste for that session replaces it on the page; a note typed on the page and one typed on the desk tab both appear. NOT a failure: a past session saying "chart after the close" (TJ-2 brings the stored bars), three labelled walk-away placeholders, the empty ideas card, or the first open of a session being slow once - the index is written as it goes. **TJ-1L adds one clause: the page reads in two columns** - What happened / Open theses / the SPY pane on the left, the entries list over the reader with New entry and the forecast on the right, Walk-away as a 2 x 2 grid and What you traded beside the ideas card - **and every table fills its cell with its full headers ("Against me first %", not "ainst me first")**; the column split is the trader's to drag and it is still where he left it after a restart. | trader, next restart on the branch |
| 152 | **TJ-2A session bars** - after a closed session, `DAY_REVIEW_DIR\bars\<date>.parquet` exists; the log shows batched M5 downloads; opening that Day Review session draws its SPY tape. | trader, next closed session |
| 153 | **TJ-2B instant walk-away** - after a closed session, Day Review shows the four labelled tables; a passed name that ran has its post-decision result, a later trade is only in left-early, and a claimed D1 pick shows its own horizon state. | trader, after TJ-2B integration |
| 144 | **Overnight AI repair** - after integration, the next D1 scan imports and records theta picks without a circular-import failure, and a local enrichment run stays advisory, validates the closed five-field contract including its 2,000-character summary ceiling, and leaves prior artifacts intact on failure. If the backend explicitly returns an HTTP 400 grammar parse/initialization error, it makes exactly one JSON-object fallback request; other HTTP failures do not retry. | lead/trader, next scan + overnight run after integration |
| 143 | **Next-test local-model proposal (Phase 0.32 Packet 3)** - after integration, use a copied published entry-quality report with a configured existing local model. Verify one validated proposal references its exact report id/hash and cells, writes immutable JSON history plus `briefs/next_research_test/next_research_test.md`, and the Review card/copy brief agree. Record actual latency, reported tokens and peak memory when available; a disabled/offline model must leave deterministic facts and the prior memo intact. | lead/trader, after Packet 3 integration |
| 142 | **Next-test deterministic progress (Phase 0.32 Packet 3)** - on a copied bounded P8 `entry_quality_window` month with known completed-bar cells, run the warehouse/report/setup-research reader twice without a model. Verify `narrated K of N`, coverage/no-trigger/missing counts and current report id/hash agree in the compact input, current JSON/memo, Review card and Tracker route while the immutable proposal source stays named; unchanged evidence performs no inference, proposal/trial/history or live-data write, only the paired current-view refresh. | lead/trader, after Packet 3 integration |
| 141 | **Fair entry comparison (Phase 0.32 Packet 2)** - on a copied bounded completed-bar/P8 slice, normalize one P8 opportunity and one existing M5 occurrence through the reader. Verify every declared P8 entry variant has one row, a no-trigger and a missing input stay in the full denominator, shared-triggered movement is printed separately from full-population coverage, duplicated scans do not raise clusters or distribution/pair samples, an unregistered recipe refuses, and no ledger/store/live output changes. This is a coverage/read-contract check, not an edge, a winner or trial registration. | lead/trader, after Packets 1-2 integration |
| 140 | **Forward entry quality (Phase 0.32 Packet 1)** - on a copied bounded completed-bar slice containing a late M5 entry, a gap and a no-trigger variant, run the pure measurement and verify each row's exact endpoint, coverage and state; a post-stop rally remains in gross MFE, the daily two-touch row is ambiguous, and no source/store/live output changes. This is measurement only, not evidence of an edge or a trial registration. | lead/trader, after Packet 1 integration |
| 139 | **Trade Mentor raw-first AI help and large popup** - after restart, the popup opens near 900x820, keeps a manual resize across hide/show, and the 10:00 card saves the exact raw reply before local AI fills only missing fields; an existing edit stays, an ambiguous number asks one question, Save writes recalled evidence and no planned-risk field. | trader, next 10:00 prompt after integration |
| 138 | **Grounded market-story narration** - the next overnight ledger records `market_story_narration` OK, its file names weekly/monthly/quarterly periods and only allowed source ids, and the next Mentor card shows its one question; with the model off, the last verified file stays byte-identical. | trader, next overnight run |
| 137 | **An evening note stays with its subject session** - file a Market Journal note for the completed session after 21:00 Pacific; it appears in that session's story with `written_session_date` showing the later write day and `written_after_the_session=true`. | trader, first evening note after integration |
| 136 | **Trendline break + retest** - arm it on a saved scan line; only a completed-D1 break, later retest and later confirmation fire it once. A forming bar, redraw or automatic Focus sweep does not. | trader, first eligible armed pattern |
| 135 | **Review identity is split honestly** - the next review scoreboard carries `opportunity_identity_v1` restatement counts; an opposite side or D1/M5 thesis is a separate episode and an ambiguous old veto labels neither. Queue ordering remains unchanged. | trader, next scoreboard refresh |
| 134 | **The M5 scanner is a child** - after restart, one `bouncebot-scanner` child runs below normal, alerts/RRS/charts/Focus match the prior path, the desk stays responsive at the close, killing the child causes one clean restart, and closing the desk leaves no child. Compare thread CPU and stall files with gate #92. | trader, next live session |
| 133 | **One report, two views (WS-RP)** - after the next overnight run, the digest root has `measured_report_<session>.json` and its Markdown sibling, `entry_index.json` names the report id and as-of, and Daily Recap > Review prints those same cells and id. Copy does not write; Export writes a capped (32 KiB UTF-8) brief, payload and manifest with the estimated token count. A missing warehouse or missing measure says unknown; it never invents a result. | trader, next overnight run after integration |
| 132 | **Daily Recap is compact and closes itself (DR-REPAIR)** - after noon, each M5 stock/side appears once with its best measured alert, pending swings remain visible, the summary states factual counts, D1 decisions show D1 outcomes, and the page refreshes once after the exchange close. | trader, next session on the combined test branch |
| 131 | **The setups table cycles and a veto hides the row (SC)** - chart a setups row, then veto / claim / Next: the chart moves to the next visible setup. A veto hides that symbol and increments `Show vetoed (N)` while the Setup Tracker continues tracking it. | trader, next restart on the combined test branch |
| 130 | **The trendline break is one frozen D1 event (PCT-2)** - after the next scan, a row whose scan note reads `trendline break` carries the `TRENDLINE_BREAK` tag and exactly one `Trendline break` D1-feed row per `(symbol, side, break_date)` while the older champion rows remain unchanged. Arming **Trendline break** from a chart freezes that line's id, endpoint dates/prices and knowledge time; one completed D1 close through it fires once, a wick/forming bar does not, and a later chart redraw cannot move it. | trader, next scan and first armed break on the plan branch |
| 129 | **Three claim names (PCT-1)** - the capture rail offers "Pullback reclaim (M15 150 / M30 75 SMA)", "Trendline break" and "Compression break" under one "Entry timing and breaks" group; a claim on one is graded by name in My claims. NOT a failure: no evidence under them yet. | trader, next restart on the plan branch |
| 128 | **A pullback fire reaches the phone every time (PCT-1)** - when a completed M15 or M30 bar reclaims its SMA with an LRSI 80 cross, ONE phone alert and ONE feed row arrive naming the trigger and timeframe (e.g. `NVDA LONG: Pullback - M15 150-SMA reclaim + LRSI 80 cross (from below 50)`), the watch STAYS armed, the same bar never speaks twice (also across a restart), and a SECOND fire on the same watch also buzzes (the `watch_id` de-dup is per fire now). AUTO-armed watches push only the two reclaim triggers; a retest on them is a feed row without a buzz; a hand-armed watch pushes all three (lead decision, overrule if wanted). NOT a failure: `(not under 50 first)` on the line; two triggers on one tick on different timeframes. | trader, first fire on the plan branch |
| 127 | **The auto-arm is cheap and honest (PCT-1)** - within two minutes of a restart every active claimed D1 pick and swing Focus name is on the Armed board with `auto: claimed pick` / `auto: swing Focus` (about 100 names today), the desk does NOT hitch on that tick, `trading_bot.log` shows ONE batched yfinance download per interval per completed bucket (chunks of 50 symbols) rather than one per symbol, and `alert_review_events.jsonl` gains `auto_arm_watch` rows and NO `arm_watch` rows from the sweep (the review scoreboard must not count them as takes). Disarming an auto watch keeps it off until its claim / pick is dropped; arming it again from the chart brings it back as hand-armed. NOT a failure: only one watch per symbol when both sides are wanted. | trader, next restart on the plan branch |
| 126 | **The Pullback alert arms and measures (PCT-1)** - next restart, chart a LONG name and click **Pullback alert** (the H1 retester button is gone; an old H1 watch loads as a Pullback watch with only its H1 trigger): the Armed board lists it once with the reason `waiting for a pullback entry (LONG): H1 15-EMA bounce, M15/M30 SMA reclaim + LRSI, SMA retest` and a health cell with three states joined by `;`, the M15 one flipping from `not measured (N of 160 M15 bars)` to `M15 from yfinance` within one completed quarter hour, and the 12:45-13:00 bar present after the close. NOT a failure: no fire for days; `stale - last refresh failed` after a network outage. | trader, next restart on the plan branch |
| 125 | **The compression calibration report (PCT-3, trader 2026-09-15, plan branch)** - the trader runs `cd scripts && python -m compression_calibration --since 2026-08-20 --live`. PASS on day one: it prints `N vetoes: A joined, B untracked, C pending` with A + B + C = N, names the measured window and any excluded session, prints seven blocks each carrying an `n`, prints the `compression_flag hit rate` line, writes one CSV under `%LOCALAPPDATA%\TradingBotV3\diagnostics\`, finishes in roughly five minutes under a quarter of a gigabyte, and modifies nothing; without `--live` it refuses and names all three stores it would read. NOT a failure: every AUC below 0.5 - that IS the finding (first live-copy run: 213 vetoes, 140 / 50 / 23, hit rate 0.18, AUCs 0.34-0.49); a large `rest` population (~1,300 rows a session = every record still carried); a blank `last_replayed_session` counted for its entry day only. The threshold / penalty change is a SEPARATE ask after the trader reads this. | trader, any time after loading the plan branch |
| 124 | **The compression measure reaches the desk (PCT-3, trader 2026-09-15, plan branch)** - next scan after a restart, `master_avwap_ai_state.json` symbol entries carry `compression_score`, the three `compression_*_atr_ratio` fields, `compression_rule_version = "anchor_compression_v1"` and `compression_break_recent` + `compression_break_rule_version` (today's live file: 1003 symbols, 35 flagged, ZERO with a score - a non-zero count IS the pass); the setups table shows an amber `compressed` chip on flagged rows after the bucket and wrong-side chips (hover: score out of 3, the three ratios, the penalty), nothing hidden or reordered, no hitch when the report changes. NOT a failure: no chip on the very first refresh after a scan (the worker warms the cache; the chip lands on the next refresh); no `compression_break` tag for days; **the FIRST scan on this build widens `d1_features_history.csv` (667 MB, 256 columns) by eight columns once - about two minutes longer under the writer lock.** | trader, next scan after loading the plan branch |
| 123 | **The day-trade lists are wiped after the close (Phase 0.28, trader 2026-09-15, `claude/desk-combined-2026-09-14`)** - on the first restart after a session close: `longs.txt` and `shorts.txt` in the shared home are EMPTY within 30 s of the desk's first Auto Pilot tick, the Auto Pilot log carries one `Day-trade watchlists reset - longs.txt: wiped N name(s) after the <date> close; shorts.txt: ...` line, `python -m watchlist_intent_events tail --list longs` shows one `remove` row per name with source `session_reset`, and the Watchlist tab's next load adds NO `observed_external` rows. PASS on day two: a name typed after 13:00 Pacific is still on the list the next morning and gone within 30 s of that day's close while the desk is open; `swinglongs.txt` / `shortswings.txt` are untouched throughout; the M5 Focus board still shows its picks. | trader, next restart after a close |
| 121 | **The claims are graded where the likes already were (D1C-B, trader 2026-09-14, `lead/d1c-integration`)** - after the next nightly run on the restarted desk, Research > Setup Tracker shows a `My claims` tab: a populations block with My liked trades / FAV / HC / Near, each with win rate first, its n, the Wilson lower bound, pending and unmeasured, plus `also FAV` / `also Near` and a second column group for the `all` window; HC reads `unmeasured: the tracker records favorite_setup / near_favorite_zone only (0 HC rows)` and never 0%; a by-setup block sorted by the bound; the status line names no setup below n = 30. PASS on day one: the tab's numbers match `python -m claimed_pick_evidence --window lately` (run from `scripts/`) byte for byte, and the tracker rows read `0 (mature by construction)` for pending and `-` for unmeasured with one `tracker read excluded: ...` footnote. NOT a failure: every claim made this week is `pending` until its fifth session closes; FAV and My liked trades having different n is the two clocks working. | trader, next nightly run after loading `lead/d1c-integration` |
| 120 | **A claimed D1 like is a pick, and the chart is done (D1C-A, trader 2026-09-14, `lead/d1c-integration`)** - next restart, on a D1 chart in Visual Alert Review press Alt+K and double-click a setup: the status line reads `claimed <setup> - placed in Setups`, the chart moves on, and the Setups tab on the right shows that symbol with the chip `My liked trade` (plus `Favorite` / `High Conviction` on the SAME row when the scan already carried it) within a second, still there after a rescan and after a restart. The same D1 flag firing again that day does not bring the chart back and the pane's badge row states `N D1 chart(s) skipped - already claimed`; an M5 alert for that name still lands in the left M5 list; an armed chart-watch on it still charts. The Setups strip shows five chips FAV / HC / Near / Liked / All that combine; right-clicking a claimed row offers `Drop my claim`, after which the next D1 flag charts again. A typed ticker after an M5 chart writes its like row as `D1` with no sidecar. NOT a failure: a blank Score, a Points total of +0 on an unmeasured claimed row and a tooltip naming `not measured` / `family ungraded` on a claimed name the scan does not carry; a claim expiring on the eleventh trading day rather than the tenth (the packet's own rule). | trader, next restart on `lead/d1c-integration` |
| 119 | **M5 left, D1 right (D1C-L, trader 2026-09-14, `lead/d1c-integration`)** - next restart the left column is the M5 alerts list alone with the Working-lately line on top and no swing picks strip under it; press the Setups toggle or F9 and the right column shows Setups on top with the swing favorites strip and its Add / Paste / Copy / Long / Short controls UNDER it, the boundary draggable (opens about 6:1) and remembered for the session, and every chip's X still drops the pick (the name leaves swing Focus and a retraction row is appended). PASS on day one: type a name, press Add, see the chip under the setups; drag the boundary, switch to tabs mode and back, and the chips are still there. NOT a failure: the strip is out of sight while the setups column is hidden; `qt_m5_column_split_sizes_v1` is never rewritten. | trader, next restart on `lead/d1c-integration` |
| 118 | **Two contexts per row, three verdicts per thesis (WS-10I, WISHLIST 10I, sweep branch)** - next restart, Research > Results carries a **By environment** control beside the window buttons. Leaving it on "All environments" must change NOTHING - the champion cards, the verdict lines and the freshness line are the same page they were. Choosing `compressed` re-cuts Bot x Swing into environment x side rows with n, the Wilson lower bound and `unknown` as its own row, and the line above says which benchmark, which rule version and which moment the cut is about ("the tape the opportunity was OBSERVED in" on Bot, "the tape the ENTRY happened in" on My trades); Bot x Day re-cuts to a held x ran table with its own heading. On Daily Recap a decision matched to a fill shows two labels with an arrow between them and one label where there is no fill; the tooltip still names the session's own label. PASS on day one: the control moves the numbers and the page never blanks; the two Daily Recap labels differ by one session on an intraday row and that is CORRECT. NOT a failure: an environment with no rows showing an empty table; `unknown` rows where the environment store has no reading for a session (all of them until the trader's ENV backfill `--apply`); the Daily Recap's Environment column disagreeing with the session's own label in the tooltip - that gap is the whole point of the packet | Trader, next restart on the sweep branch (unknown rows until the ENV backfill --apply) |
| 117 | **One Watchlist, and the two pages are gone (WS-WL, WISHLIST 10G, sweep branch)** - next restart: the left nav no longer lists **Chart Review** or **Focus Picks**, and the Trading Desk's setups column (the "Setups" toggle or F9 if it opens hidden) carries a **Watchlist** tab beside Setups. PASS on day one: type a name in the box and press **Add** - it lands on `longs.txt` / `swinglongs.txt` per the side and horizon chosen, once, with a `mine` badge; press **Chart only** instead and it charts on the centre chart and joins no list; **Paste many** with a name already there reports it and does not double it; the Positions view lists an open journal position with its account, quantity and last sync time, greyed and labelled `sync stale` if the last verified import is older than the previous close; **Ctrl+L** puts the caret in the box; a row's **Arm alert** writes one phone alert and **Disarm** leaves it in the list, disarmed; the Journal's **Positions on the Watchlist** button lands on that view; after another restart the pasted name and the position are both still there; removing a name you typed leaves a Focus pick's injection of another name alone. NOT a failure: an empty Positions view on a desk where the Journal has never been prepared (the tab refuses to create the database - the status line says `positions not read yet`); an option position showing a quantity and no dollar exposure; a name on no list wearing only an `alert` badge; the old **Watchlists** file-editor tab still sitting beside the new one. OPEN FOR THE TRADER: rename the old **Watchlists** tab (e.g. "List files") and whether the desk should open with the setups column shown | Trader, next restart on the sweep branch |
| 116 | **The H1 retester is armed from the chart and fires once (WS-10C, WISHLIST 10C step 1, sweep branch)** - next restart the arm bar under the chart shows a new **H1 retester** button; chart a LONG name and click it: the Armed board lists it with the reason `waiting for an H1 15-EMA bounce (LONG)`, its arm DATE, and a health cell reading `H1 from cache`, `H1 from yfinance` or `not measured (N of 45 H1 bars)`. PASS on day one: the button arms and disarms; the row survives a desk restart AND tomorrow's date roll while a `New HOD` armed beside it disappears at the roll; the health cell flips from `not measured (N of 45 H1 bars)` to `H1 from yfinance` within one completed hour (that single transition proves the fallback, the poll and the warm-up refusal at once); when a completed H1 bar tags the 15-EMA and a later one closes back above it, ONE phone alert arrives and one row appears on the D1 feed reading `H1 15-EMA retest confirmed` with both bar times, and the M5 and D1 detector feeds show nothing else new from it. NOT a failure: `not measured (N of 45 H1 bars)` for the first hour; `not measured (..., yfinance unavailable)` while the network is down; no fire at all during the week (a retest either prints or it does not; a watch that expires after 10 trading days with an `armed_alert_expired` row did its job); a fire is NOT a claim the trade was good. OPEN FOR THE TRADER: the yfinance fallback is the lead's ruling - the alternative is a wider M5 window for armed symbols in `bounce_bot_lib` (ask-first) or a `v2` rule sheet on less history **AMENDED 2026-09-13 by the review repairs (B1-B3):** (a) the health cell has FOUR states - `H1 from cache`, `H1 from yfinance`, `H1 from yfinance (stale - last refresh failed)` and `not measured (N of 45 H1 bars[, yfinance unavailable])`; leave an armed watch running across a session close and confirm the next session it is still measuring (the bar count does not freeze and the cell does not drift into `not measured`); if it ever reads `stale`, say so rather than treating the verdict as live; (b) **a bounce that finished before you armed does not fire** - chart a name that has already bounced off its hourly 15-EMA, arm it, and nothing should happen; arming and immediately getting an alert is a FAILURE, and re-arming after a fire does not re-fire on the same bounce; (c) when a watch fires, the feed row and the chart appear at once - the desk must NOT pause while the phone is being notified - and the buzz may land a moment later; if the phone endpoint is slow or down the desk stays responsive and the Alert Center's push status shows the reason afterwards; closing the desk right after a fire waits about two seconds at most. | Trader, next restart on the sweep branch; first live arm on one LONG name |
| 115 | **Daily Recap reads the day from the stores (WS-DR, WISHLIST 10F + 5F, sweep branch)** - after the next desk restart the left nav shows **Daily Recap** where AWAY Recap was, in the same slot under Market Journal; it lands on the LAST COMPLETED session with "Today" offered below it and marked provisional, the swing lookback on 3, and four tabs - What worked today, Swings that followed through, My decisions, Rejected and it worked - each printing its own line of cohort, window, n and pending count. PASS on day one: all four tabs draw rows for a normal session; every number is a signed percentage and an unmeasured cell is an em dash whose tooltip says WHY, never a 0.00; restart the desk, reopen the page on that same session and the tables read the same; double-click a row in "Rejected and it worked" and the chart opens on that name with the trader's own reason and the adverse move in view beside the favorable one, and the waiting list does not grow by one; the staged-pick block still lists AWAY's staged names and its Add button still adds to swing Focus. NOT a failure: an empty "Rejected and it worked" tab on a day the trader refused nothing that later ran; a blank "After the decision %" on every row except a day-trade pass (only a pass writes the M5 sidecar that makes the timing knowable); `unknown` in the Environment column for a session nobody labelled; a "Journal match" of `no_match_after_window`; an "unavailable sources" clause naming a store the desk has not written yet; a swing tab with fewer rows than expected on a week containing a holiday (the lookback is exchange sessions, not days). ASK OWED (not built): the builder's one-line `add_alert` change in `alert_center_panel.py` that would skip the enqueue door for a symbol-less status row - behaviour-neutral, an alert file, so it stays a question | Trader, next restart on the sweep branch |
| 114 | **The Market Journal tells the story and challenges the thesis (WS-10D, WISHLIST 10D, sweep branch)** - next restart: open Market Journal and select the last session; the Story pane reads "You said" (the notes verbatim, each marked written-during or written-after), then "The market did" (all six of SPY / QQQ / IWM / VXX / TLT / USO named, an unmeasured one saying so with its reason), then "Sources" as clickable ids that jump the list to that entry; the Active theses list shows a note containing "if SPY loses 5,400" with that sentence as its invalidation and a note without one reading `invalidation: unstated`; selecting a conditioned thesis asks one or two questions quoting the trader's own condition; saving an interpretation writes a new row in `market_theses.jsonl` while the note stays byte-identical; "Paste weekly forecast..." files under "External forecast", never "You said", creation time `unknown` when not supplied. PASS on day one = the pane renders for the last session with at least one real note and the sources click through. NOT a failure: every benchmark `unmeasured` (the daily-bar cache is scan-fed); an empty theses list on a session with no note; the weekly rollup absent until the next overnight run - the overnight PASS is `market_story_rollups` in the job ledger with a weekly pack naming its sessions exactly | Trader, next restart + next overnight run on the sweep branch |
| 113 | **New bars only (WS-SN2, WISHLIST item 4, sweep branch)** - next live day after a restart: `trading_bot.log` carries one `SN2 M5 window fetch, cycle N: A full window(s) = B bar(s), C delta(s) = D bar(s); E bar(s) fetched in total` line per cycle, and from the SECOND cycle of the session onward `C` is most of the scanned set and `E` is down by an order of magnitude from the first cycle's (expect roughly 230,000 -> under 20,000 rows a cycle); `thread_cpu.jsonl`'s `run_strategy` core fraction in hour 13 is below SN3's reading; the day's M5 alerts for the names that alerted the day before still fire. NOT a failure: a handful of full windows every cycle (a new name, a halted name, a revised print - each logged as a refetch); the first cycle of the session being all full windows; the desk's RAM ~100 MB higher (the kept windows). A FAILURE is `A` staying at the whole scanned set every cycle (IB's forming last bar tripping a refetch - check `SN2_FORMING_TAIL_FORCES_REFETCH`) or any alert the previous day produced that this day does not | Next live day on the sweep branch |
| 112 | **The said-vs-did report has both halves (WS-5B, WISHLIST 5B, sweep branch)** - after the next nightly `preference_trade_outcomes` slot on the sweep branch the ledger reason reads `N endorsement(s) and M refusal(s), never pooled` beside `n_trades_matched`, and the CSV header ends `like_mode,verdict_family,match_state` with `schema` = `preference_trade_outcomes_v2`. PASS on day one: the reject half is NON-EMPTY and the LARGER one (live counts 2026-09-13 over 45 days: 473 vetoes, 401 not-todays, 91 M5 click-aways, 27 dislikes vs 148 claimed likes, 623 pick likes, 1 pass) - a reject count of 0 is the failure; then Weekend Prep > Focus Review shows a tenth button "Said no" whose table lists the week's refusals with `Match state` filled and whose note counts them apart from the likes. NOT a failure: a refusal with `no match` + `window_open`; `matching_unavailable` never appearing; the `unfavorite` rows never appearing; an empty "Said no" table in a quiet week; the AI package's `preference_to_trade` counts growing | Next nightly preference_trade_outcomes slot + Weekend Prep on the sweep branch |
| 111 | **The Journal tags a trade from the setup the trader wrote about it (WS-10E, WISHLIST 10E, sweep branch)** - on the first nightly run after the desk restarts, with a closed trade and a Market Journal note about that symbol written inside the trade's own window naming a setup: the Journal's Trades detail for that trade shows a `note lane: <setup> from note mj-... "<the words you typed>"` line above the overnight AI note, and Weekend Prep's Tag this week lists that trade with `provisional` in Status and `your journal note` in the new `From` column, waiting to be confirmed. PASS on day one is that ONE such row exists and the quoted span is words the trader actually typed. NOT a failure: `note lane: no explicit claim in N candidate note(s)` (the lane refusing to reach); `note lane: unmeasured (date-only fill)` on a broker-imported trade; no line at all on a trade the tagger has not visited since (press Rebuild auto tags); a confirmed tag staying exactly as it was | First nightly run on the sweep branch with a closed trade + a same-window note |
| 110 | **Trade Mentor pop-up and hidden context (WS-TM, amended by trader 2026-09-14)** - after loading the reviewed branch with the desk stopped: Give a read opens one editable modeless window, and another click reuses it; typing and Ctrl+Enter save the raw read, while X/Escape keep a draft and record a skip. The 10:00 trade form scrolls with every control reachable; the arm bar and chart height stay fixed. With the Settings checkbox ON, the next eligible whole hour opens the same window without taking keyboard focus from the field being typed in (native Windows live check; offscreen verifies intent flags, not window-manager behavior). Pause and expiry hide it; AWAY still opens no automatic prompt. A saved read carries its own hidden, timestamped context for the 17 requested symbols, and the existing AI journal source keeps their compact measurements beside the words. NOT a failure: an explicit missing/stale/pending context, no prompt after over 20 minutes idle, or the checkbox OFF (default); automatic AI coaching remains unbuilt. | Trader, next restart after loading the reviewed follow-up; native focus and live cache/provider freshness remain owed |
| 109 | **Three clocks on the scan (WS-10A, WISHLIST 10A, sweep branch)** - after the next desk restart the Setups panel's status row carries one line with three clocks (`Scan ok HH:MM - inputs through <Day MM-DD> (D1 complete) - shown: HH:MM report`) and System Health shows the SAME sentence under `master_scan_freshness`. PASS on day one: after the close slot, `data/runtime/master_avwap_scan_manifest.json` says `latest_input_bar_session` = today with `preview_bar_used: false`, `status: ok` and `symbols_fetched == universe_size` (~1,097 - if `universe_size` were wrong every scan would read `partial`, the one thing no test proves on the real payload); and `cd scripts && python -m master_avwap_lib.scan_replay --symbol <a name the trader picks> --session <today>` prints a four-checkpoint trace. NOT a failure: an intraday scan saying `inputs: today preview` (the forming-bar rule working); `close - no recorded snapshot` before the close slot has run; `Scan: not recorded yet - no report` on the very first launch; a `partial` line on a day a provider dropped symbols - the manifest being truthful is the point | Next scan on the sweep branch |
| 108 | **Wrong side is shown, never hidden (WS-WS, WISHLIST item 9, sweep branch)** - at the next desk restart: (1) on the Master AVWAP setups table, a LONG whose close sits under its current AVWAPE carries an amber `wrong side` chip immediately right of its bucket chip, and hovering that cell shows the bucket label with a second line naming the side and what was read (`LONG below AVWAPE (band zone LOWER_1 to VWAP)`); a SHORT above its anchor carries the same chip; on the 2026-09-11 scan look for NU, JCI, NUE, MFC (LONG, under) and HDB (SHORT, over) - roughly one row in five, about 93 of 435; (2) NOTHING disappears or moves: row count, sort, bucket filter, movers-only, the Points switch and the scores are what they were, and the chip is absent from every other column; (3) the next AWAY digest tags such picks ` [wrong side]` after the symbol and ends the pick list with `N wrong side of the anchor`. NOT a failure: a refresh with no wrong-side row (a clean board is a real answer); the chip absent in the compact profile (the cell is 96 px; the tooltip still says it). STILL OPEN (trader): hide vs mark, the previous anchor, and the other surfaces | Trader, next restart + next AWAY digest on the sweep branch |
| 107 | **Chart history and the viewport (WS-CH, WISHLIST 10H, sweep branch; rewritten 2026-09-13 after Astra's review B4 - the standalone Chart Review page was retired by WS-WL and is not a door)** - next restart: (1) chart any symbol on the centre Visual Alert Review chart through a door that exists: a ticker click anywhere on the desk, or the Watchlist tab's **Chart** / **Chart only** buttons (Ctrl+L, type a name, Chart only) - it opens on the same ~90 daily candles as before (same price scale, same painted levels, no line from four years ago), and dragging LEFT keeps producing bars back to roughly four years (up to 1,000 sessions, whatever the store holds) with no pause and no network; (2) on a symbol the bot is scanning, press **Load older** under the M5 chart twice - the bars extend by two sessions each time and the candles you were looking at stay exactly where they were; after four presses the chart holds ten sessions and the button reads `Load older · 10-session max`; (3) the desk stays clickable throughout. NOT a failure: a symbol that pans back only a year or two (the store holds what the scan built); the drawn levels stopping at the left edge of the opening window (levels are computed for the opening view and panning does not recompute them); `older bars unavailable` on the button (the cache could not serve the older sessions, the chart is untouched) or the button doing nothing on a symbol outside the scan set; there is NO `D1 back to <date>` readout on the centre chart (that strip lived on the retired page; an oldest-date readout on the centre pane is an open trader decision, DESK_INTERNALS "The centre pane's own provenance line"). Still owed by this gate: the live proof that the centre chart's D1 history reaches the store's oldest sessions on a real symbol | Trader, next restart on the sweep branch |
| 106 | **The theta picks are recorded and graded (WS-TH, WISHLIST item 6, sweep branch)** - after the next D1 scan on the restarted desk, `theta_picks.jsonl` in the shared home holds ONE row per theta symbol for that scan date (sold puts and PCS rows both, each with its support set, rank, both scores and the sold strike / expiry / premium), and a second scan the same day adds NO row; after the next overnight run `master_avwap_theta_outcomes.csv` sits beside `master_avwap_tier_outcomes.csv` with every row `pending` and `held_5/10/20` blank, and the AI-jobs ledger shows `theta_pick_grading` OK directly after `daily_digest`; the Setup Tracker's Theta tab shows its population sentence and either "No export yet" or the table. PASS on day one is exactly that: rows recorded, an export written, nothing graded - a `pending` row, a blank `held_at_expiry`, `n = 0` on every cell, an empty `first_support_broken` and "not enough per third yet" are CORRECT (5 sessions is the earliest any `held_5` can fill, 20 the earliest a cell carries an `n` and a bound). A FAILURE is a theta report written with no store row; a `held_*` that is False on a session the calendar has not reached; a blank `strike` on a PCS row; an `n` that grew when a symbol reappeared; or any change to `master_avwap_theta_puts.txt`, a theta score or a support stack | Next scan + next overnight run on the sweep branch; cells 20 sessions later |
| 105 | **The Trades splitter opens at 3:2 with no blank band (WS-J1, WISHLIST item 1 leftover, sweep branch)** - next restart: Journal > Trades opens with the table taking about three fifths of the width and the detail pane two fifths, the Tag review row one slim line directly above the table with no empty band between them; drag the splitter and it stays where you put it for the session; the rows, columns and counts are unchanged. NOT a failure: the split returning to 3:2 after a restart (nothing is persisted by design) | Trader, next restart on the sweep branch |
| 104 | **One RRS pass per cycle (WS-SN3, WISHLIST item 4, sweep branch)** - next live day after a restart: `trading_bot.log`'s cycle preamble shows ONE `rrs_scan` stage per cycle where it showed `rrs_scan_5m`, `rrs_scan_15m`, `rrs_scan_1h` and `rrs_scan_gui`, and its seconds are well under the four lines' 275 s sum; `thread_cpu.jsonl`'s `run_strategy` core fraction in hour 13 is below 2026-09-08's 0.62; the RS Window tab and the Strength page show the same numbers for the same minute as before, and switching the RRS timeframe still repaints. NOT a failure: `rrs_strength_scan.csv` carrying three blocks per cycle instead of four, `rrs_group_strength.csv` one instead of four (those duplicates were the fourth pass), and `environment_focus_history.json`'s `hit_count` still advancing four times a cycle (kept on purpose). STILL OWED (ask-first): dropping the duplicate fourth `_record_environment_focus_history` call, a scoring input | Next live day on the sweep branch |
| 103 | **The alert feed diffs itself (WS-SN4, WISHLIST item 4, sweep branch)** - next live day after a restart: `%LOCALAPPDATA%\TradingBotV3\diagnostics\ui_stalls.jsonl` shows no stall over 1,000 ms charged to `alert_center_panel.py` on a veto or a Focus change (2026-09-08's were 4.0-4.1 s and 24.2 s); the feed right after a ✕ shows the same rows minus that one, in the same order, with the surviving ×N badges still on them and the gold Focus frames still gold; liking a name from a feed row lights THAT row's star, frame and ★ badge and moves nothing else; the minimum-tier switch and Clear still repaint the whole feed. NOT a failure: a name that had just escalated dropping from two rows to one at its original position (one live row per symbol + side is the rule); the open-burst digest row staying on screen through a veto and naming one fewer name; a row count that differs from the backing list (a repeated name is one row) | Next live day on the sweep branch |
| 102 | **One D1 environment label per session (WS-ENV, WISHLIST item 7, sweep branch)** - first scan after the restart: `trading_bot.log` carries ONE `D1 environment: SPY=<label> QQQ=<label> IWM=<label> (d1_environment_v1, bars through <session>)` for the last COMPLETED session; `C:\TradingBotData\d1_environment.jsonl` gains exactly three rows for that session and never a second row for a session already there, however many scans run that day; Research > Results (Bot setups x Swing) shows "By environment (SPY, d1_environment_v1)" under the two champion sections with win rate first, n, the Wilson bound and `unknown` as its own row. PASS on day one = the log line, three rows, and the section present with every row reading `unknown`. Then the TRADER runs `python -m d1_environment_store backfill --benchmark SPY --since 2025-12-17` (dry, then `--apply`; expect 33 unknown / 69 compressed / 43 mixed / 27 trending_up / 12 trending_down over the 184 cached sessions) and the section fills. NOT a failure: an all-`unknown` section before the backfill; a `compressed` SPY label on a quiet week; no line on a scan where no benchmark returned completed bars. STILL OWED (ask-first): the legacy.py stamp on the tracker outcome row is not built - the dated store joined on `scan_date` answers the same point-in-time question | First scan on the sweep branch; then the trader's backfill --apply |
| 101 | **The board says whether it is being scanned (WS-10B, WISHLIST 10B, sweep branch)** - the next DESK session after a restart: (1) on the Strength window's M5 Strength Board (TC2000) the rightmost column is `Scan` and every visible row has text in it - `adopted` on the parity rows that joined M5 Focus, `already_in_focus` on names already there, a verbatim reason on the rest (`not adopted: not above yesterday's high`, `not today`, `declined today`, `not adopted: floor ...`); (2) `longs.txt` gains each newly adopted name exactly ONCE across two refreshes and every trader-typed name is still there; (3) `trading_bot.log` carries one `Strength board: N rows, A adopted, S staged, R not adopted (reasons: ...)` line per refresh and the next BounceBot `Monitoring N` cycle lists an adopted name; (4) the AWAY leg: flip Auto to AWAY for one refresh and the column reads `staged (AWAY)`, `longs.txt` and the Focus lists gain nothing, the names sit in the auto-populate queue, and flipping back to DESK adopts them through the existing drain (re-measured first). NOT a failure: every row `already_in_focus` on a long-running desk; `0 staged` in DESK mode; a blank column with no Focus store | Next DESK session on the sweep branch (+ one AWAY refresh) |
| 100 | **The overnight advice is never blank on success, and a night names its completion (WS-AI1, WISHLIST 10K step 1 + 5C, sweep branch)** - on the first nightly AI run after the desk restarts on the sweep branch: (1) the `journal_enrichment` ledger row reads `enriched A, abstained B, failed 0 of N`, A > 0 when a trade closed that session, naming any superseded blank rows, and the Journal's Trades tab detail shows a non-blank "Overnight AI note (advisory)" with status, confidence and written_at for one of those trades; (2) the main summary's ledger row names its completion (`synthesized` / `partial` / `unsynthesized_fallback` / `failed`) and is `ok` ONLY when `synthesized`, and the System Health AI row prints the same word; (3) the package carries the `preference_to_trade` section whose `n_statements` / `n_trades_matched` / `n_trades_unmatched` reconcile to the ST5 report and whose three coverage counts sum to the unmatched total. NOT a failure: B > 0 (the model declining is a recorded answer); a night with no closed trades (the row says so); the six 2026-09-09..11 blanks still in the table beside their replacements (append-only); a `degraded_no_narrative` row after an `unsynthesized_fallback` - that is the gate working and the document was still published | First nightly AI run on the sweep branch |
| 99 | **The star and the X are the day's decisions (WS-SX, WISHLIST item 8, sweep branch)** - next restart, on the Desk's setups table: like a row with Alt+L and its star fills within a second with the tooltip `Liked today (quick, hh:mm)`; veto one from the chart and its X turns bright red with `Vetoed today hh:mm` within ~200 ms and no table stall; a symbol both liked and vetoed today shows both marks; nothing moves, disappears or changes order; the next morning both marks are back to their plain state. NOT a failure: a name only unfavorited, or only looked at, wears no mark; a rail "skip for now" is not modelled; the "Reviewed today" badge still reads what it read before (the two sets differ by design) | Trader, next restart on the sweep branch |
| 98 | **A forming bar never reaches the daily-bar cache (WS-FC1, WISHLIST item 2, sweep branch)** - next scan after a restart: the run manifest carries `daily_bars_forming_dropped` and `daily_bars_invalid_dropped` (both present even at zero) and `trading_bot.log` carries exactly one line per scan naming both with their totals; a scan that runs mid-session shows a non-zero forming count and the symbol's cache file still ends on the previous session; no cache file written that day ends in a candle breaking `low <= open, close <= high`. Then the TRADER runs `cd scripts && python -m master_avwap_lib.daily_bar_cache repair` (dry run; expect 66 files named on the 2026-09-12 count, re-measured on the day) and, when satisfied, `--apply`: 64 rewritten with a refetched session bar, MCW and TERN reported as removed with no replacement (Yahoo returns nothing for them), and a re-run of the dry run reports 0. NOT a failure: zero on both counters after the close; the 66 pre-existing files still there until `--apply`; MCW / TERN losing their last row - a bar that never happened is not evidence. STILL OWED (ask-first, the trader's word): whether `_persist_durable_daily_bars` should filter the Parquet mirror too, and whether `fetch_daily_bars`' return should stop carrying today's forming bar to the D1 indicators (a detector-input change, golden fixtures first) | Next scan on the sweep branch; then the trader's repair --apply |
| 97 | **The exit comparison answers it per setup (WS-EF1, WISHLIST item 3, trader 2026-09-08, sweep branch)** - after the next persisted tracker write on the restarted desk: (1) `master_avwap_exit_framework_by_family.csv` exists beside `master_avwap_exit_framework_stats.csv` in the runtime folder, the pooled one unchanged in shape (24 rows, same header); (2) Research > Setup Tracker > **Exit frameworks** shows a `Setup family:` picker above the sentence with `All setups (pooled)` first and the table below it exactly as it reads today; (3) picking `1stdev_breakout` shows only that family's rows ranked by the Wilson lower bound with the sentence naming the family, its `study` population and its largest `n_closed`, and the words `BELOW FLOOR` because every cell closes under 30 - the rows are still SHOWN; (4) picking `avwape_to_1stdev` shows 36 rows with no `below floor` wording. NOT a failure: a family whose rows are all under the floor, a family with no experimental twin, or a `control` family with a blank Bucket - control records carry no priority bucket | Next persisted tracker write on the sweep branch |
| 96 | **The weekend verdict card names a cohort (WS-5A, WISHLIST 5A, sweep branch)** - on the next Weekend Prep refresh after a desk restart, the verdict card's like line and rejection line EACH name a cohort with a side, an n and a percent (no "R" after a percent) or say `nothing with enough behind it yet (best n was N against a floor of 5)` / `nothing has matured to 3 sessions yet (K row(s) at other horizons)` / `no like|veto cohorts measured yet`. PASS on day one: at least one of the two names a cohort (on copies of the live rollups: `Likes that work: human_focus_like_avwape_to_1stdev LONG +2.45% over 3 sessions (n=14)` and `Rejections worth another look: human_focus_veto_v1_incoming_trendline SHORT +3.72% side-adjusted (n=11)`), and the two cohort tables' Avg and n cells read exactly as they did (`+1.90%`, `21`). NOT a failure: the pooled `ALL` rows never appearing in either line; a named cohort whose n is under 30 (the card's floor is 5 by the builder's ruling, the trader may raise it); no "Rejections that were right" line (the card is capped at eight lines) | Next Weekend Prep refresh on the sweep branch |
| 95 | **The AWAY digest's swing order follows the Points switch (WS-PT4, WISHLIST item 4 block 4, sweep branch)** - after the next desk restart on the sweep branch, with `Points` ticked on the setups strip, the next AWAY session's `autopilot_today.txt` swing block ends `Ranked on: ... | order: points (switch on)` and its top names are the names the desk's setups table shows first with the same switch on; with `Points` unticked the block reads `... | order: Wilson bound` and the pick lines are the ones the digest printed before this change. NOT a failure: an order different from the Wilson one (that is the point), a non-favourite/near/high-conviction bucket sitting below them, or a `+N more near-favorite rows hidden` line - the cap is applied after the ranking, as it always was | Next AWAY session on the sweep branch |
| 94 | **Watchlist intent is a dated event (WS-5D, WISHLIST 5D, sweep branch)** - at the next desk restart on the sweep branch: open the Watchlists page, add a name on Shared Longs, then run `python -m watchlist_intent_events tail --list longs` from `scripts/`. PASS on day one is one `add` row with `trader_edit`, today's date and an offset in `ts`; removing the name appends a `remove`; adopting a Focus pick appends `machine_inject`; editing `longs.txt` in Notepad and pressing Refresh appends `observed_external` rows stamped at the REFRESH time, never an invented earlier one. NOT a failure: one `baseline_recorded` row per list on the first load; `observed_external` rows for names the AWAY auto-populate added while the desk was closed (that writer is not labelled yet); a `(intent not recorded)` status suffix, which means the evidence append failed and the watchlist was still saved | Trader, next restart on the sweep branch |
| 93 | **Workspace memory recalls and never authorizes (WISHLIST 11, trader 2026-09-12)** - in a fresh Claude session and a fresh Codex session: (1) a bounded question about a prior trader preference (for example the options strategy or the chat rule) is answered by reading `MEMORY.md` and ONLY the matching detail file, citing file, tag and date; (2) a live-status question ("where are we", "which branch") is answered from this checkpoint and the code, never from `memory/`; (3) a recon or reviewer run leaves `memory/` untouched (`git status` clean there); (4) no recalled line is offered as authorization for app work. | First Claude and Codex sessions that boot with `MEMORY.md` |
| 92 | **The scanner breathes and scans the trader's picks first (SN5/SN6, trader 2026-09-08)** - on the next live day after a restart: (1) `trading_bot.log` shows the fast-lane line `scanning N Focus symbol(s) ... (T trader-picked first, then A auto-adopted)` and the first symbols scanned are the trader's own; (2) `%LOCALAPPDATA%\TradingBotV3\diagnostics\thread_cpu.jsonl` gives `run_strategy` a lower core fraction in hour 13 than 2026-09-08's 0.62 and `ui_stalls.jsonl` fewer blocked seconds per hour than that day's 245-365; (3) every Focus name still appears in the cycle (the set is unchanged); (4) shutdown is not slower. A worse number is a finding, not a revert - SN1-SN4 are the real fix. | Next live day |
| 91 | **The point system grades itself (trader, 2026-09-08)** - (1) after the first report refresh on the restarted desk, `setup_points_log.jsonl` exists in the shared home with one row per favourite / near / high-conviction setup for that scan date and NO duplicate `(scan_date, symbol, side, points_version, snapshot_id)` across refreshes (a revised same-day score appends once); (2) the setups status row reads `Points grade: N ranked rows logged, none graded yet (outcomes arrive 5 sessions after the scan).`; (3) five sessions later the same line reads `Points grade over N graded rows at 5 exchange sessions ...` with a tercile per side, and says `not enough per third yet` until each third holds 30; (4) `setup_points_weights.json` holds four multipliers, all `1.0` with a `kept at 1.0` reason until a part's halves reach 30 each; (5) the `...` menu's `Points: learned weights` is OFF and the Points tooltip says so. | Trader, next restart + 5 sessions |
| 90 | **The setups table ranks by points and hides nothing (trader, 2026-09-08)** - the next desk restart: the Master AVWAP setups table has a `Points` column at its right edge with a signed total on every row and a four-part tooltip; the **Points** checkbox on the control strip is OFF; ticking it re-orders the favourite / near / high-conviction rows by the total (highest first) with every other row after them, the status counts unchanged; unticking restores today's order exactly; the choice survives a restart. | Trader, next restart |
| 89 | **The Strength window is one flat page (trader, 2026-09-07)** - the next desk restart: the Strength window on the Trading Desk is ONE column with ONE scrollbar on its right - Strength (Focus + field), then Auto RS/RW Board, then the environment title with Copy RS / Copy RW and *vs SPY / vs Sector / vs Industry* one under another, then M5 Strength Board (TC2000) with Longs over Shorts; no arrow headers, nothing to expand, no box inside it with its own up/down bar (a table past 30 rows may scroll itself; the Strength tables may scroll sideways); the charts no narrower than before. | 2026-09-07 Strength page entry |
| 88 | **The desk opens without paying for eight pages nobody asked for (G7)** - the next desk restart, at the trader's own 3456 x 2160, plus ONE windowed bench run while the trader is away (this run doubles as gate #81's on-screen check). On the desk: (1) the desk WINDOW appears and is clickable sooner than it did - Research's nine children are built but only the open tab reads; (2) click **Research** and it lands on **Results** with no wait, then click **Setup Tracker** - the tables fill in a moment LATER and the window stays clickable the whole time (the read is on a worker now); (3) click **Day Trade Tracker** - it takes about half a second to appear the FIRST time and instantly after, which is the cost that used to sit in startup; (4) step the Setup Tracker's **Min closed** spinbox up and down five times - the page keeps up and the tables do not flash; (5) open a **Market Journal** entry that HAS a capture - the four charts appear, and an entry with none shows the note and no empty axes; (6) nothing anywhere shows a number, a sort or a row it did not show before. The bench run: `scripts\ui\desk_bench.py --data-dir <staged copy> --platform windows --sizes 3456x2160 --repeat 3 --compare <the G7 after-JSON>` with the desk DOWN, and its `research.construct` / `setup_tracker.refresh` / `market_journal.construct` within noise of the offscreen after-table in `docs/GUI_FLUIDITY_MEASUREMENT_RUNBOOK.md` section 7. **Known and expected, not a failure:** `research.tab.Day Trade Tracker` is SLOWER than before by design - its read is still synchronous and G7 moved only WHEN it runs | 2026-09-07 G7 entry |
| 87 | **Research opens on Results, the four selections show four different populations, and "not enough evidence" reads as a sentence (G5)** - the trader's first read after the restart. (1) Clicking **Research** lands on a tab called **Results**, not Market Prep, and the eight old tabs are behind it in the order they had. (2) The three control rows read **Bot setups | My trades**, **Swing | Day trading**, **Recent 20 sessions | All history | Custom...**, with Bot / Swing / Recent already chosen; the two date fields appear ONLY on Custom. (3) Clicking through all four selections gives four DIFFERENT readouts and **no family name appears under both Bot x Swing and Bot x Day** - they measure different things and are never pooled; Bot x Swing shows TWO labelled sections (closed R on the representative exit, then favorable direction in percent) and never one merged list. (4) The **Not enough evidence** card reads as a sentence per row - "below the evidence floor - 11 graded of 11 against a floor of 30", "too concentrated to lead - one name is 0.81 of the sample", "not measured yet - 44 pending" - and never as a blank or a zero. (5) Change any control and the detail pane on the right goes away; click a row, press nothing, and a refresh brings the SAME row back with the same numbers. (6) The freshness line names the snapshot's first eight characters, and they are **the same eight** the Working-lately strip and the Setup Tracker banner print - one reading, four surfaces. **What a PASS looks like on day one:** until the first post-restart close-slot tracker write, the swing sections may read `no_evidence` with the machine's own reason, and every band may say "nothing to show" - that is the snapshot being honest, not the page being broken. My trades will say **"no confirmed tags yet - nothing here names a setup"** wherever the trader has not confirmed a tag, and `n with R` will be 0 of N until planned risk is recorded. Nothing on this page may change a number that appears anywhere else **Also read on that first pass (the fix round):** the three window buttons on **Bot setups** are GREYED OUT and hovering one says the snapshot owns its own window, while the line under the controls reads *The snapshot owns this window: 20 sessions ending <session>* and never a date range; switch to **My trades**, the buttons come alive, and **Custom...** with a narrow range really does drop the trades outside it (the count changes). The freshness line names each source with a ROW COUNT (`swing_trade_r <- 10 row(s)`) and the word `None` appears nowhere on the page. A band card reads `3 of 27 shown`, never `27 shown` over three lines | 2026-09-07 G5 entry |
| 86 | **The Setup Tracker's explanation never outlives its context (G4b)** - on the next desk restart, on the Research tab's **Setup Tracker**: click a **Current Picks** row so the right-hand pane prices it (stop, 1R, two targets), then switch to **Playbooks** - the pane is GONE, not an empty box and not the previous plan. Come back to Current Picks and press **Refresh Tracker**: the pane stays DOWN, because a pane the trader closed by moving tabs is never reopened by a re-read. Then click the row again and press Refresh Tracker: the pane either shows that same symbol with the REFRESHED numbers (read the tier and the last close against the row in the table beside it - they must agree) or disappears if the re-read dropped the row. Repeat both on **Setup Types**, where the pane must come back on the row's OWN zone and bucket, not a same-family twin in another zone. **Display only**: the fourteen tables, their sort order, their columns and every number in them are unchanged by this packet, so a changed count or a changed order on that screen is a defect, not this gate passing. Gate #80 covers the Day-trade Tracker's pane; this is the other half | 2026-09-07 G4b entry |
| 85 | **Four tables read whole at the trader's own width (G2b)** - the next desk restart, at 3456 px. (1) **Setup Tracker ▸ Human Picks**: the ten measurement columns sit BESIDE a `Cohort` column no wider than 260 px, every one of them on screen without a horizontal scroll, and the width past `Delta %` is EMPTY - a stretched `Cohort` or a stretched `Delta %` is this gate failing. Hovering a clipped cohort shows the whole value. (2) **An EMPTY Controls tab** (and Studies, if its export is also absent): `Family` takes the slack and `Win % (low)` does NOT - the empty render measures headers, which is how the numeric column won before. (3) **Playbooks**: `Exit Plan` reads in full and `Recent Samples` is the column that elides, in the MIDDLE. Catch Rate is the same shape the other way round: `Missed Samples` reads, `Caught Samples` elides. (4) **The Desk Setups table at FULL width**: symbol, side, bucket and key level all read unclipped, `Setup Tags` is the column that grew, and a long key level elides in the MIDDLE (`$412.50…08-27`, never `$412.50…`) **while its row still paints as a setups row** - alternating background, the favorite tint, the LONG/SHORT and bucket chips, the selection fill. A `Key Level` cell drawn in plain text on an unstyled row is the specific regression this packet's delegate exists to avoid. Then press **F9** (or whatever switches the compact profile back): the compact setups table is byte-identical to before - same widths, same hidden columns, `Exp R` still beside the level. **Nothing here is a number**: no count, sort order, window or export on any of these tables may read differently from before, and the AWAY Recap's `Line`, `Trigger` and `Cell / held x ran` columns keep their full value in the tooltip | 2026-09-07 G2b entry || 84 | **The tracker's three defaults are live and the record says so (ST7)** - after the first persisted tracker write on the merged branch: (1) `trading_bot.log` carries `Setup tracker policies: selection=first_actionable_v2 execution=gap_aware_v2 levels=prior_session_v2` - an ABSENT line is a fail of the same weight as a line naming the v1 policies. (2) Every record in `master_avwap_setup_tracker.json` carries `execution_convention` = `gap_aware_v2` and `level_knowledge` = `prior_session_v2`, and every row of `master_avwap_setup_type_recent_stats.csv` carries `selection_policy` = `first_actionable_v2` - a row still reading `closed_first_v1` after that write is the defect this gate exists to catch (it is the exact inverse of gate #78's clause, which was written while the flip was still owed). (3) `n_pending` is NON-ZERO on at least one recent family row, and **no row whose `representative_status` is `pending` is graded** - on the 2026-09-03 mirror 271 of 2,712 episodes were. (4) The Setup Types and recent tables re-rank, and the order is expected to be the ST4 comparison's: 2,249 -> 2,712 episodes and 61.6% -> 72.0% favorable. **Expect `n_pending` around 902, not ST4's quoted 915**: the pre-ST4 bridge is keyed on the `representative_status` key being ABSENT, and 48 live-vintage records carry it PRESENT and EMPTY (measured, no representative scenario), which reads UNMEASURED rather than pending - 13 episodes, so 902 + 13. **History IS restated by this write, by decision** (0019) - that is not a failure to report. (5) The first D1 scan after the write still writes NONZERO `recent_tracker_score_delta` / `setup_type_score_delta`. The reproducible figures, measured on a COPY of the 2026-09-06 snapshot: **32 recent family rows, 1,688 of 2,195 episodes closed, 14 nonzero recent deltas and 74 nonzero `setup_type` deltas**. With the bridge reverted the same copy gives 32 rows but **0 closed of 2,195 and 7 nonzero recent deltas** (the `setup_type` 74 is unaffected - that builder takes no policy), so this clause fails on a HALVING, not only on a collapse | 2026-09-07 ST7 entry |
| 83 | **One reading, four surfaces, and a switch that only reorders (ST6)** - first DESK session after merge. (1) The **Working lately** strip at the TOP of the M5 alerts column prints a line for **all three kinds** - none may be silently absent - ending `as of <the last completed session>` and one `observational leader among K cells` PER KIND; the word *proven* appears nowhere on it. (2) `%LOCALAPPDATA%\TradingBotV3\working_lately\snapshot_latest.json` exists, is **under 48 KB**, and its `snapshot_id[:8]` is the SAME eight characters the Setup Tracker banner prints, with the words `panel read` ABSENT from that banner (their presence means the service never reached the page). (3) Clicking the strip lands on the Setup Tracker tab, where the Summary card and the banner name the SAME family. (4) The switch ON reorders the M5 list with the same row count and the same x N badges, and **OFF restores today's arrival order exactly** - toggle it twice on the running desk, which is the failure the re-review found. (5) `leader_change_events.jsonl` gains at most one line per kind per session, every `ts` carrying a market-local offset, and a desk RESTART adds none. **What a PASS looks like on day one, stated so it is not read as a defect:** `swing_trade_r` will read `no evidence - N cell(s) carry no measurable statistic` until the first post-merge CLOSE-SLOT tracker write, because today's live `master_avwap_setup_type_recent_stats.csv` predates ST2's integer columns; the swing verdict on the first snapshot that DOES have them will read `awaiting persistence (1 of 2)` rather than naming a leader, because the first build has no predecessor and a named leader on day one would mean the rule did not run; and a `daytrade_held_run` cell refused as `concentrated` is the rule working. A day-trade **`no clear leader`** is also a PASS: on the live data today `lrsi_cross_50` (1.217) and `regime_pause_rs` (1.209) sit 0.008 apart, well inside the declared `LEADER_MARGIN_HELD_RUN_R` of 0.10, and printing the winner of that would be printing the winner of a coin flip. **The day-trade line's bound must be BELOW its own statistic** (`held x ran 1.22 (>= 0.98, n=...)`) - the re-review found it printing `held x ran 1.21 (>= 2.070)` | 2026-09-06 ST6 entry |
| 82 | **Weekend Prep opens at 2160, Focus Review shows one table, and the pane never lags the read (G1)** - on the desk, at the trader's own screen: the Weekend Prep tab opens with **no table overlapping another and nothing drawn past the bottom edge**, Focus Review shows the nine view buttons with **Week's picks** checked and exactly ONE table under them, the horizon combo appears only when Vetoes or Likes is chosen, clicking a row fills the pane beside it with every column of that row and the full reason text, and switching view empties the pane. Then: **`Refresh everything` still fills all nine views in one pass** - click it, wait for the page, then click through all nine buttons and find every one of them populated without a second refresh. **The pane's own check**: with a row selected on Vetoes, press `Refresh everything` and read the pane against the row beside it - **every value must agree**; then change the Horizon and read them again. A pane that still shows the old numbers under the same row number is this gate failing. Clicking the button of the view **already** shown must change nothing - the selected row stays selected and the pane keeps its text. The other five Weekend pages still show ten rows. **Nothing here is a number**: no count, sort or window on this page may read differently from before | 2026-09-06 G1 entry, fix round 2026-09-07 |
| 81 | **The bench's numbers hold on the trader's real screen (G0)** - the 2026-09-06 baseline was taken OFFSCREEN, and offscreen Qt has no DPI, no compositor and no real paint. One run of `scripts\ui\desk_bench.py --platform windows --size 3456x2160 --repeat 3` against a staged copy, at a moment the trader is not using the desk (it opens real windows), must (1) record the `QScreen` geometry and `devicePixelRatio` in the JSON, (2) still flag `weekend_prep` and `weekend_prep.focus_review` as `overflow`, and (3) **keep `research.construct` and `setup_tracker.refresh` as the two SLOWEST ops by sync p95, in that order**. Clause 3 first read "the two ops over 250 ms"; that line is noisy and is NOT the check - the branch's own JSON already crossed it with three ops at the target size and the reviewer's re-run with four. A windowed run that puts a different op at the top is what would invalidate the yardstick. A `--compare` against `desk_bench_baseline_2026-09-06.json` is the readout. **If the windowed run disagrees on WHICH ops are slow, the offscreen baseline is the wrong yardstick for G7 and must be retaken windowed** | 2026-09-06 G0 entry |
| 80 | **An explanation never outlives its context (G4)** - on the next desk restart, on the Research tab's Day Trade Tracker: click a **Bounce Types** row so the right-hand pane explains it, switch to **Combos**, and the pane is GONE (not an empty box, not the previous explanation); (The My Decisions sub-tabs carry no clickable explanation, so nothing there can be checked - the reviewer's finding; the sub-tab clear is defence only.) Then with a pane open on a segment, press **Re-aggregate Outcomes**: the pane either shows that same segment with the REFRESHED numbers (compare Avg R against the row in the table beside it - they must agree) or disappears if the re-aggregation dropped the segment. **Display only**: the tables, their sort order and every number in them are unchanged by this packet, so a changed count or a changed order on that screen is a defect, not this gate passing. `setup_tracker_panel.py`'s detail pane is NOT in this gate - it is packet G4b | 2026-09-06 G4 entry |
| 79 | **Personal evidence counts once and refuses to name a best setup (ST5)** - the next nightly `preference_trade_outcomes` slot writes a report whose ledger reason carries `n_trades_matched` beside `n_statements_matched` and names the window as `10 sessions`; on the current store expect **10 trades behind 13 matched statements** over a 45-day read (the pair is what makes the duplicate visible - equal numbers on a quiet window are not a failure, a MISSING `n_trades_matched` is). Then on the desk: Weekend Prep's "Tag this week" lists the **26 provisional rows** with `Week` reading `backlog` on the ones older than the current week, the "Missing planned risk" table lists closed trades with no plan (expect ~165, newest first) and one row opens that trade in the Journal's Trades tab; the coverage line under the verdict card reads `Confirmed tags: 1 of 172 closed or partly closed trades. Provisional awaiting review: 26. Planned risk recorded: 0 of 172.` and the Analytics tab shows that sentence beside `1 confirmed setup tag - under the n=30 floor (26 provisional awaiting review) - no personal setup can be called best.` **The Analytics tab's four population numbers must read 165 complete / 7 partly closed / 32 open summing to 204, with `uncertain` a cross-cutting 120 (84 / 7 / 29) carrying NO pooled money** - a `partly_closed` of 0 or an `uncertain` that sums beside the other three is the review's blocker back again. **Nothing here is a scoring change**: the champion's scoring population, every detector and every alert are untouched by construction | 2026-09-06 ST5 entry |
| 78 | **SUPERSEDED by #84 on 2026-09-07 for its policy clauses; its scoring-path clause LIVES ON as #84 (5).** ST4 built `first_actionable_v2` as opt-in evidence, so this gate demanded `selection_policy = closed_first_v1` on EVERY recent family row and called `first_actionable_v2` in an export the defect to catch. The trader took the decision that evening and packet ST7 made v2 the DEFAULT, so that clause is now the INVERSE of the truth and would record a false FAIL on Tuesday - **do not read it as an open gate**. What still stands and is read under #84: (a) the five ST4 columns sit at the END of the shipped header after ST2's, with an empty `as_of_session`; (b) the scoring path is intact - the first D1 scan after merge leaves `recent_tracker_score_delta` / `setup_type_score_delta` NONZERO, the reproducible figures being **32 recent family rows and 74 nonzero `setup_type` score deltas over 11,372 projections** on the 2026-09-06 snapshot; (c) `n_excluded`, `excluded_reasons` and `fully_excluded_groups` are present and never zero across all 32 cells; (d) the artifact stays readable at `%LOCALAPPDATA%\TradingBotV3\diagnostics\st4_selection_compare\selection_comparison_20260906T160305.json/.csv` with its README block, headline 32 cells / 27 changed / 26 rank moves, v1 2,249 episodes / 61.6% / Wilson 0.593 against v2 2,712 / 915 pending / 72.0% / Wilson 0.699. **A replay is still blind to a COMPACTED record** (0 of 141,324 scenarios in this window, 99,562 of 206,341 across history), so an earlier `as_of_session` must show `undatable_exit_in_population=N` rather than a swollen pending count | 2026-09-06 ST4 entry; superseded 2026-09-07 ST7 entry |
| 77 | **Its artifact clause STANDS; its no-stamp clause is SUPERSEDED by #84 on 2026-09-07.** (1) STILL OWED and unchanged: the three JSON/CSV pairs in `%LOCALAPPDATA%\TradingBotV3\diagnostics\st3_execution_compare\` (`both__comparison_20260906T110731.*`, `exec_only__…110814.*`, `levels_only__…110855.*`) exist and are READABLE by the lead, with the headline copied into the 2026-09-06 ST3 entry: **472 of 794 changed** (`n_setups` 794 is the artifact's OWN denominator - 800 were offered and 6 carried no tradeable scenario; `--limit` is never the denominator), raw expectancy -0.0981 -> -0.1192, 43 of 50 groups moved rank, plus the two single-axis runs. (2) **SUPERSEDED.** This clause demanded that the next persisted tracker write carry NO `execution_convention` and NO `level_knowledge` on any record and no `fill_basis` on any event, because ST3 shipped the repair opt-in. Packet ST7 (2026-09-07, decision 0019) made `gap_aware_v2` / `prior_session_v2` the DEFAULT and made both stamps UNCONDITIONAL, so a record with neither key is now the defect - **read #84 (2) instead; this clause would record a false FAIL.** The trader's decision it deferred was taken on 2026-09-06 | 2026-09-06 ST3 entry; superseded 2026-09-07 ST7 entry |
| 76 | **Real counts on the desk, and one honest leader (ST2)** - after the next persisted tracker write: (1) `master_avwap_setup_type_recent_stats.csv` and `master_avwap_setup_type_stats.csv` both carry `n_wins` / `n_losses` FILLED (not blank), and on the recent file `n_wins + n_losses + n_flats + n_unmeasured + n_pending == n_episodes` on every row, with `n_episodes == tracked_setups`. (2) The recent-types table shows **Win % (unweighted)** and **Win % (recency-weighted)** as two columns whose values DIFFER on at least one family - if every pair is equal the weighting had no bite that day, which is information, not a pass. (3) The Setup Types tab leads with **Win %**, and its population sentence names `trade_r_representative_exit`, states its window (`ALL HISTORY`) and ends with the expired clause - **`N expired unmeasured, excluded` when N > 0 and the literal `0 expired unmeasured` when none, never an empty tail** (M3's gate #72 clause 5 reads the first form). (4) The banner prints one of the four verdict states and, when it prints a leader, names the SAME family the recent table lists first by bound (pins are novelty badges and do not count); when it prints `discovery only`, the word "Leader" is absent from that line. (5) No study family is ever named as the leader, and the exclusion count is shown. (6) **The Summary card above the banner agrees with it - the same family AND the same verdict state**, on a fresh day and on a stale one (they are computed once per page, so a disagreement is a defect, not a race); its "Setup types working" block shows BOTH sides, and the Best Type Edge tile reads the biggest `score_delta` (live base `SHORT +23`), not the table's first row. (7) `master_avwap_setup_short_horizon.csv` carries `n_wins` / `n_losses` / `latest_measured_session` filled, with **`n_wins + n_losses + n_flats == samples_2d` and `samples_2d + n_unmeasured == tracked_setups`** on every row, and `latest_measured_session` at or near the last completed session rather than 40+ sessions back - it is the MEASURED bar's date, not the entry. (8) The 2-session block's label names its verdict state (`Short-term (1-2d), leader` when there is one) and the words "discovery" and "carries no session" are absent from a leader line. **`not enough 2-session samples yet` appears ONLY when the block truly has no rows** - the live state is 12 eligible families 0.001 apart, which must render as `Short-term (1-2d), no clear leader`. (9) The swing line says **entry-dated** and the 2-session line says **measured**; neither claims the other's clock | 2026-09-06 ST2 entry |
| 75 | **Each outcome carries its own clock (ST1)** - after the next persisted tracker write (Tuesday 2026-09-08 13:00 PT): (1) `master_avwap_session_horizon_outcomes.csv` exists beside `master_avwap_tier_outcomes.csv`, with `sessions_spanned == horizon_sessions` on EVERY measured row and a populated `unmeasured_reason` on every other one (expect `target_session_not_complete` on the newest scan dates and `no_bar_for_target_session` for symbols the scan held no frame for). (2) the v1 counts logged by the tier tracker on the same pass are UNCHANGED in shape. (3) The tier outcomes file's new `outcome_kind` column reads `favorable_direction_scanrow_v1` on every row, and the Master AVWAP setups table's header reads **Family favorable %** with a coverage line beside the status bar. (4) `setup_docs.family_record_coverage_line()` and `autopilot_core.swing_family_record_line()`, called in the same minute, print lines that are IDENTICAL TO EACH OTHER, and the tier performance export's population agrees with them on the stale rule. **Compare them to each other, never to a number written down here**: the window moves with the calendar, so the triple changes every session (`2587 / 0 / 16971` at `end=2026-09-03`, `2462 / 0 / 17096` on today's default window - both correct, and either would fail a stored-string check). (5) **`trading_bot.log` carries EXACTLY ONE line for this export**, and it is `Session-horizon outcomes exported N row(s), M measured, C same-session scan row(s) collapsed, D true duplicate(s) dropped` - the two counts SEPARATE. Expect D small (300 over the whole live history, 75 ids at four horizons) and C large (91,880); a four-figure D means a re-scan is being reported as a duplicate again. On a 30-session window over a history of this size, expect roughly 90,000 rows and ~25 MB. A run where the export FAILED logs `Session-horizon outcome export failed` instead and the run result carries `session_horizon_export_error` with NO counts - an absent count and a zero are different claims. **Nothing here promotes**: `POLICY_SESSION_V2` still has no production caller, and a decision to read v2 is the trader's | 2026-09-06 ST1 entry |
| 74 | **The control and study populations are shown, and the April framework is read (M5)** - restated after the reviewer round so it can PASS (the first wording demanded an equal `n` the framework does not produce). After the next persisted tracker write: (1) `master_avwap_control_discovery.csv` and `master_avwap_study_discovery.csv` exist, the control file carrying its three cohort rows plus one family row per (side, family), in an `all` block AND a `lately` block, every row stamped `population_setups`. (2) The **Controls** tab's sentence reads `N graded episodes from the M control setups the scan REJECTED` with **N < M** and M near 401 (Studies likewise, M near 3,992) - the two numbers are different things and a tab printing one of them twice is the defect this clause replaces. (3) Both tables sort by the Wilson lower bound, not the raw rate. (4) The **Exit frameworks** tab shows `comparison_apr2026` rows beside `baseline` ones, grouped by side and bucket with the baseline above its twin, Experimental reading True. (5) **The denominators reconcile, which is the real check**: on a (side, bucket) where the template has no `blocked_stop_rules`, the comparison's `n` EQUALS the baseline's and `Filtered` is 0; on SHORT / near_favorite_zone for `exp_full_band2_hard_stop_125r_no_sma50_short_nearfav`, `n + Filtered` equals the baseline's `n` (the reviewer measured 585 + 98 = 683 on the live tracker). A smaller `n` with a matching `Filtered` is the experiment working; a smaller `n` with `Filtered` 0 is a defect | 2026-09-05 M5 entry |
| 73 | **Both band families measured, side by side (M4)** - after the next nightly build and a forced `recompute-outcomes --apply` on the trader's go: `band-coverage --compare swing_house_v1 swing_house_variant_v1 --month 2026-09` prints both recipes on the SAME occurrences with the Wilson lower bounds and a `not_paired` count, and `feature_snapshot_daily` rows for that session carry BOTH band families (`avwape_*` and `avwap_variant_*` with `avwap_variant_formula_version` = `avwap_bands_oneoption_bb20_v1`). **Expect `not_paired` to be non-zero on the first pass and to shrink**: only sessions rebuilt after M4 carry the challenger's bands, and an August occurrence has none. A twin row on an occurrence whose challenger bands are NULL is `plain_no_target` by design, not a defect. **Nothing may be read for a verdict** before the declared 20 forward sessions counted from the first session carrying both families - T4's criteria decide | 2026-09-05 M4 entry |
| 72 | **The tracker keeps up and says how old it is (M3)** - the next 13:00 PT close slot, checked against REPRODUCIBLE numbers rather than the audit's prose (reviewer, 2026-09-05: the first wording was unsatisfiable). (1) `trading_bot.log` carries `Setup tracker purity: pin=yahoo ... refused=False` and then the tracker WRITE, with no "refresh skipped" line. (2) The payload's `saved_by` reads `close_slot` and its `saved_at` is market-local with an offset. (3) The three stats CSVs carry that same `tracker_saved_at`, and the Setup Tracker's status line shows BOTH clocks in ONE zone - `Tracker as of ... (close_slot); scan factors as of ...`, the two offsets equal. (4) `trading_bot.log` carries the literal token **`n_expired_unmeasured=`**; on the 2026-09-04 mirror the reproducible counts are **setups 32 `no_replay_stale_sessions` + 13 `no_baseline_scenarios` = 45, study 7, control 0, 52 total**, so expect ~52 on a tracker of that vintage - not the audit's "37 + 41", which counted a different thing. (5) The Setup Types tab says `N expired unmeasured, excluded` with N matching the summed CSV column (Current Picks deliberately says nothing - different population). (6) Family win rates unchanged to the cent where those setups contributed only a denominator, and the champion's scoring population unchanged by construction. **`python scripts/tracker_store.py verify` must still print `"ok": true`** - the mirror gained two header keys, so this write is also gate #57's next observation, and **the FIRST save after merge rewrites every record's content hash** (`saved_at`/`saved_by` join the header, and `last_replayed_session` / `expiry_reason` / `stale_sessions` join the records), so a large `written` count on that one save is expected and is not a parity failure | 2026-09-05 M3 entry |
| 71 | **`unresolved` means UNMEASURED (M2)** - the first after-close sweep after merge logs the four-way split in `trading_bot.log` - `Outcome sweep finalized N pending trade(s): measured_eod A, swept_measured B, unmeasured C (of which expired D); already final in the CSV: E (counted in the total, not in the split); ...` - where **A + B + C + E == N**, because `expired` is a SUBSET of `unmeasured` and an already-final row's status was written by an earlier attempt this run never saw; the Daytrade Tracker's status line shows the coverage sentence after Q1's window sentence; and `outcome_semantics.terminal_kind` over the live file for the last 20 sessions reports `measured_eod + measured_swept` at or above the 7,427 measured at merge (3,820 + 3,607 of 8,161 events) with `unmeasured` near 644. **The packet's gate text said 7,600 and about 495**; those were read a day earlier over a window one session back, and the branch's own streamed read is the number to compare against | 2026-09-05 M2 entry |
| 70 | **The band challenger measures (M1)** - after the next persisted tracker write: `master_avwap_band_variant_stats.csv` shows `n_variant > 0` on the rows whose records have >= 20 closes before the anchor, the four `_variant` columns fill, and the Setup Tracker's Band variant view reads `Measured N of M setups` rather than `Measured 0 of M`. **T4's >= 20 sessions of forward accrual start that day**, not 2026-08-26 - nothing accrued before it | 2026-09-05 M1 entry |
| 67 | **The narration comes back, bounded (N3)** - the next overnight `setup_research` row reads `narrated K of N eligible cell(s)` (expect K about 64, N about 619) with a `.narration.json` beside ONE pack for the date and the pack `.md` carrying the `## Narration` coverage line; a row still reading `narration absent` is the news and its message now names the HEAD's size. Replaces gate #40's narration clause | 2026-09-05 N1-N3 entry |
| 66 | **The nightly synthesis completes (N2)** - that night's `ai_summary_*.json` has `map_reduce.synthesized: true` (or `synthesis_retry: "shorter"` and a WEIGHED document, not the 12-row pile), `synthesis_stop_reason` present and `""`, and the ledger reason no longer ends "NOT synthesized"; two consecutive synthesized nights close it. Watch `trading_bot.log` for a new prompt-truncation refusal (the reduce call now asks for 8,000 output tokens against 65,536); an EMPTY `slices_retried` is expected | 2026-09-05 N1-N3 entry |
| 65 | **The sidecar completion finishes a row (N1)** - the first nightly run after merge: the `sidecar_completion` ledger line reads `completed 1 of 1` (or names a reason that is NOT `research_store_unreachable`), and `b9344eb372284d7f98f6083b50178e0b.completed.json` exists under `C:\TradingBotData\trader_annotation_bars\` with ~78 bars whose `dt` strings all carry an offset. `lake_read_failed: <Exc>` is a PASS of the naming half and a FAIL of the read. Clears gate #39's blocker | 2026-09-05 N1-N3 entry |
| 64 | Q5 scorecard off the Qt thread: one session past the close with no `autopilot_service.py` stall over 1,000 ms, the scorecard lines in the log, one CSV row per pick group, `picks_scored_at` set and never `picks_scoring_failed_at` | 2026-09-04 Q5 entry |
| 63 | Q4 overnight stages: stage half MET 2026-09-05 (every deterministic row before `ai_summary`, `entry_index.json` beside the packs, `refused: audit not recorded`); **audit half MET 2026-09-06** (`audit_recorded: true`, `gate_met: true` over four verified packs). Remaining: the first `journal_enrichment` ledger row that is not `refused`, on the next session night (Tuesday 2026-09-08; Monday is Labor Day - read Wednesday morning) | 2026-09-04 Q4 entry; 2026-09-06 entry |
| 62 | Q3 grounding on a real night: `ai_morning_brief.txt` opens `Analyzed A of N. Membership-only B. Failed C.` with A + B + C == N, every membership-only block leads with `membership only`, every drop named by one of the three detail strings (`position claim without a position source`, `position claim in the executive summary`, `numeric claim without a resolvable metric_ref`); a collapse in the analyzed count means over-drop. The position rule the code enforces is `ai_summary.POSITION_SOURCE_IDS = {"journal.trades_and_reviews"}` - a position claim stands only on the journal, never on a watchlist or Focus membership (the fix-round ruling; `tests/test_q3_ai_grounding.py` pins this sentence). Met 2026-09-05 (`Analyzed 53 of 108. Membership-only 55. Failed 0.`) | 2026-09-04 Q3 entry |
| 61 | **MET 01:26 PT 2026-09-05**: `band-coverage` 105 of 105 September occurrences with all three bands, the observed / reconstructed / legacy split PRINTED, `path_kind` populated | 2026-09-05 overnight entry |
| 60 | Q1 measured held: the Daytrade Tracker's Measured column populated, the status line naming the window and its missing sessions, `held_run_score.load_episodes()` reporting the four counts on the live file | 2026-09-04 Q1 entry |
| 59 | **MET 01:24 PT 2026-09-05**: 3,678 anchors bridged, `anchor_instance` 3,681 rows, `swing_house_v1` graded 376 / 291 where 0 / 257 stood | 2026-09-05 overnight entry |
| 58 | T1 + T2 capture and board rules, one DESK session: a veto double-click retires with no box and ONE row; a claim double-click with nothing typed advances with ONE row; a quick like leaves the chart up; "✕ Not today" still opens the box; board clicks leave "queue clear"; TC2000 parity names reach M5 Focus after a refresh, and a "Not today" or a Focus-list removal stays gone with `longs.txt` not regaining it | 2026-09-04 T1 and T2 entries |
| 57 | Tracker mirror parity (decision 0017): five consecutive live saves with `python scripts/tracker_store.py verify` printing `"ok": true` and the `Setup tracker mirrored` log line; then step 2 moves the first reader | 2026-09-04 06:00 entry |
| 56 | **MET IN FULL 07:53 PT 2026-09-04**: lake dedupe, month rebuilds and the forced outcome recompute (134,502 rows superseded) | 2026-09-04 06:00 entry |
| 55 | Tee quiet (S1) and gauge names threads (S3): `thread_cpu.jsonl` shows `warehouse-m5-tee` under 5% of a core after the close, no `Hot thread:` warning for it, `tee_high_water.json` beside the spool, one session of rows per segment | 2026-09-03 evening entry |
| 54 | One chart on the desk: in workspace mode every board, setups-row, RS Window, Industry Board and Watchlists click lands on the centre chart with NO popup; tabs mode brings the popup back | 2026-09-03 ticker-click entry |
| 53 | Desk clickable through a build (F1): a below-normal child `python.exe` does the build, the owned-child count includes it, the next scan is not refused, the desk stays clickable | 2026-09-03 F1 entry |
| 52 | Surfaces say what they measure (R4 Part B): the Family Win % cell, the Record line, the Tier column and the "Verdict (edge score)" header, each on the desk | 2026-09-03 R4 Part B entry |
| 51 | Corrected numbers on the desk (R4 Part A): RVOL populated across a half day, Held columns filled on the four answerable tabs and BLANK on the four Swing tabs | 2026-09-03 R4 Part A fix-round entry |
| 50 | Headline statistics agree (V3): win rate first on every swing surface, Held x Ran first on every day-trade surface, sorts agree | archive: 2026-09-02 V3 entry |
| 49 | Weekend Prep in one click (V2 item 2): Refresh builds every step, the verdict card shows five to eight lines with an n each, "Tag this week" confirms | archive: 2026-09-02 V2 entry |
| 48 | Hidden surfaces (V2): Alerts, D1 Focus, Armed and Universe hidden and every rail hotkey still firing | archive: 2026-09-02 V2 entry |
| 47 | One box, one Enter (V2): a Market Journal entry written from the desk tab, filed against the right session | archive: 2026-09-02 V2 entry |
| 46 | Tagger runs itself (V2): one nightly run tags new trades and the Journal nav button shows the count next morning | archive: 2026-09-02 V2 entry |
| 45 | One window, two sections (V1): RS/RW opens ABOVE Strength and neither widens the column | archive: 2026-09-02 V1 entry |
| 44 | TC2000 parity (V1): the Strength section matches the trader's own TC2000 list for the top ten names on the same minute | archive: 2026-09-02 V1 entry |
| 43 | REFUSAL (P10 C): no after-like cell is read for a verdict before the declared 20-session window closes | archive: 2026-09-02 P10 entry |
| 42 | After-like grid collects (P10 C): `bronze_like_occurrence_link` rows and after-like outcome rows inside the reserve, ledger row `collecting` | archive: 2026-09-02 P10 entry |
| 41 | One like, one dislike from every screen (P10 A): a star, a rail like and a "Not today" each leave EXACTLY ONE annotation row with the right `surface` | archive: 2026-09-02 P10 entry |
| 40 | Narration fits (R3): one overnight `setup_research` run publishes exactly ONE pack for the date with a `.narration.json` beside it | narration clause REPLACED by #67 (N3); one-pack half met 2026-09-05; archive: 2026-09-02 R3 entry |
| 39 | Quick like (P9): one swing and one M5 quick like reach `trader_annotations.jsonl` with `like_mode` quick, nothing in Focus, and the M5 one's intraday columns are numbers next morning | blocker cleared by N1 (gate #65); archive: 2026-09-02 P9 entry |
| 38 | Merged tree on the desk (R1, R2): stall watchdog ON and quiet on every new surface; the Setup Tracker's picks count after the first scan | archive: 2026-09-02 R1/R2 entries |
| 37 | First parameter grid (P8): one overnight run publishes rows for every declared cell inside the reserve, ledger row `collecting`, and no cell is read early | archive: 2026-09-02 P8 entry |
| 1 | ~~Frozen rebuild + selftest~~ MET AGAIN 2026-09-02 (74/74 frozen) | done |


### Long-owed live gates (Phases 0.5 to 0.13, 2026-08-27 to 2026-09-01)

Still owed and unchanged since they were written; nothing here was closed by moving it.

| # | Gate | Owed by |
|---|---|---|
| 36 | Tagged backlog (P6a): the trader confirms or edits at least ten of the 24 provisional tags | archive: 2026-09-01 P6a entry |
| 35 | Preference to trade (P6): a real import shows a `trader_capture` candidate; the nightly report lists likes traded / not traded | archive: 2026-09-01 P6 entry |
| 34 | Pass and rejection cohorts (P5): two real passes and one not-today reach both CSVs and the two Weekend Prep tables | archive: 2026-09-01 P5 entry |
| 33 | Swing variables (P4): the Attributes tab opens without a stall, `stale_horizon_observations_dropped` carries a real count, `expected_r_note` names its exit template | archive: 2026-09-01 P4 entry |
| 32 | Fact-pack truth (P3): the Markdown opens with the eligible block, `n_episodes` beside `n`, excluded families named, bucket coverage a real count | archive: 2026-09-01 P3 entry |
| 31 | P2 surfaces: the six named surfaces open and `ui_stalls.jsonl` charges nothing to them | archive: 2026-09-01 P2 entry |
| 30 | P1 grading loop: a `human_focus_swing_vetted` row, a same-day like merge, ONE pooled `compressed` cohort, `r_gaps` present | archive: 2026-09-01 P1 entry |
| 29 | P0 decisions: no LRSI line on the M5 bar, LRSI rows still in the outcomes CSV, no BANGER branch in the alert path | archive: 2026-09-01 P0 entry |
| 28 | HTF LRSI study (0.12 B): one overnight run publishing `htf_lrsi_*` rows with H2 `bar_derived` present and no stub in the input | archive: 2026-09-01 Phase 0.12 entry |
| 27 | Focus de-clutter (0.12 A): pullbacks only on the D1 feed, an armed extension still fires, an expired watch leaves a row, a faded pick restores with a fresh clock | archive: 2026-09-01 Phase 0.12 entry |
| 26 | Snappiness packet 3: the after-close TI replay finishes in minutes, a quiet-hours night with no Industry Board download, one drift session | archive: 2026-08-31 packet 3 entry |
| 25 | Snappiness packet 2: `bar_cache.py:75` and the GC lines quiet; a journal retag does not freeze the desk | archive: 2026-08-31 packet 2 entry |
| 24 | Snappiness packet 1: the four named stall sites quiet with the watchdog ON | archive: 2026-08-31 packet 1 entry |
| 23 | Theta premium (0.11): a percent-floored, support-first theta report with a `premium=` line on every sold put and DRAM `via thetalongs.txt` | archive: 2026-08-31 theta entry |
| 22 | Strength Board in the Desk: open the section, chart a row, add a name, judge the vertical stack | archive: 2026-08-31 Strength Board entry |
| 21 | Day-trade pass: the reasons and note reach `trader_annotations.jsonl`, the chart STAYS UP, an M5 pass carries its bars | archive: 2026-08-31 pass entry |
| 20 | Today's swing picks: the real list shows as THEIRS, the split drags and survives a restart, Paste / Copy work, a removal retracts | archive: 2026-08-31 swing picks entry |
| 19 | Desk lockup fix: a large staged batch drains without charging `focus_picks_panel.py` or `setup_delegate.py` | archive: 2026-08-31 lockup entry |
| 18 | Tax report: one desk run with the BoC rates booked | archive: 2026-08-28 R7 tax entry |
| 17 | File authority: one desk import where a shared day agrees and the sync keeps its times | archive: 2026-08-28 R7 authority entry |
| 16 | IBKR file import on the desk; the second account's mask resolves once Flex has named it | archive: 2026-08-28 R7 IBKR entry |
| 15 | Statement layering and "Check a statement..." against the live journal | archive: 2026-08-28 R7 layering entry |
| 14 | Questrade YTD statement import on the desk | archive: 2026-08-28 R7 statement entry |
| 13 | Journal auto-tagging: tag real trades, rename one, filter on it | archive: 2026-08-28 R7 tagging entry |
| 12 | Sliced summary overnight: 46 slices, a synthesized summary, briefs finishing in the window | archive: 2026-08-28 slices entry |
| 11 | Narrated summary + ticker briefs overnight at the raised context | archive: 2026-08-28 context entry |
| 10 | Narrated digest overnight without being forced | archive: 2026-08-28 narration entry |
| 9 | Feature-history exports: `output/scan-factors` and `output/tier-tracker` written again without `ParserError` | archive: 2026-08-28 corruption entry |
| 8 | Phase 0.8 live soak, the trader's to run | archive: 2026-08-26 fluidity entry |
| 7 | SOAK 1 on Phase 0.9 G-P2.3, not yet run | archive: 2026-08-27 Phase 0.9 entry |
| 6 | Market Journal: a Desk-tab note reaches the left-nav page | archive: 2026-08-27 evening entry |
| 5 | The four 2026-08-27 trader rules, one DESK session on a directional day | archive: 2026-08-27 morning entry |
| 4 | Group RS/RW tape: one DESK session with the four trader rules | archive: 2026-08-27 group tape entry |
| 3 | Desk memory: the first swing-scan slot without the 8-13 GB jump | archive: 2026-08-27 memory entry |
| 2 | Warehouse canary: one post-scan run verifying writes and bounded memory, then every bucket filled, then a fact pack against warehouse counts | archive: 2026-08-27 tracker entry |
