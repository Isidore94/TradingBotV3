# Changelog

One line per change, newest first. The commit message holds the detail. Keep
about 40 lines and delete the oldest. The full old changelog and inventory are in
`notes/CHANGELOG.md`, local only.

- 2026-09-25 Journal stop backfill P8 P1 (branch `claude/p8-p1-stop-backfill-2026-09-25`): `scripts/journal_stop_backfill.py [--apply] [--since D] [--trade-id ID]` fills an empty trade plan (stop, entry, risk) from the M5 alert registered that session before the fill (`backfill_m5_alert`) or the D1 detail plan of the latest scan before the entry (`backfill_d1_plan`), through a guarded `JournalStore.backfill_risk_fields`; a plan or risk_source already set is never touched and options are skipped. Dry run on a copy of the live journal: 28 of 125 stock trades filled (5 M5, 18 D1).
- 2026-09-25 P8 P2 per-trade inputs (branch `claude/p8-p2-mentor-stop-first-2026-09-25`): each Mentor trade block asks stop, target, setup, then thesis; a trade with no stop is listed first and brings the trade section onto any card, ahead of the budgeted questions (Save never needs a stop); the Journal Trades tab's "Confirm suggested setups" lists every needs_review/provisional trade with the Mentor's suggestion and why, and confirms the ticked rows on a worker (confirmed rows untouched).
- 2026-09-25 Night narration P8-P4 part 1 (branch `claude/p8-p4-night-narration-2026-09-25`): the day story's evidence cap drops 60,000 -> 30,000 chars and uncited pack parts (report card, congruence, skill, walk-away, forecast text, trades) are dropped in that order to fit, every id still citable; its ledger reason names the bytes and estimated tokens sent (live packs 44 KB -> 30 KB); the econ summary prompt says the brief's prose is the brief day's (both live rejections were a past 09-24 auction, correctly refused); a never-quoted theta pick is `dead` (`never_quoted`) once its expiry or 30 sessions pass, and the ledger says "N measured, P pending, D dead (never quoted), U unmeasured".
- 2026-09-25 Night AI plumbing P3/P4 part 2 (branch `claude/p8-p3p4-runner-2026-09-25`): a failed night `journal_import` is retried once at 07:00 PT (`run_ai_jobs.py --retry-journal-import`, second scheduled task, never beside the night run, own "morning retry" ledger row); Health's journal import line adds unresolved self-heal and position mismatches from the last run; the night budget protects `plan_review` and `improvement_ideas` ahead of `observation_tags`; the unread `note_vocabulary_audit` slot is deleted; every slot declares a goal and its ledger rows carry it (gate #258).
- 2026-09-25 Trade Mentor day question (branch `claude/mentor-plan-question-wording-2026-09-25`): "Followed the plan?" now reads "Today overall: did you stick to your trading plan?" and sits 12 px apart from the per-trade questions; new rows' journal text carries the new wording.
- 2026-09-25 Night AI fixes (branch `claude/night-ai-fixes-2026-09-25`): pytest caps -n to 2 from 22:00-02:00 PT and AGENTS.md bars builders then (09-24 low-memory bluescreen); improvement-idea citations are an enum of tonight's ids; market-story narration rejects an up/down claim no bar or note supports and bad inline ids; journal enrichment drops setup tags with no review row or note; setup-research narration keeps only statements quoting a fact-pack number (gate #257).
- 2026-09-25 Trade Mentor setup list (branch `claude/mentor-setup-list-every-trade-2026-09-25`): every trade whose setup is missing now gets the setup list and Confirm button, not only trades with a machine guess; with no guess the list opens on "- pick a setup -" and Confirm stays off until a name is chosen. Each trade now asks setup (list), thesis (optional), stop and target once: the catch-all note box, its "Fill missing fields with local AI" button and the duplicate setup text row are gone; Save this trade also confirms a setup picked by hand (an untouched guess is never filed).
- 2026-09-25 Reliability P2-11 a-e (branch `claude/p2-11-cleanup-2026-09-25`): 276 silent `except: pass` sites now log a reason through `swallowed.note_swallowed` (rate-limited, DEBUG for harmless cleanup) or name the unknown; ruff selects E7, E9, all F and B, with the code fixed (a dead duplicate `run_ai_summary` removed); Health adds Universe vs floor and IB connection rows (audit worker); the market-prep OpenAI key and ntfy token move to Windows Credential Manager via `keyring` (verified copy before the JSON is blanked); `master_avwap_lib.legacy` loads lazily (boot import 1.71 s -> 1.33 s) and the Focus chips' stylesheets became theme.qss variants (gates #253-#256).
- 2026-09-25 Journal owed items P2-10 (branch `claude/p2-10-journal-owed-2026-09-25`): made-up-entry trades stay listed but leave the Day Review, Weekend Prep and day-record P&L totals; the Mentor rule lane's size baseline is read on a worker; `journal_questrade_gaps.py --statement FILE [--apply]` fills FAILED Questrade days from a portal statement (gap days only, existing dup rules); setup evidence reads `CLOSED_PARTIAL`; `journal_feed`'s shared store has one lock-guarded owner (gates #248-#250).
- 2026-09-25 M5 setup key stamp P1-4 4a (branch `claude/m5-setup-key-stamp-2026-09-25`, shadow only): each new M5 alert gets the previous session's D1 setup key (the backfill's rule) in a new append-only sidecar `m5_setup_key_stamps.jsonl` keyed by event_id, written by one background worker after the outcome row is on disk; the outcome CSV is byte-identical with the hook on or off; the backfill joins the sidecar with `--m5-stamps` (gate #252).
- 2026-09-25 Swing scan P0-2 rest (branch `claude/p0-2-rest-2026-09-25`): the scan-factor leaderboard builds in about half the time with byte-identical output (2c; the 40-session window file was not built: the observations and tier-outcome files are all-history); compression calibration reads the tracker's SQLite mirror when fresh (2d(4)); the scan batches its Yahoo daily-bar requests 100 per download under the yfinance lock and logs one `[daily-bar cache]` hits/misses/refreshes line (2e); `--prune-copies` also lists the three `.damaged-20260905T200233` leftovers (gates #222-#225).
- 2026-09-25 Setup keys P1-4 4a-4d+4f (branch `claude/p1-4-setup-keys-2026-09-25`, shadow only): the scan stamps `perm_dist_<ma>_atr`, `perm_weekly_ema8_hold_weeks` and a permutation key on every `d1_features_history` row (appended columns; detector/scoring output identical), tracker events and warehouse occurrence tags carry the key; a scratch-only backfill writes `permutation_outcomes.parquet`; a ledger-registered search with a 20-session hold-out writes `permutation_report.json`, shown in Research -> Setup keys; a Saturday slot writes three cited sentences per family; five wider facets (gates #226-#229).
- 2026-09-25 Optimal entry P1-6 (branch `claude/p1-6-entry-plan-2026-09-25`, display only): M5 alert rows and Working now show each alert's entry state (valid / improved / gone / unknown, completed cached M5 bars, off the Qt thread); the full-width setups table adds Entry / Stop / TP1 / TP1 R / Shares from the D1 detail plan (stale inline) and a Timing chip from Pullback alerts (compact: in the Key Level hover); a Settings `risk_per_trade_dollars` sizes every plan (floor(risk / |entry - stop|), never an order); the journal trade detail shows the entry grade and MFE/MAE (gates #234-#237).
- 2026-09-25 Best names P1-5 (branch `claude/p1-5-best-now-2026-09-25`, display/ranking only): the AWAY digest top ten takes at most 3 per (family, side) then fills in rank order (Wilson, or points when the switch is on) and says so on the Ranked-on line; a setup-key label shows on digest lines and a full-width setups column when `d1_features.csv` carries `permutation_label`; a "Best right now" strip under "Working now" ranks D1+M5 names, M5 alerts (grade, live R) and Movers dip-strong names with entry/stop, diffed off the Qt thread on bar boundaries; the open sweep reads all of `universe_all.txt` with typed and Focus names first (gates #230-#233).
- 2026-09-25 Trading plan P1-7 (branch `claude/p1-7-trading-plan-2026-09-25`): `C:\TradingBotData\trading_plan.md` (six fixed headings; template made only when missing) is shown read-only on Day Review and Week Review, and every change is kept in `trading_plan_history/`; new stage-3 night slot `plan_review` gives up to three challenges, each citing one plan line and one evidence id, asked on the Mentor card (accept adds a dated Decisions line, reject keeps the reason, untouched ones expire in 7 days); the research pack carries the plan and the week's challenges; the recap's rule for tomorrow is copied under "What I am testing".
- 2026-09-25 Market analysis P2-8 (branch `claude/p2-8-market-analysis-2026-09-25`): 8a nightly breadth (A/D, % of universe_all above SMA20/50, `breadth_v1`) from the daily cache into `market_breadth.jsonl` via the read-grades slot; Market Prep names the morning read on three axes (SPY label, breadth, internals) and Day Review grades each leaning axis against SPY's next close in ATR (`market_axes_v1`, flat band `atr_0.25_v1`); 8b the regime chip adds display-only internals phrases ("vol bid + narrow tape"), omitted when missing or stale; 8c sidecar review stamps for the sector/industry maps (`python -m map_freshness stamp`) and Health rows that warn past 90 days or say "age unknown".
- 2026-09-25 Looking back (P2-9, branch `claude/p2-9-looking-back-2026-09-25`): Research > Results (Bot setups) shows a cumulative-R pick curve with n per population (swing picks by scan date from the tracker's episode R; M5 alerts as +1R/-1R brackets), never pooled, plus each grade and working-lately cell over the last window beside the window before it (same builders, split by date; `n<30` under the floor); built on the working-lately worker into `looking_back_latest.json`, grades and snapshot unchanged (golden-pinned) (gates #246-#247).
- 2026-09-24 Tracker readers to SQLite (P0-2 2d steps 1+3, branch `claude/p0-2d-tracker-sqlite-2026-09-24`): the close slot's tracker load and the Journal auto-tagger's context rows read the SQLite mirror instead of the 1.4 GB JSON, only when the mirror is stamped with the JSON file's exact size/mtime (else the JSON, reason logged); the mirror now keeps the JSON's encoding and key order (format 2, one full rewrite on the first save); the stamp is written only if the JSON still matches the stamp taken right after its own save (gate #221).
- 2026-09-24 Night AI chain P1-3 (branch `claude/p1-3-night-chain-2026-09-24`): a 30 s Ollama probe before the first model slot makes a dead-model night deterministic only (Health + phone digest say so); weeknights have a 150-min budget (`ai_night_budget_minutes`) that skips model slots by the trader's priority, ticker_briefs last; ticker briefs are Saturday-only for the week's traded/claimed/liked/swing-Focus/alerted names (cap 40) with a 7-day (symbol, week) cache; the summary reads at most 24 slices a run (`ai_summary_max_slices`); the nightly IBKR Flex import waits and retries "statement not ready" and Health shows the last journal import.
- 2026-09-24 Setup permutations 4a (branch `claude/p1-4a-setup-permutations-2026-09-24`): new pure `scripts/setup_permutations.py` - `facets_for_row(row, ctx)` gives a versioned permutation key (family x 37 registered facets over real `d1_features_history` columns, `unknown` on missing/NaN) and a short label; nothing stamps or reads it yet.
- 2026-09-24 Econ morning brief (branch `claude/econ-morning-brief-2026-09-24`): the Mentor opens once a session with "Today's news & econ" (night AI lines + today's timed events in ET/PT + the week) from the pasted brief, redrawn "from today's brief" after a paste; timed releases at or after 07:00 PT (10:00 ET) warn 30 min before and at the time (desk toast; AWAY/EVENING also phone); times come only from a fixed parser (the brief's NEXT 7 DAYS block, else its prose); new night slot `econ_brief`; the playbook reads "Bullish reversal"/"Bearish continuation" too.
- 2026-09-24 Movers fix (branch `claude/movers-ib-scanner-2026-09-24`): every in-desk yfinance download now waits on one lock (overlapping downloads hung Movers/Strength/Industry forever, so the board never published); Movers takes its candidates from the IB market scanner (gainers, losers, hot by volume) and Yahoo only fills names the bot lacks; the status line names the source and says when a refresh is stuck.
- 2026-09-24 D1 alert follow-ups: a Range breakout fire logs range_breakout_v1 and its range/ATR measure in `d1_event_fired`; an incoming trendline with a garbled break date is never frozen; the compact-desk round-trip test stops the movers poll so a late board update can't flip it.
- 2026-09-24 Veto follow-ups (same branch): a saved veto arms one alert by reason - Compressed -> Range breakout, SMA incoming -> new SMA break + 15EMA retest (sma_break_retest_v1, in the Line break group), Incoming trendline -> Trendline break + retest on the nearest incoming scan line, Too extended -> Pullback as before; the snapshot popup uses the same grouped D1 menu.
- 2026-09-24 D1 alert menu regroup (branch `claude/d1-alert-regroup-2026-09-24`): three groups - Pullback (Pullback (fast) moved from the M5 menu; new Pullback to D1 line), Breakout (new Range breakout: 20-day high/low out of a base <= 4x ATR14), Line break (new Line break; trendline break and retest) - old kinds and Any bounce leave the menu but still fire and show while armed; the Focus auto lane is unchanged.
- 2026-09-24 Swing scan P0-2 2a/2b/2f (branch `claude/p0-scan-publish-first-2026-09-24`): a failed scan keeps the child's last 40 stderr lines in autopilot.log and `scan_failures.jsonl`; `[run_master memory]` lines beside the timings; the close slot writes signals/reports/state before the setup tracker, and a tracker failure is a ledger row + digest OPERATIONS line + Health 'tracker last written' instead of a failed scan; `tracker_store.py --prune-copies [--yes]` deletes only the tracker `.bak` and `.damaged-20260905T200233` copies.
- 2026-09-24 Universe write floor (P0-1, branch `claude/p0-universe-floor-2026-09-24`): the scheduled stale/after-close rebuild can no longer override the floor (only the manual "Rebuild universe now" can); a refused rebuild logs one WARNING, puts "universe rebuild refused: N < floor F, kept K" in the phone digest OPERATIONS, keeps the old lists and retries in 60 min; the ledger row gains per-stage counts and yfinance batch errors; `universe_builder.py --restore-snapshot <stamp>` puts a snapshot back (ledger row `universe_restore`).
- 2026-09-24 Movers board: Pop + Dip stacks three tables (Pop, Dip-strong, Dip-weak; Bounce-strong/-weak in a bounce), sized to their rows; names new to a list get a blue Sym cell; a SPY pullback now stays lit when SPY falls through the open (it went dark after one bar on 09-24), and when a pullback and a bounce both qualify the later turn wins.
- 2026-09-24 Movers board: Pop shows longs and shorts together (side toggle hidden there), column headers sort (desc, asc, board order; unmeasured last), and right-click Hide / Del hides a name for today with an Unhide chip.
- 2026-09-24 WISHLIST.md cleared and replaced by the build plan from the deep review (11 packets, P0 universe floor and swing scan first); an item is work only when the trader moves it to TODO.md or says go.
- 2026-09-24 Chart wheel zoom (branch `claude/chart-wheel-zoom-2026-09-21`, merged): scrolling back shows more - the wheel anchors on the newest candle and the price scale follows the view.
- 2026-09-23 Compact desk (Settings > General > Desk layout, default New): page tabs replace the left menu, the sector tape is hidden and paused (no fetches), Movers stand in their own column right of the charts, the Alert Center tabs are a bottom drawer (click a tab to open, again to close; capture hotkeys and Ctrl+J open it), the arm bar and verbs are one row with M5/D1 alert menus that click the real buttons, and the BounceBot strip's Start/Stop, active count, Entry assist and Mode sit in the status bar. D1/M5 charts 541/540 px at 2560x1400 (classic 211). Old (classic) is unchanged and switches live.
- 2026-09-23 Auto modes: DESK sends nothing to the phone; EVENING scans like DESK all morning, keeps the review queue empty and shows one catch-up card (best longs/shorts, pullback alerts, swing setups, price alerts) on the flip out; in EVENING the SPY ±1% alarm (latched) and fired price alerts ring the phone every 10 s until the mode changes.
- 2026-09-23 Journal UI overhaul (branch `claude/journal-ui-2026-09-23`): Calendar is a Mon-Fri grid with a Week column, tinted day cells (net, trade count, W/L; grey dashed = breakeven), a month summary, prev/next, and 12 month cards instead of the black heatmap + 12x31 table; it reads the whole selected year. Analytics opens with 12 stat cards (PF, expectancy, drawdown, streaks, avg R...), a Long vs Short table, breakdowns by weekday/hour of entry/hold time with avg win/loss/expectancy, one breakdown per table. Tags: chips, completer, one-click common tags, dashed suggestion chips. "Numbers in CAD/USD" badges; tab tooltips; equity curve steps same-day trades in close order.
- 2026-09-23 Journal auto-fill: setup suggestions from what the desk logged before each entry (`journal_setup_evidence.py`: fired alerts, armed watches, liked cards, claimed picks, Focus as corroboration; options match their underlying), the 09:00 Mentor offers the best one on every trade that has any evidence (lane `evidence`, with the why, one click confirms), the nightly `journal_auto_tag` slot fills `regimes` up/down/range/unknown per journal date (`journal_regime_fill.py`, trader rows never overwritten; backfill: `python scripts/journal_regime_fill.py --apply`), and `journal_analytics` exposes time of day / weekday / hold / day-vs-swing (`derived_trade_fields`, `DERIVED_GROUPS`).
- 2026-09-23 Journal P&L inputs: IBKR Flex times read as New York time (were 3h late); a socket combo-leg fill and its Flex row share one uid and the socket BAG parent is dropped; fills sort by instant; trades with a made-up entry (SYNTHETIC_OPEN, or a stock short in a TFSA/RRSP) are kept but left out of totals, calendar and stats with a 'N trades need missing fills - not counted ($X)' line; one shared side-word list; repair CLIs `journal_pnl_repair.py` and `journal_questrade_gaps.py` (dry run by default).
- 2026-09-23 Movers board follows the day: Long on an up day, Short on a down day, once per day state; a Long/Short tap holds until the day state changes.
- 2026-09-23 Movers board phase 2: yfinance gap-fill for stale bot names (cap 600, most liquid first), Lvl column (HOD/LOD brk, ext > 2 ATR from VWAP), ER and ▲/▼/new tags, industry group tags + "Groups:" line, explicit +F to M5 Focus through the adoption gate, and a Dip-strong outcome log (`movers_dip_outcomes.jsonl`, `scripts/movers_outcomes.py --summary`).
- 2026-09-23 Movers board tops the Alert Center's right column: Pop (15-min move in ATRs x RVOL), Dip-strong (holds up vs SPY since an up-day SPY pullback of 0.30%+; Bounce-weak mirror) and My names, a Long/Short toggle and a SPY banner; Focus/Faded review moved to a Review menu and the old Strength page sits behind Deep read. Display only, zero IB (`movers_scan.py`, `ui/services/movers_service.py`, `ui/widgets/movers_board.py`).
- 2026-09-23 RVOL on chart review: D1 volume columns coloured by daily rvol (volume / 50-day avg; white <1, yellow 1-1.5, orange 1.5-2, green 2-3, blue 3+, grey = unmeasured or today's preview), and `RVOL 1.84×` on the D1 and M5 header rows (M5 adds `· bar x.xx×`, the last 5-min bar) - today's volume vs the same time of day over 15 sessions (`rvol.session_rvol`), one yfinance 5m download per symbol per 2 min off the Qt thread (`ui/services/intraday_rvol_service.py`).
- 2026-09-23 Research lake: market context v3. Under `python -m scripts.research_warehouse.cli` the champion import failed silently, so the v2 backfill wrote every timeframe unknown; the import now works under `-m` and a failure raises instead of writing unknown. v2 rows stay as history.
- 2026-09-23 Day Review "Review my day (5 min)" walk (`ui/widgets/recap_walk.py`, cards in `recap_walk_cards.py`): a button by the glance strip and a closed-day banner (plus a nav dot) open a card-at-a-time walk that replaces the page body - trades (chart, R/P&L, grade and environment at entry, what-if, the awake Mentor questions and exit drafts), up to 3 real misses (recipe, lately, where to look, when the desk showed it), a good pass, calls, the market environment verdict and the lesson (rule check, Keep/Stop/Try + mood, tomorrow's rule, streak); ~10 cards by teaching value, keys, resume per session, every read and save on a worker, the day record rebuilt after; saved clues now draw on the page's name and trade charts.
- 2026-09-23 Day Recap chart clues (`ui/widgets/clue_marker.py`, not wired to a page yet): clue mode on `CandleChart` snaps a click to the nearest completed bar (tz-aware time), a tag-chip form saves via `record_clue` on a worker ("saved" / "not saved") with a bar OHLCV + SPY same-bar close snapshot (`record_clue(context=...)`, optional), `draw_clues` shows saved clues as tag-initial markers, `clues_for` reads them, `mark_clue_flow` wires it all for the Walk.
- 2026-09-23 Day Recap rule loop (`recap_rule_loop.py`): today's rule + streak on a status-bar chip and at the top of the Market Prep page / daily prep markdown (worker reads; hidden when none), and a Mentor `rule_reflection` question ("kept it or broke it?") on a closed trade the rule's tag can check (hold_winners, respect_stop, no_trade_first_15m, size_down_in_chop), once per trade, max 3 a day, unknown data never asks; answers read into the day record's recap.
- 2026-09-23 Week Review coach: Weekend Prep > Week Review shows your edge / your leaks (top/bottom 3 rows with n >= 10 from the day-record week files), repeats, rules kept, a 4-week trend, a week picker and a Month rollup; "Ask the AI" saves plain-words questions (`WEEK_QUESTIONS_FILE`) and the new night slot `week_questions` answers them from record excerpts only, every claim cited (uncited claims are not shown), and writes `records/week-<W>-frontier.md`.
- 2026-09-23 Day Recap data layer: `recap_store.py` is the one writer of the trader's recap inputs (card answers, Keep/Stop/Try + mood, rule, rule check, chart clues, environment verdict) in `day_recap_events.jsonl`; `day_session_record.py` writes one never-pruned point-in-time record per session (`day_review/records/<date>.json` + `.md`) and weekly rollups, rebuilt for the last 5 sessions by the night facts slot. No UI yet.
- 2026-09-23 Day Recap coach, findability (`scripts/recap_findability.py`, read-only, not wired to a page yet): each notable name's traits at pick time (unknown if recorded later), a plain-words recipe, how it did over 20 sessions split by environment (n < 10 = too few to tell), where the desk shows such names, when the desk first surfaced it; plus the session's environment timeline and label vocabulary. CLI `python scripts/recap_findability.py --date YYYY-MM-DD`.
- 2026-09-23 Recap point-in-time history: every setup-grades write also appends a dated, tz-aware snapshot (`SETUP_GRADES_HISTORY_DIR/<date>.jsonl`, unchanged content skipped, 600-day prune; reader `setup_grades_history.grades_as_of`), and the opening regime keeps a per-session row for the first read and the directional anchor (`AUTO_OPENING_REGIME_HISTORY_FILE`; reader `opening_regime_history.opening_regime_for`). Evidence only.
- 2026-09-23 Day Review cleanup (Day Recap step A): one clock (desk zone, HH:MM; UTC note times fixed), plain words, a glance strip over a "Details" report card, ◀ ▶ + Alt+Left/Right + remembered session, J/K rows, ONE miss table with five filter chips and a single-click name chart, trade charts, bars-file checks off the Qt thread, non-modal forecast paste, and a no-trade day is not an error.
- 2026-09-23 Research lake: silver `bar_d1` never held SPY, so every D1 and M5 market-context row was unknown. The nightly D1 ingest now always adds SPY/QQQ/IWM; repair CLIs `backfill-benchmark-d1` and `backfill-market-context` (dry run unless `--apply`) fill history under `auto_market_bias_multiframe_v2`; readers take the newest definition.
- 2026-09-23 Test-run guard (conftest): one `-n` > 1 pytest run per machine (OS lock; a second run waits up to 30 min, naming the holder), and `-n` capped to 4 on weekdays 06:00-13:30 PT. Two parallel runs beside the live desk bluescreened the machine. `TBV3_TEST_LOCK=0` / `TBV3_TEST_WORKERS_CAP=off` bypass.
- 2026-09-23 GUI gc freezes: yfinance ticker threads (strength board, autopilot) now close their own SQLite cache connections instead of leaving them for the GUI-thread young sweep (5 s -> 0.1 s beside a busy thread); journal-health read closes its connection; chart daily-bars cache capped at 512 symbols (LRU); `thread_cpu.jsonl` now logs memory, object count and gc sweep time each minute (`thread_cpu_gauge.py --memory`).
- 2026-09-23 Snappy arms: arm clicks show ⏳ queued at once and arm on one ordered worker (ARMED / FAILED + reason; a second click cancels; drained at shutdown). Scanner-child M5 bars, zone arms and the warehouse tee cache are read off the Qt thread; bars not fetched yet are unknown and never fire or clear a watch.
- 2026-09-23 Hide Oil & Gas / Real Estate (default ON, one shared switch): hidden from the setups table, Alert Center feeds (no row, chart or sound; Focus and armed watches still show) and the phone report (`Hidden: N` line). Display only; unknown sector shows; everything is still recorded.
- 2026-09-23 Wall gate: review charts within 1 ATR20 of an SMA in their path or of the D1 trendline (or on its break day) are hidden and followed up by auto-armed `sma_break`/`ema15_reject` or an M15/M30 Pullback (cap 20; anything we cannot follow shows as `at wall`). Replay CLI `scripts/wall_gate_replay.py`.
- 2026-09-23 Night AI fixes: journal enrichment keeps the grammar on (a grammar 400 retries the same schema without length bounds, then JSON mode; the prompt no longer invites the model to return the schema, and an echoed schema is named). Day story sends the model a trimmed pack (~40 KB, was ~100 KB that timed out at 540 s) and refuses an oversized one before loading the model.
- 2026-09-23 Nightly AI reports name tickers: the measured report's swing examples show symbol, side, setup and date (not an occurrence hash); daily digest facts v3 add a capped names block (top D1 setups, settled swings, M5 alerts, journal trades) and the narration must name those tickers with their numbers or it is not published.
- 2026-09-23 Research pack (`scripts/research_pack.py status|export`): read-only source inventory and a point-in-time pack of D1 setups, M5 alerts and journal trades joined to their setups, with a floored family/trait summary, for a frontier model. Refuses live-store output.
- 2026-09-23 AGENTS.md: the full suite takes 4-7 min, so run it in the background (a foreground run was killed at 99%).
- 2026-09-23 AGENTS.md test commands: offscreen Qt on Windows too, the main venv path for worktrees, and rerun suite failures on `main` before blaming a branch.
- 2026-09-23 Mentor asks once: a trade is asked about on one card only. Any answer opens its Save; leaving the card files what was typed and marks it `MENTOR_ASKED`; local AI fills blanks from the trader's words or leaves them blank. Fixes the DRAM trade asked every hour.
- 2026-09-23 Pullback dip gate (`pullback_sma_reclaim_v2`): M15/M30 Pullback triggers need price to have come to the SMA (0.2 x D1 ATR20 or a close break-and-reclaim, within 15 M30/30 M15 bars) and an LRSI flip under 20 before the 80 cross. Stops the ARM 09-23 no-pullback alert; QDEL 09-23 short still fires (real-tape goldens).
- 2026-09-23 AGENTS.md: builders commit WIP and resume existing worktrees, short code comments, Mentor data map row, and search tests before reversing a rule.
- 2026-09-23 Capture tab fills its space (sections stretch to the full tab on one line; picklists grow to show every reason). Veto vocabulary v4 adds Bad industry (q), Too early (w), Not remotely good (e); v3 reasons unchanged and pooled.
- 2026-09-23 M5 swing context: an M5 bar row on a name+side that is also a D1 swing setup shows `· D1 <grade>` (New when ungraded) and ★ when claimed; with prioritise on, those rows draw first (claimed, then grade). Display only, fed from the setups table's model reset.
- 2026-09-22 Close-scan freshness: a daily-bar cache hit now also needs the latest completed session (not just a file touched <30 min ago), so the 13:00 PT close scan no longer publishes on yesterday's bar after the 12:45 preview. Intraday unchanged; a failed refresh still falls back to the cache.
- 2026-09-22 "Working now" strip under Working-lately in the M5 column: today's M5 alerts by setup grade with average R since they fired (completed cached bars only, first stop touch = -1R, missing data = no data); tooltip per alert. Display only.
- 2026-09-22 Parallel tests: pytest-xdist, `pytest -n 8` runs the suite in ~4 min (was ~28). Three font-order-dependent tests fixed (desk icon fonts loaded up front; compact setups table drops Points and trims Bucket before it overflows).
- 2026-09-22 Setup grades: PROVEN/A/B/C/D/New from the trackers' real results (swing: tracker win rate + low bound; day trade: +1R before -1R). Shown on the setups table and M5 bar, best first; the priority switch now defaults ON.
- 2026-09-22 Slim-down review repairs: restored binding safety rules and complete owed gates, corrected STATUS/TODO, and set Codex defaults to Sol lead, Luna max manager and Luna xhigh workers.
- 2026-09-22 Repo slim-down: rewrote AGENTS.md to 8 KB; CLAUDE.md imports it; moved checkpoint/plan to STATUS/TODO and history to gitignored notes/. Initial Codex routing: gpt-6-luna for small work, gpt-6-sol for hard builds/reviews.
- 2026-09-22 Four work streams integrated on main (`cf9c6b2a`): AI-R1/R2/R3 overnight
  AI repair, SP1/SP2 setup-score repair, AR-1/2A/2B/3 alert review follow-up,
  TJ-17A–D recap learning.
- 2026-09-21 Per-trade Mentor Save: an answered trade is stored at once and never asked
  again (`182f3e08`).
- 2026-09-21 TJ-9E: the Mentor tells an exit from an entry. One exit box, plus a night
  slot that drafts why/felt/watching blind to the outcome (`7230b30d`).
- 2026-09-20 TJ-7: mood and process fields, reported only, never acted on (`b63db7af`).
- 2026-09-20 TJ-6: up to three grounded AI ideas a night, kept only by the trader's
  click (`a7809d7c`).
- 2026-09-20 TJ-5: Week Review first in Weekend Prep (`1b9d77e0`).
- 2026-09-20 TJ-12: a six-line report card heads Day Review (`843a3f02`).
- 2026-09-20 TJ-4: the day pack and the overnight day story (`d929e34f`).
- 2026-09-20 TJ-16: prediction ledger vs baselines, plus a blind word tagger (`c4a760e5`).
- 2026-09-20 TJ-14B: Mentor question registry, budget of three (`161e905c`).
- 2026-09-20 TJ-10: read grader, prediction ledger, congruence lines (`57b44ca9`).
- 2026-09-20 TJ-9Q: Questrade fill classifier; a sold put is a sale (`86c64f96`).
- 2026-09-20 TJ-13B: night-only large-model probe (`e54c8203`).
- 2026-09-19 TJ-14A: Mentor card with a forced prediction click (`e8c04f88`).
- 2026-09-19 TJ-15: D1 miss contrast (`fb3f55e9`).
- 2026-09-19 TJ-3: note markers on Day Review charts (`72647104`).
- 2026-09-19 TJ-11/11F: walk-away v2 and the decision session (`a89ec7d5`, `f00ec302`).
- 2026-09-19 TJ-9: forced 09:00 trade labels (`8077a758`).
- 2026-09-19 TJ-13A: night-only local inference, night slates (`9eaae1dd`).
- 2026-09-18 TJ-2A/2B: session bars and walk-away tables.
- 2026-09-18 TJ-1/1L: one Day Review page in two columns (`e00b734a`).
- 2026-09-16 Phase 0.32 entry quality finished (`5bc09528`).
