# TradingBotV3 — the workspace, the machine and the way work is done here

Lessons and observations that cannot be re-derived from `CLAUDE.md`, the checkpoint, CHANGELOG or git history. Where `CLAUDE.md` already states the rule, this file keeps only the provenance. **Split by topic on 2026-09-20** at the 15,000-character budget; nothing was dropped. The siblings are `tradingbotv3-scan-and-desk.md` (the scan's seams, the desk's stores and what the live files hold) and `tradingbotv3-journal-and-ai.md` (the journal, the broker rows, the overnight AI and the local models).

- [observed] 2026-08-26 — Source: `trading_desk.cmd` and the `TradingBotV3 0700 Launch` scheduled task (`scripts\launch_gui_auto.ps1`) — Both launchers start SOURCE through `launch_gui.py`, so a pushed commit is live at the next restart and `dist/` is a verification artifact. Check what the launcher starts before saying a fix is live; the direction of that gap changed once already (2026-08-20/21 the desk ran a pre-fix binary all night).
- [observed] 2026-08-26 — Source: registry `HKLM:\SYSTEM\CurrentControlSet\Control\CI\Policy` `VerifiedAndReputablePolicyState` — Smart App Control read `0` (off, with `SAC_PreviousState = 1`); it read `1` (enforced) on 2026-08-19 and 2026-08-21, and verdicts are per file hash. Read the value; never recall it.
- [observed] 2026-08-31 — Source: commits `ed3c73c` and `edc7999`, branch `claude/swing-favorites` deleted under its session — Several sessions share the desk checkout `c:\Users\Aaron\TradingBotV3`. Run `git branch --show-current` before staging and before pushing, stage explicit files, never `git stash`, and confirm with `git log -S` that the commit holds your work.
- [observed] 2026-09-07 — Source: the desk checkout's reflog after a failed `git worktree remove` — A leftover worktree directory keeping a `.git` file routes `git -C <dir>` to the desk checkout (it moved `main` to `claude/g7-speed-pass` while the desk ran). `rm -rf` the directory before any git command there; check the desk checkout's branch before every fast-forward.
- [observed] 2026-09-05 — Source: the live setup-tracker overwrite (~19:56 PT) — Restore recipe that worked byte-exact: quarantine the damaged files by rename, copy the SQLite mirror back, `TrackerStore.load_payload()`, pre-write the transition sidecar with `setup_tracker_ledger.build_sidecar`, then `save_setup_tracker_payload` and `export_setup_tracker_views` on production code. The rule itself is `CLAUDE.md` "A scratch script never resolves the live home folder by accident".
- [inferred] 2026-08-24 — Source: the AI-layer review, where the builder's fail-before-fix refuted two of five packet premises (AI-P3, AI-P5) — Reproduce a claimed defect at the code level before writing a packet and record the reproduction command in it; a CHANGELOG entry or spec digest is a pointer, not evidence.
- [observed] 2026-09-12 — Source: `git remote -v` — The repository is `Isidore94/TradingBotV3` on GitHub; the sibling `Isidore94/JumpStarter` is the trader's agent-workspace template project, checked out at `C:\Users\Aaron\JumpStarter`.
- [observed] 2026-09-13 — Source: the sweep lead sessions of 2026-09-12/13 (`%TEMP%\tradingbotv3-writer-locks` held 73,500-89,800 zero-byte `.lock` files) — The machine-global writer-lock directory serialises concurrent pytest suites: three full suites at once is the practical cap (the trader's instruction of 2026-09-13), and a suite's tail must be read from pytest's own exit line, never a piped tail.
- [observed] 2026-09-19 — Source: TJ-11 review 2026-09-19 — A packet that grants a file "one function only" must say so in the TESTER's brief too: TJ-11's tester pinned `store.load_annotations`' SHARED default to forward-map, the builder complied, and the setups-table veto hide and the review queue's Reviewed-today mark would have changed on the next session (Monday 2026-09-21: base 0 hidden, branch 12).
- [observed] 2026-09-19 — Source: TJ-11 review 2026-09-19 — A rate whose denominator drops `no_run` while keeping `run` is 100% by construction: an open horizon is right as `unmeasured` for a ROW and wrong for a POOLED rate (TJ-11's lately window shipped 34% where the closed-horizon-only rate was 26%).
- [observed] 2026-09-20 — Source: TJ-13B review 2026-09-20, `tests/test_tj13b_local_large_provider.py` against main `1f260ffa` — a guard pinned byte-for-byte to a branch's BASE goes red the moment the lead fixes main; re-pin it in the integration merge, not in the packet (TJ-13B's weeknight slate tuple was re-pinned to main's order, same 20 names, positions 12-14).
- [observed] 2026-09-20 — Source: TJ-10 re-review 2026-09-20 — `TRADINGBOTV3_DATA_DIR` does not move the machine-local cache: a reviewer's scratch script overwrote the live `machine_cache\daily_bars\SPY.csv` (restored from the durable parquet, 189 of 189 rows verified). A scratch script sets BOTH that variable AND `LOCALAPPDATA`.
- [observed] 2026-09-07 — Source: the Strength page render session (scratch `render_strength_page.py`) — An offscreen Qt render check needs `QT_QPA_FONTDIR=C:\Windows\Fonts` (no font database otherwise), the widget reparented with `setParent(None)` before `show()`, and its own text width set because a `QTextEdit` document measures `(0, 0)` before the first relayout.
- [observed] 2026-09-12 — Source: WS-SX builder, measured in the full suite (`QFontDatabase.families()` before and after `tests/test_qt_focus_panel.py::test_focus_picks_is_top_level_app_page`) — An offscreen test process starts with an EMPTY font database, and the first test that builds a `MainWindow` imports `qtawesome`, whose twelve ICON fonts then become the only families Qt knows: "Sans Serif" resolves into Font Awesome, `✕` (U+2715) still draws and `★`/`☆` (U+2605/U+2606) draw NOTHING. A pixel assertion about a star therefore passes alone and fails in the full suite; `tests/conftest.py` now defaults `QT_QPA_FONTDIR` to the machine's real font directory.
- [observed] 2026-09-13 — Source: WS-DR builder, `tests/test_ws_dr_daily_recap.py`, branch `claude/ws-dr-daily-recap-build` c2470460 — A bare `MainWindow` plus one `processEvents()` calls `alert_center._enqueue_review_alert` with the scanner's own symbol-less `Scanning paused.` row (side WATCH), which the real door discards on its first line; a test that stubs that method as a recorder records a call the product discards. Measure the waiting list (`center._review_queue`) instead.
- [observed] 2026-09-13 — Source: WS-WL builder, measured offscreen, `docs/DESK_CHART_UNIFICATION_PLAN.md` "Chart Review retires as a PAGE" — A Qt tab raised with `setCurrentWidget` leaves focus on the tab BAR, so a `WidgetWithChildrenShortcut` bound on the PAGE never fires; raising must also move focus into the page. Also: `JournalStore` builds its schema on construction, so any reader on a desk whose Journal was never prepared must refuse while `store_needs_preparation()` is true.
- [observed] 2026-09-13 — Source: packet RV-H1-PHONE-WORKER builder probe, `tests/test_rv_h1_phone_worker_delivery.py`, branch `claude/rv-h1-phone-worker` f53d2230 — A PySide6 signal emitted from a plain `threading.Thread` to a Python callable is QUEUED and delivered only by a running event loop; a worker that reports an outcome while the desk is closing must also report it on the thread that waited (`shutdown()`), or the outcome is lost.
- [observed] 2026-09-13 — Source: packet RV-JOURNAL-TEST-ORDER, `tests/test_qt_journal_panel.py:256-266`, branch `claude/rv-journal-test-order` 1599eab7 — A Qt test that patches `journal_feed._STORE` but not `journal_db_path` reads the suite's SHARED scratch journal DB; any earlier test that prepares it flips `JournalPanel` to the no-preparation branch, the Prepare button is hidden and the header legitimately says `No accounts` - the fix is a `tmp_path` DB plus a visibility assertion, never a production label.
- [observed] 2026-09-14 — Source: D1C-A reviewer reproduction (branch `claude/d1c-claimed-picks-build` @ 784cb27e / 270dd839) — The Qt pytest suite is order-flaky in both directions on the sweep branch and its children: `test_g7_speed_pass`, `test_ws_10a_scan_freshness` ("manifest re-read N times while painting": its panel's own file watcher and poll timer fire inside the paint loop under load) and `test_ws_wl_watchlist_tab` each failed in one full run and pass alone, while `test_ws_sx_star_x` fails 4/14 in isolation and passes inside the suite. One full-suite number is a sample, not a baseline; the lead's base run of b8ebd5a4 the same day was 7968 passed / 0 failed.
- [observed] 2026-09-15 — Source: PCT-3 review round 2 (`claude/pct-3-compression` @ 2221288e) — `tests/conftest.py` points `TRADINGBOTV3_DATA_DIR` at ONE `mkdtemp` for the whole pytest session; a test that drives the real `runner._run_master_impl` leaves ~37 files there (ai_state, the tier-outcomes CSV, the priority report, the feature history) and silently un-skips a later test that keys on one of those files existing (`test_r4fix_digest_horizon`). A test that runs a real scan must own its own `tmp_path` data dir and prove the shared dir is unchanged afterwards.
- [observed] 2026-09-18 — Source: TJ-1 review rounds 1 and 3 — `tests/test_ws_10a_scan_freshness.py::test_the_manifest_is_read_on_the_refresh_path_and_never_on_paint` is a pre-existing flake ("the manifest was re-read 2 time(s) while painting"; 2 of 4 full-suite runs, passes standalone every time). Unowned as of 2026-09-18.
- [observed] 2026-09-19 — Source: TJ-9 review 2026-09-19 — A Qt-slot seam with no test is where a packet dies: `ui/app.py` used `trade_mentor_trade_check.KIND_M5_TRADES` (the constant lives in `trade_mentor_schedule`), every Mentor prompt would have raised, and 8,873 tests passed. Drive the slot, not only the helper.
- [observed] 2026-09-20 — Source: TJ-13B build + review 2026-09-20 — while the `ai_jobs_runner` lock is held (22:00-06:00 PDT nightly) about 42 tests that call the real `runner.run_slots` FAIL rather than skip (29 in `test_ai_jobs_runner`, the rest in tj13a / tj13b / `test_ai_evidence_coverage` / `test_ai_jobs_store_window`); verify with the lock FREE before calling a branch red.
- [observed] 2026-09-20 - a targeted test run cannot see a source-count pin in ANOTHER packet's test (R4's one-owner minimum-height pin broke on TJ-5's sparkline); only the lead's full suite on the merge caught it. Source: TJ-5 integration.
- [observed] 2026-09-21 - journal_feed._store() caches a module-global JournalStore for the life of the process, so the first caller decides which store the whole run uses; a new reader added to DayReviewService.read_day made three Day-Review test files poison every later JournalPanel in the same pytest process (32 errors, green on base). A builder's "pre-existing" claim is checked by running the BASE. Source: TJ-9E review 1.
- [observed] 2026-09-20 - a structural guard that greps TWO tokens in ONE file's source can be unsatisfiable by its own packet (TJ-7's mood / review_policy guard flagged three modules that state the invariant in prose); the builder reworded docstrings rather than weaken it, the reviewer re-proved the invariant by tracing importers, and the lead narrowed the guard to CONTACT with the policy. Source: TJ-7 review 1, ae7c06c7.
