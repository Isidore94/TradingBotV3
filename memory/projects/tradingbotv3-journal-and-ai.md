# TradingBotV3 — the journal, the broker rows, the overnight AI and the local models

Split out of `tradingbotv3.md` on 2026-09-20 at the 15,000-character budget; every earlier line moved verbatim. Same rules: a tag, a date and a source on every line, a detail file outranks `MEMORY.md`, and recall is never authority.

- [observed] 2026-08-28 — Source: the local-model context raise measured that night — `gemma3:12b` at a 65,536-token context loads in 8.1 GB, fully on the iGPU, at about 118 tok/s prompt-eval; prompt-eval time, not RAM, bounds an overnight run (a 45k-token package costs about six minutes).
- [observed] 2026-09-13 — Source: WS-RP tester, `scripts/ai_summary.py:163,2872,2952` — The desk has no tokenizer: `ai_summary._ESTIMATED_CHARS_PER_TOKEN` (2.5, measured 2026-08-28) and `_BUDGET_CHARS_PER_TOKEN` (2.0) are its whole token arithmetic, and real counts only come back as the server's `usage.prompt_tokens`. A "token count" printed anywhere on the desk is that estimate and must say so.
- [observed] 2026-09-17 — Source: TJ-1 tester handoff (branch `claude/tj1-day-review`, 79ca5fd0) — `measured_report.report_from_payload` raises `TypeError` on a payload with no `entry_quality` block (`scripts/measured_report.py:1699-1712`, the `available_windows` walk has no `or ()` guard while every sibling field has one); every published payload carries the block, so it is latent. Not yet fixed.
- [observed] 2026-09-18 — Source: TJ-1 builder handoff (branch `claude/tj1-day-review`, 7988e915), reproduced on unmodified code — `ai_jobs/cohorts.py:132` runs `update_human_focus_outcomes` with `reference_date=None`, so its 45-calendar-day window is anchored on the WALL CLOCK (`human_focus_tracking._market_date(None)`); a pick older than that is dropped while the job still reports `graded=2` with zero outcome rows written. Test fixture dated 2026-08-03 aged out at 00:00 ET on 2026-09-18. Job-side fix not yet authorized; the test was made deterministic on the branch.
- [observed] 2026-09-19 — Source: branch `claude/tj13a-night-slates` @ 6878c72a, reviewer reproduction against a copy of the live ledger — TJ-13A made local inference night-only seven days a week and gave each night a slate (`ai_jobs.runner.night_kind` / `slots_for`); `ai_summary` now runs on SATURDAY night only, keyed to Friday's session, so Mon-Thu sessions get no AI summary.
- [observed] 2026-09-19 — Source: TJ-9 tester probe of a read-only copy of `trade_journal.sqlite3` — Questrade's executions endpoint sends no `securityType`/`symbolType`, so all 226 live Questrade fills are `security_type='UNKNOWN'`; the instrument is readable from the payload's own `symbol` (`AAOI18Jun26P120.00`) and `side` (`STO/BTO/BTC/STC`); `group_key` and `_contract_multiplier` read the type, so classify + backfill + rebuild must be ONE tested CLI step (packet TJ-9Q).
- ~~[observed] 2026-09-19 — Source: same probe — `journal_importers.normalize_side` does not map `STO`/`BTC`/`Cov`, so a sold put reads +qty: the trader's four sold puts read `direction=LONG` and three are stuck `OPEN`.~~ SUPERSEDED 2026-09-20 (count corrected) → [observed] 2026-09-20 — Source: TJ-9Q reviews on copies of `trade_journal.sqlite3` — `normalize_side` does not map `STO`/`BTC`/`Cov` (live: STO 4, BTC 4, COV 25), so a sold put reads +qty: the trader has **three** sold-put positions (four STO fills - AAOI has two), all three reading `direction=LONG` and sitting `OPEN` with `quantity_closed` 0, plus ONE bought put (QQQ) that is correctly LONG/CLOSED but 100x too small.
- [observed] 2026-09-19 — Source: TJ-3 reviews 2026-09-19 — A marker's index is an index into the tape it is DRAWN on, and a stamp past the tape's end is unplaced, never clamped: on the live journal `trade_date` is the EXIT day, 116 of 216 `opened_at` values fall on an earlier day and 62 of 216 legs fall after 13:00 PT.
- [observed] 2026-09-20 — Source: TJ-9Q reviews on a copy of `trade_journal.sqlite3` — a mass `trade_id` re-key must carry every trade-keyed table, not only `trade_annotations`: `ai_trade_enrichment` and `note_lane_verdicts` have no reader-side alias path (`list_ai_enrichment` / `list_opportunity_events` match the literal `trade_id` and never call `resolve_trade_id`); TJ-9Q's reclassify carries them and names what it cannot place.
- [observed] 2026-09-20 - a JSON schema's maxItems / additionalProperties is a grammar hint to the provider, never a guard; TJ-16 and TJ-4 both shipped a verifier that trusted it. Source: TJ-16 review 1, TJ-4 review 1.
- [observed] 2026-09-20 - a ledger status is a vocabulary ai_jobs/ledger.py owns (ATTEMPT_STATUSES = failed + degraded; skipped is normal): a reader that tests status != "ok" named 22 slots broken on the live ledger when 2 were. Source: TJ-12 review 1.
- [observed] 2026-09-20 - C:\TradingBotData\day_review\sessions\ held 3 session folders and ZERO pack.json, so Day Review's story, the report card's pack lines and Week Review all open on an honest empty state ("0 of 5 sessions have facts") until the post-close tick writes packs. Source: TJ-4 / TJ-5 review reproductions, read-only counts.
- [observed] 2026-09-20 - a Mentor question's answer mix is not a desk measurable: day_report_card.process_line's origin_answers is one kind, one session, and _pool_cards drops it. Source: TJ-6 tester step 0.
