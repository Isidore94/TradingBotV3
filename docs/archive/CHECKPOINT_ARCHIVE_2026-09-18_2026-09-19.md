# Checkpoint archive: 2026-09-18 to 2026-09-19

Dated checkpoint entries retained verbatim from `CURRENT_CHECKPOINT.md`. Open gates and operational restart rows remain in the active checkpoint.

### 2026-09-19 - TJ-14A MERGED

Branch `claude/tj14a-mentor-card` (tip `6808abd9`), merged `e8c04f88` into `lead/p033-integration2`; reaching `main` after the night's AI run. What it is: TJ-14 items 1 and 6 - the Mentor card keeps **What I see** (free text, optional, `mentor.observation`) apart from **What I expect** (`mentor.prediction`, schema `mentor_prediction_v1`: direction, horizon, confidence, optional `because`). Horizons `rest_of_day` (every card) and `next_5_sessions` (the 08:00 and 12:00 D1 cards); day directions up / down / chop / no_view, five-session up / down / range / no_view; `No view` is a COMPLETE answer and hides How sure, which is otherwise forced. Submit stays grey until every direction row AND its How sure are clicked, and the gate lives INSIDE `submit()` and `read_unchanged()`, not only on the buttons. A card answered with clicks and no words is one complete row whose `text` stays `""` (never a synthesised sentence; `is_publishable` relaxed for a clicked row only; the wordless row's `entry_id` salted so an `m5_d1` card's two rows differ, an entry WITH words keeping a byte-identical id). `market_journal.prediction_of` is the ONE reader and answers None for all FOUR older vintages in the live ledger (69 rows: 28 with no `mentor` key, 13 `mentor == {}`, 6 without context, 22 full v1). TJ-9's forced trade-check section keeps its OWN gate. **The lead has confirmed this vocabulary as TJ-10's and TJ-16's contract.**
Round 1 was NO-GO on one blocker: `read_unchanged` after an answered 08:00 / 12:00 D1 card filed a D1-timeframe row carrying a `rest_of_day` call (the host handed `rows[-1]` and the card fell back silently) - permanent bad evidence in an append-only ledger. Now **a row's timeframe and its prediction's horizon ALWAYS agree** (`HORIZON_FOR_TIMEFRAME`), enforced at the WRITER (`build_entry` raises `PredictionTimeframeError`, `is_publishable` refuses), and "Read unchanged" reaffirms PER TIMEFRAME from the latest read of each (today's session only, all-or-nothing per card; no fallback; a timeframe with no earlier read makes the verb unavailable and the tooltip says which; a clicks-only answer is never offered, because the verb restates WORDS).
Item 6: `trade_mentor_context_v2` - 18 symbols (`XLRE` added alphabetically, SPY still index 6), each with the day's change, place in the day's range and both prior-session sides, plus a `derived` block (breadth, fear with a divergence flag, rates, oil, sector leaders / laggards on the day AND over 30 minutes, offense vs defense, sectors above VWAP with its denominator), every line naming its inputs and `unmeasured` naming the one actually missing. Completed bars only, v1 rows still readable, `compact_for_ai` carrying ONE scalar `common.internals` because `ai_summary._bounded` cuts six levels down. ONE builder serves the live card and the pure `internals_at`; `internals_bars_at` reads M5 from the durable tape and D1 from the desk's daily cache, and for RSP, USO and TLT - which have NO file in `machine_cache/daily_bars`, the scan universe never fetching them - builds prior-session daily bars from the tape itself (`_TAPE_D1_LOOKBACK` 3, no network, point-in-time), so breadth / rates / oil are measured on a rebuild while the five-session and SMA20 facts stay `unmeasured` and say why. The card SHOWS the strip (`MentorInternalsStrip`, styled in `theme.qss` by object name, no second fetch), and `day_review_bars` adds the internals symbols to its one batched post-close download - one yfinance call per FIFTY symbols, so the fixed base of 18 costs a second call only past 32 other decided names; zero IB, no D1 leg.
Measured: the stored Mentor row grew ~53-57% (4,462 -> ~7-8 KB; about 7 answers a day); a synthesised 21-day v2 month file reads in ~9 ms; `_previous_mentor_read` costs ~10.7 ms once per prompt on the Qt thread; the 09:00 card (strip + What I see + prediction row + forced trade section) is 591 px tall at 520 px wide inside its scroll area with nothing clipped; card construct p50 0.60 -> 1.12 ms. Three thresholds were widened with the measured number beside each (popup-context cap 6 -> 10 KB, resilience budget 3,000 -> 6,000 chars) and 6 WS-TM test functions gained clicks across 9 call sites.
Recorded, not repaired: a rebuilt row's `m5_as_of` is Pacific-zoned where the live card's is Eastern - the same moment, compared with `astimezone` and never as a string; `read_unchanged` now returns `{"ok", "entries": [...]}`; `internals_bars_at` has no production caller yet (a capability until TJ-10 / TJ-16 call it). Items 2-5 are TJ-14B. Full suite on the merged tip: **9,264 passed, 14 skipped, 72 subtests, 2 failed - exactly the two known unowned flakes `test_ws_10a_scan_freshness` and `test_g7_speed_pass` - on `1f260ffa` (wave 1 + TJ-11F + TJ-3 + TJ-15 + TJ-14A), 2026-09-19 22:03 PDT; the run began at 21:49 so every AI-lock test ran before the 22:00 task start and none stood down (the usual 14 skips); ruff clean, smoke 7/7**. Live gate #159's first clause owed.

### 2026-09-19 - TJ-15 MERGED

Branch `claude/tj15-miss-contrast` (tip `8547c4a5`), merged `fb3f55e9` into `lead/p033-integration2`, with the lead's slot-position fix `1f260ffa` on top. Merged into `lead/p033-integration2`, reaching `main` after the night's AI run. What it is: what the misses had in common, measured - the pure `scripts/evidence_contrast.py` (two counts, two medians and `compression_calibration.auc` per feature; rank key `abs(auc-0.5)` then feature NAME; `rate()` the ONE Wilson over CLOSED horizons) and the deterministic Stage 1 slot `scripts/ai_jobs/miss_contrast.py` publishing one `miss_contrast-<session>.json` pack beside the digest. **No model anywhere in this chain.**
Two reviews by reproduction on the real 2026-09-18 window, NO-GO then GO, both round-1 blockers about what the pack SAYS. (1) A leader named off FOUR values against ONE (`veto / sma_incoming`, n=47, measured=30, features 4 v 1 at AUC 1.0): a feature is now ranked only with `MIN_CONTRAST_SIDE_N` = 10 rows on EACH side and `MIN_REPORTABLE_N` across both, a thinner one NAMED in `thin_features` with both counts and no AUC, `compared` counting the ranked only, and a group leading only with a reportable rate AND a ranked feature. (2) About HALF the reviewed population is M5 - 1,026 M5 against 1,013 D1 (plus 4 H1, 2 "5M") - and it was pooled under the D1 swing ruler: the pack now judges **D1 only** and counts the rest in `excluded_by_timeframe` / `no_timeframe`. Also in the fix round: the join reaches the decision's own session OR the ONE prior exchange session (`MAX_SCAN_AGE_SESSIONS = 1`, always the last scan at or before the stamp), because the live scan runs ~07:00-07:50, 10:01, 12:45 and 13:00 desk time and an own-session-only join lost 47-83% of decisions (live: 238 same-session / 140 prior / 97 none of 475 measured); only a VETO groups by its vocabulary code; an unreadable `session_date` refuses and writes nothing. Live re-derivation: 1,013 D1 decisions in 11 groups; leaders `compressed` (`last_volume`, 32 v 71, AUC 0.72), `veto` with no code (`rs_vs_industry_5d`, 15 v 53, 0.70), `like` (25 v 59, 0.33 - separation, not height). Costs: the ~709 MB features file STREAMED by session, 7.8 s warm / 17.7 s cold at ~100 KB traced allocation; whole slot ~18-24 s and ~240-300 MB heap with 600-900 durable daily frames; ~119 KB pack, written to `store.digests_dir()` (the DAS), not local-first.
**The lead's integration fix (`1f260ffa`): the slot sits ABOVE the `market_story_rollups` + `measured_report` pair that closes stage 1**, not after it - WS-10D and WS-RP each pin `measured_report` directly after `market_story_rollups` and `test_veto_cohort_grading` pins the whole slate, three red order pins the packet's targeted runs never reached. `_STAGE_ONE_LAST_SLOT` is untouched; a slot appended after it would silently leave the Sunday slate. plan.md 12.3 now carries the lesson (a slot packet runs `-k "slot or stage or slate"` plus those three test files). The lead also amended two unsatisfiable tester literals (`53d41a9a`) and authorised seven floor keywords (`8547c4a5`); no tester assertion is left red.
Recorded, not repaired: the no-code veto bucket reads just `veto` where it wants `veto (no code recorded)`; the pack never states a leading feature's DIRECTION in words; `MIN_CONTRAST_SIDE_N` = 10 is lenient (a merged-tree like leader rested on 10 v 37 - watch it on gate #160); `read_latest` is a FILE READ that TJ-5's Week Review table must call on a worker; six private `walkaway_day` seams are called deliberately rather than copied. Full suite on the merged tip: **9,264 passed, 14 skipped, 72 subtests, 2 failed - exactly the two known unowned flakes `test_ws_10a_scan_freshness` and `test_g7_speed_pass` - on `1f260ffa` (wave 1 + TJ-11F + TJ-3 + TJ-15 + TJ-14A), 2026-09-19 22:03 PDT; the run began at 21:49 so every AI-lock test ran before the 22:00 task start and none stood down (the usual 14 skips); ruff clean, smoke 7/7**. Live gate #160 owed.

### 2026-09-19 - TJ-3 MERGED

Branch `claude/tj3-note-markers` (tip `58ee11f4`), merged `72647104` into `lead/p033-integration2` at 21:14 PDT. What it is: the trader's own words drawn on the Day Review charts - the pure `scripts/day_review_markers.py`, an ADDITIVE `NoteMarkers` family in `ui/widgets/candle_chart.py`, and a page that only pushes what the worker built. **The rule the packet turns on: a marker's `index` is an index into the tape it is DRAWN on, and it exists only when the stamp fell inside that bar's own span** (`start <= stamp < start + width`, the width read off the tape; a daily tape by market-local DATE; comparison always through `astimezone`, journal UTC against a Pacific-local tape). A stamp past the end of the tape is `after_tape` with `index: None` - KEPT, counted on the worker by `placement_counts` and SAID in the caption (`2 marks after the tape - not drawn.`), **never clamped onto the last candle**; a stamp in a hole is `between_bars` the same way; `before_tape` / `no_tape` / `unreadable` yield no marker at all.
Both reviews GO by reproduction. The clamp was measured on a copy of the live journal: 62 of 216 trade legs (29%) fill after 13:00 Pacific and were being drawn on the 12:55 candle; `trade_date` is the EXIT day and 116 of 216 `opened_at` values fall on an earlier day, so they correctly place nothing. On the live 2026-09-17 session: 278 marks over 174 name charts = 270 drawn + 8 counted. `candle_chart.py` is the live Alert Center's chart and was proven ADDITIVE at base and tip - identical scene items, view range, earnings glyph positions, ribbon span, full-widget PNG sha256 and bar / level / empty-space click sequences, with `_position_note_glyphs` and `NoteMarkers.paint` running 0 times when no markers are set; glyphs pool hidden, every item is `ignoreBounds`, `set_data` drops stale markers, `markerClicked` is emitted IN ADDITION to the existing signals. Both payloads are built ON THE WORKER at the end of `read_day` from rows already read (no second store, no read on a row click, the paint path never imports the builder); `read_day` median ~502 -> ~554 ms on a staged session.
Recorded, not repaired: `name_charts` carries a tape per decided name, so the payload is ~1.0-1.5 MB for a 174-name session - trimming it to the visible rows is a LATER packet. The page has NO D1 toggle and TJ-3 added none; on a daily tape a weekend or holiday stamp is `between_bars`, to be decided knowingly if a D1 toggle ever lands (the trader's TJ-11F rule is that a Friday-evening call is Friday's judgement). Full suite on the merge: 9,166 passed, 14 skipped, 72 subtests, 2 failed + 1 teardown error - exactly the two known unowned flakes `test_ws_10a_scan_freshness` and `test_g7_speed_pass` (41/41 green standalone) - on `72647104` (wave 1 + TJ-11F + TJ-3), nightly AI lock free, 2026-09-19 21:29 PDT; ruff clean, smoke 7/7, source selftest 92/92. Live gate #147 owed, after gate #146/#152's bars back-fill.

### 2026-09-19 - TJ-11F MERGED

Branch `claude/tj11f-decision-session` (tip `67143e3e`), merged `f00ec302` into `lead/p033-integration2`, which sits on `main` `04a2de0e`. What it is: the trader's ~16:20 PDT answer to the lead's TJ-11 question - *"a veto on friday night (after the market close) should not be considered monday since we have new information then"* - built the same evening. A decision belongs to the session whose information it JUDGED: `market_calendar.decision_session(stamp)` is the session whose New York date the stamp falls on when that date is a session day (pre-market, in-session and after the close alike), else the most recent PRIOR session. A new annotation row carries `decision_session_rule: "judged_session_v2"` beside `decision_session`; a stored value WITHOUT the marker was written under the struck forward rule and every reader recomputes it from the row's own stamp; **no row is ever rewritten or backfilled**. `day_review_service._stamped_dates_for` maps a session onto the non-session dates AFTER it, and the D1 ruler starts from the judged session's close. `session_date` is byte-identical to base for every writer and reader, so nothing on the live desk outside Day Review changes. Decision 0021 answer 16 is struck in place and reversed as answer 33; `plan.md` TJ-11 item 5 and gate #156 are reworded.
Reviewer GO by reproduction on a COPY of the live annotation store (1,198 rows): 0 rows carried `decision_session` or the marker (the desk had not written since 2026-09-18 21:07); the 18 Friday-evening calls (12 veto + 6 `like_claim`, 21:04-21:07 PT, stamped New York's Saturday date) read on `read_day("2026-09-18")` - 150 = 132 + 18 + 0 - and NOT on `read_day("2026-09-21")` (0 rows); `pick_feedback.decisions_today` byte-identical to base for 09-18, 09-19 and 09-21. Calendar edges checked: Labor Day weekend -> Friday 2026-09-04, a Thanksgiving-Thursday evening -> Wednesday 2026-11-25, an early-close Friday evening stays on its own day, Sunday 23:00 PT (Monday's New York date) -> Monday under BOTH rules with 0 live rows there, garbage -> `None`. No caller of `decision_session` outside Day Review. The stored-field vs service-tag disagreement TJ-11's review recorded is GONE.
Recorded, not repaired: the rule marker is TWO independent literals (`store.DECISION_SESSION_RULE` and `walkaway_day.DECISION_SESSION_RULE`) and wants one imported constant; the page's "Today - provisional" entry lets Day Review open on a NON-session date, where `_stamped_dates_for("2026-09-19")` returns the weekend itself and the service tags those in-memory rows `decision_session: 2026-09-19` with the v2 marker although the rule would say 2026-09-18 (the same 18 rows base showed on that page, never written to a store) - it wants a guard; `load_annotations(..., by_decision_session=True)` and `row_decision_session` still have no production caller. Full suite on the merge: `9,089 passed, 14 skipped, 72 subtests, 1 failed - the known unowned flake `test_ws_10a_scan_freshness` (it now fails in every full-suite order and is green standalone) - on `f00ec302` (wave 1 + TJ-11F), nightly AI lock free, 2026-09-19 20:55 PDT; ruff clean, smoke 7/7, source selftest 92/92`. Live gate #156 owed, reworded.

### 2026-09-19 - TJ-9 MERGED; wave 1 complete; the build paused on the usage limit

TJ-9 merged into `lead/p033-integration` as `8077a758` after three review rounds by reproduction on a copy of the live journal: round 1 NO-GO on four blockers (a wrong-module constant outside its `try` at `ui/app.py` would have raised on EVERY Mentor prompt while 8,873 tests passed; `same_session` read the CLOSE date, wrong for 116 of 216 live trades; a `vetoed:` provisional tag was offered as a one-click SETUP on a live trade; no section after an AWAY / expired 09:00), round 2 NO-GO on one (a `journal not ready` card never became the questions once the fills landed), round 3 GO. Item 7 was refuted as an additive change and became packet TJ-9Q (see `plan.md` TJ-9). Batched advisories: `_trade_check_is_owed` reads the annotation log and opens two journal stores on the Qt thread per prompt (~50 ms today - wants a per-session cache); `_journal_retry_date` is in-memory only (a restart before 10:00 allows a second morning pull); `accept_auto_tags` appending a tag downgrades `claimed_before_entry` to `recalled_after` (name that rule wherever the three are reported). ONE full suite covered all three wave-1 merges: 9,062 passed, 14 skipped, 72 subtests, 1 failed - the known unowned flake `test_ws_10a_scan_freshness` (20/20 green standalone) - pytest exit 1 for that flake only, on the merged wave-1 code `8077a758`, nightly AI lock free (2026-09-19); ruff clean, smoke 7/7, source selftest 92/92. Wave 2's three testers (TJ-3, TJ-15, TJ-14A) were running when the session hit its usage limit at ~13:15 PDT; all four live agents died with HTTP 429 and the glance block says how to resume them. Gate #154 owed.

### 2026-09-19 — TJ-11 MERGED

Branch `claude/tj11-walkaway-v2` (tip `a744bec1`), merged into `lead/p033-integration` at `a89ec7d5`; not on `main` yet and not pushed. What it is: Day Review's walk-away gets a SECOND ruler - a D1 decision is measured on daily bars from its session's CLOSE over 1/3/5 exchange sessions (`pending <date>`, never zero), beside TJ-2B's unchanged M5 tape. Measured on the live stores, session 2026-09-17 carried 146 D1 decisions and 94 M5 ones and TJ-2B graded all 240 on the five-minute tape. `scripts/real_miss.py` holds `REAL_MISS_V1` (1.0 ATR in favour before 0.5 ATR against, the adverse extreme taken first inside a bar, point-in-time ATR(14), anything missing `unmeasured`), shared with TJ-15's future slot. A fifth Earlier-calls table, against-you-first and at-the-close columns, one deterministic sentence per table, instrument-aware trade rows, and a skill line reading the trader's rejects against the same scan's liked and untouched names.
Review round 1 was NO-GO at `21ed0eb6` for two real defects, both fixed at `a744bec1` and re-reviewed GO by reproduction on copies of the live stores. (1) The session stamp had leaked onto the LIVE desk: `load_annotations(session_date=)` forward-mapped BY DEFAULT, so on Monday 2026-09-21 `pick_feedback.decisions_today` would have hidden 12 setups rows and marked 18 names Reviewed-today that `main` shows, and the writer change would have moved Friday-evening vetoes into `review_learning`'s veto cohort, the cohort graders and the recap. Lead design, binding: `session_date` keeps exactly its base meaning and value for every writer and reader; the decision's session is the NEW ADDITIVE key `decision_session` on NEW rows only, never backfilled, empty when the calendar cannot answer, read by TJ-11 alone - parity with base pinned for `pick_feedback`, `review_learning`'s join, the three cohort graders and `daily_recap_reader._decisions`. (2) The skill line's pooled rate was OUTCOME-CENSORED: an open 5-session horizon kept an early `run` and dropped a `no_run`, so open cells were 100% by construction (lately LONG rejected shipped 34% = 30/87 where the closed-horizon-only truth is 26% = 20/77). A pooled rate now counts a name only when its horizon is CLOSED, runs and no-runs alike; open names print as `pending P` and sit in neither half; the ROW may still show its early run.
The 18 D1 calls stamped Saturday 2026-09-19 at 21:04-21:07 PT are 12 veto-cohort and 6 like-cohort picks with a non-session `trade_date`, a Monday-close ruler and ZERO outcome rows. The graders and those rows are deliberately untouched (lead decision (a)); only TJ-11's readers map forward. **Superseded the same evening by TJ-11F: those readers map BACK, so these 18 rows read on Friday 2026-09-18 - see the TJ-11F entry above.**
Measurements: daily-bar read at the caps (400 symbols / 150 earlier / 150 untouched, 60-bar tails, NAME order and never a result) 3.37 s and 112 MB; `read_day` 6.8-9.8 s per session on a staged home; worst single GUI stall on reload 672.9 -> 92.4 ms with settle-deadline hits 2 -> 0. Full suite on the merge: **9,062 passed, 14 skipped, 72 subtests, 1 failed - the known unowned flake `test_ws_10a_scan_freshness` (20/20 green standalone) - pytest exit 1 for that flake only, on the merged wave-1 code `8077a758`, nightly AI lock free (2026-09-19); ruff clean, smoke 7/7, source selftest 92/92** (one run covers this and TJ-13A). Live gate #156 owed.
Recorded, not repaired: `load_annotations(by_decision_session=True)` and `row_decision_session` have NO production caller yet (the service maps through `_stamped_dates_for`); the stored `decision_session` and the service's tag DISAGREE for a same-day after-close call (a Friday 16:30 ET veto's stored field says Monday while the page shows it on Friday), and TJ-15's nightly slot must pick one deliberately; `market_calendar.session_close` is a flat 16:00 with no early-close modelling (pre-existing); and on a fresh day the session skill line honestly reads `measured 0, pending N` with the lately line carrying the content.

### 2026-09-19 — TJ-13A MERGED

Branch `claude/tj13a-night-slates`, merged into `lead/p033-integration` at `9eaae1dd`; not on `main` yet and not pushed. What it is: local inference is night-only SEVEN DAYS A WEEK (the weekend "open all day" exemption is gone; the market-session block's own weekend short-circuit is untouched), and each night picks its slate on the exchange calendar from the evening it started - `runner.night_kind()` / `runner.slots_for()`, weeknight without `ai_summary`, Saturday with `ai_summary` and `weekly_synthesis`, Sunday the deterministic stage plus what the weekend still owes. `EXPECTED_SLOT_ORDER` and decision 0018's stage boundaries are unchanged; decision 0018 carries the amendment.
Four overnight repairs ride with it: the summary gives up on an unreachable endpoint at the FIRST call and bounds its synthesis package to the local evidence budget (119,677 characters measured against 78,119); the enrichment reply wrapped in the response schema's own name is unwrapped; the morning brief counts membership-only names instead of printing 259 empty sections; and the measured report's example lists name three different symbols. A per-ticker projection's `truncated` flag is now re-derived from the projection (80 of 133 sources had inherited it falsely), and the A.I. Summary page's Generate button asks the same window before a local run.
Review: GO at `35d1cf8f` by reproduction on a COPY of the live 476-row ledger. The three advisories were then fixed at `6878c72a` - `ai_summary` capped at `max_attempts=3` (it was 0/unbounded, and fail-fast made a dead endpoint cheap enough to run TEN times in one simulated Saturday night); `--slot` resolves against `default_slots() + optional_slots()` with an unknown name an error exit; `record_rejected_reply` writes LOCAL (`project_paths.AI_REJECTED_REPLIES_DIR`, 200 files), so no DAS path is reachable from a slot.
Full suite on the merge: **9,062 passed, 14 skipped, 72 subtests, 1 failed - the known unowned flake `test_ws_10a_scan_freshness` (20/20 green standalone) - pytest exit 1 for that flake only, on the merged wave-1 code `8077a758`, nightly AI lock free (2026-09-19); ruff clean, smoke 7/7, source selftest 92/92**. Live gate #158's TJ-13A half is owed and first readable after tonight's Saturday slate (2026-09-19 22:00 PDT), which on the live ledger will actually run `journal_import`, `ai_summary` and `weekly_synthesis` for session 2026-09-18 - the other 18 slots are already `ok` for it. Note for reading the ledger: a REFUSED endpoint degrades in about 2 s, but a HUNG one still costs ONE 900 s read timeout on the first slice, so a ~15 minute degrade is the repair working. `weekly_synthesis`'s cosmetic artifact date was deliberately left unfixed this round and will show on that first Saturday slate.

### 2026-09-19 — Review-loop audit approved; TJ-9 … TJ-13 planned

A read-only audit of the live stores (2026-09-18; the assessment is an artifact, not a file) found the loop broken at three links: 215 journal trades with one confirmed setup tag and no stop or note; no grader for the trader's market reads and no view-versus-picks check (the thesis store is empty); ~95% of daily decisions are D1 calls measured with TJ-2's same-session M5 ruler, and a call after 21:00 Pacific is stamped with New York's next calendar date (Friday evening's carry a Saturday). The trader approved every recommendation with one change - trade labelling is FORCED at 09:00 Pacific through the Trade Mentor. `plan.md` 12.4 now holds TJ-9 … TJ-13, TJ-4 and TJ-5 carry dated amendments, 12.5 the new order, and decision 0021 six new binding answers. Later the same day the trader added two more (answers 19-20, TJ-13 items 5-7): local inference runs at NIGHT only, seven days a week - the weekend is not an all-day window, Saturday night carries the weekly slate and Sunday night the backlog - and the large local model writes the week story instead of OpenAI. Measured for it: the `TradingBotV3 AI Jobs` task already fires 22:00-06:00 Pacific every night as its own process, weekend firings skip every slot, `weekly_synthesis` has never run and the 27B is used by nothing. **Second look, same day:** asked how sure the lead was that the finished plan meets the goal, the answer was ~65% as written, with eight holes; the trader said "Yes add all of this" and asked that the Trade Mentor stop asking everything hourly and instead ask for whatever data the desk is missing. Measured for it: `extract_thesis` on the trader's 42 real notes gave 21 `unstated` (it missed "D1 SPY is still downtrending"), so the graded read becomes a forced CLICK; no baseline or untouched-name comparison exists in any reader; 50 trades since 08-04, 8 options, 15 of 37 closed held past five sessions; fills reach the journal only at night. `plan.md` now carries dated amendments in TJ-6, TJ-9, TJ-10, TJ-11 and TJ-12, new packets TJ-14 (Mentor question registry, budget of three, same-session fills) and TJ-15 (measured miss contrast), and the 12.5 order with a gate-first clause. **Then:** the trader asked that the Mentor keep a DESCRIPTION apart from a PREDICTION so tendencies can be found - TJ-14 item 1 now splits the card into What I see / What I expect (direction, horizon, How sure, Because) and new TJ-16 plans the prediction ledger with a point-in-time context snapshot, naive baselines, calibration, a right-vs-wrong contrast, span-grounded word tags from the model and the week story's voice (decision 0021 answer 29). **Internals:** the trader asked that the Mentor process RSP / VXX / USO / TLT and the sector ETFs itself. Measured: Phase 0.31 already stores `mentor.context` (17 symbols, four facts each, 99-119 of 119 readings a day measured) on every ANSWERED read, but only `ai_summary` reads it, the card never shows it and `XLRE` is missing - TJ-14 item 6 plans `trade_mentor_context_v2` with derived lines, a strip on the card, a rebuild for any moment from the session tape, and one function feeding every AI pack and TJ-16's ledger (answer 30). **Reconciled for the builders:** section 3 and 12.1 carry the trader's 09-18/19 words, 12.3's AI window is corrected (stored ET, 22:00-06:00 Pacific, nights only seven days), TJ-4's pack gains `internals` / `skill` / `report_card`, TJ-7's Mentor strip becomes TJ-14's `day_close`, TJ-9's retry is the desk's pre-card pull, the forced trade section sits outside TJ-14's budget, TJ-13 lists every new slot by stage and picks up the measured report's repeated-name defect, `REAL_MISS_V1` is one shared pure function, and 12.5 has a one-screen build table. Baseline unchanged: documentation only, no code, no store written.

### 2026-09-18 — TJ-2B MERGED

The worker supplies four pure walk-away populations from durable rows, claims, later trades and exit-day bars. The page reads one payload. Review passed, full pytest passed with exit 0, and gates #152/#153 remain owed on a real closed session.

### 2026-09-18 — TJ-2A MERGED: Day Review session bars (`main` `4dd99034`)

- **What landed.** `day_review_bars.py` writes closed-session completed regular-hours M5
  parquet bars for decided/traded names and SPY/QQQ/IWM/VXX. The post-close worker builds
  the index first, then bars; a past missing file backfills once on a worker. TJ-2B's four
  tables are not part of this branch.
- **Verified.** Reviewer GO after staged-copy reproduction; full suite 8,814 passed, 14
  skipped and 72 subtests, exit 0; ruff clean; smoke 7/7; source selftest 91/91. No live
  store write, desk restart, or pytest network request.
- **Still owed.** Gate #152 after a closed session.

### 2026-09-18 — TJ-1L BUILT: Day Review in two columns (branch `claude/tj1l-day-review-layout`)

- **What happened.** The trader read the new page on a 3800 px screen and said *"there's a
  lot of empty space horizontally that's not being efficiently used"*; offered three shapes,
  he chose two columns. Presentation only, inside the one `QScrollArea` the page already
  was. Row 2 is a horizontal `QSplitter` named `DayReviewColumns` at 55/45, restored at
  construction and saved per machine on the drag through `ui.panels.desk_layout`
  (`qt_day_review_columns_v1`) - LEFT *What happened*, *Open theses* directly under it, then
  the SPY pane with a 320 px floor taking the column's slack; RIGHT the entries list over the
  reader in their own 60/40 splitter, then *New entry* spanning the column, its verb row and
  the external forecast collapsed to three lines. Row 3 is *Walk-away* as a 2 x 2 grid of
  equal columns (the real table top-left, TJ-2's three populations as small titled frames,
  top-aligned). Row 4 is *What you traded* beside *Ideas from the desk's AI*.
- **The right column's boxes span it** (lead's tweak on the same branch, after the first
  3800x2000 render): the reader and *External forecast* stopped at about 420 px of an 890 px
  column because of the G3 100-character cap. `refresh_reader_measure` keeps its name and its
  `_apply_scaled_metrics` caller and now clears the cap instead of setting one; the Market
  Journal's own reader is untouched and `test_g3_market_journal_reader.py` still pins it.
- **The headers.** "Against me first %" printed as "ainst me first" because the shared width
  rule clamps a column at 260 px and that header hints 273 px under the desk theme; a header
  is centred, so it clipped at both ends with no ellipsis. Both tables on this page now
  measure their columns (`ResizeToContents`, set once at construction) and stretch the last
  section, so they fill their cell and print their headers whole. The shared rule is
  untouched and still serves every other table on the desk.
- **What did NOT change.** `day_review_service.py`, `day_review_index.py`, every reader,
  worker, schedule and signal, and every `tests/test_tj1_*.py` (byte-identical, green). One
  `CandleChart` built on first need and reused, re-asserted in the new test file.
- **Proof.** `tests/test_tj1l_day_review_layout.py`, 21 tests, committed RED first
  (`04cdca56`): 17 failed, 3 passed, 1 error with `day_review_panel.py` reverted to `main`'s
  copy; 21 passed with it restored. The three that pass either way are named as guards.
- **Bench** (`desk_bench.py`, staged copy of the home folder, 1900x1000, seven repeats):
  construct settle p50 **33.6 -> 53.3 ms** (two splitters and ~15 more widgets, once per desk
  start; a direct profile puts all of it in `_build_layout`, 23.6 -> 38.4 ms for the whole
  panel), first show settle p50 **900 -> 864 ms**, `day_review.reload` **876 -> 869 ms**,
  entry click **122 ms** unchanged. The two numbers the trader feels did not move; construct
  is the one that did, and it is named here rather than rounded away.
- **Renders** (offscreen, staged home): `day_review_after_1900x1000.png` and
  `day_review_after_3800x2000.png`, with `..._before_...` beside them for comparison, in the
  session scratchpad; the before pair shows the empty right-hand half and the clipped
  headers the trader was looking at.
- **What remains.** The lead's diff check, a reviewer, and the merge; the live check folds
  into gate #145.

---

## Earlier entries

This file holds the last **three build days** (rule set 2026-09-05; before it, the rule
was "older than the oldest open gate", which never fired while gate #2 stayed open).
Everything dated **2026-09-03 to 2026-09-17** (78 entries) moved to
[`docs/archive/CHECKPOINT_ARCHIVE_2026-09-03_2026-09-17.md`](docs/archive/CHECKPOINT_ARCHIVE_2026-09-03_2026-09-17.md)
on 2026-09-20; everything dated **2026-09-01 and 2026-09-02** (22 entries) moved to
[`docs/archive/CHECKPOINT_ARCHIVE_2026-09-01_2026-09-02.md`](docs/archive/CHECKPOINT_ARCHIVE_2026-09-01_2026-09-02.md)
on 2026-09-05; everything dated **2026-08-31 and earlier** is in
[`docs/archive/CHECKPOINT_ARCHIVE_2026-08.md`](docs/archive/CHECKPOINT_ARCHIVE_2026-08.md).
The gates tables above still name those entries in their "Owed by" column; read them in
the archive. The archive is evidence, not authority.
