-r requirements-gui.txt
pytest
pytest-timeout
# Read-only research-lake query engine (warehouse Phase 7, LD-04). Optional:
# the read path answers every slice query through pyarrow without it.
duckdb
pyinstaller

# The linter pyproject.toml configures. CLAUDE.md names ruff as part of the stack,
# but it was never declared here, so `uv pip install -r requirements-dev.txt` produced
# an environment that cannot run the configured lint.
ruff
